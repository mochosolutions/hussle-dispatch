import Profile from './Profile';

const HeaderContent = () => (
  <div
    style={{
      display: 'flex',
      alignItems: 'center',
      marginLeft: 'auto',
    }}
  >
    <Profile />
  </div>
);

export default HeaderContent;
