import emStyled from "@emotion/styled";
import { serializeStyles } from "../../@emotion/serialize/dist/emotion-serialize.esm.js";
import { ThemeContext, css, keyframes } from "@emotion/react";
import { default as default2 } from "./StyledEngineProvider/StyledEngineProvider.js";
import { default as default3 } from "./GlobalStyles/GlobalStyles.js";
/**
 * @mui/styled-engine v5.18.0
 *
 * @license MIT
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
function styled(tag, options) {
  const stylesFactory = emStyled(tag, options);
  if (process.env.NODE_ENV !== "production") {
    return (...styles) => {
      const component = typeof tag === "string" ? `"${tag}"` : "component";
      if (styles.length === 0) {
        console.error([`MUI: Seems like you called \`styled(${component})()\` without a \`style\` argument.`, 'You must provide a `styles` argument: `styled("div")(styleYouForgotToPass)`.'].join("\n"));
      } else if (styles.some((style) => style === void 0)) {
        console.error(`MUI: the styled(${component})(...args) API requires all its args to be defined.`);
      }
      return stylesFactory(...styles);
    };
  }
  return stylesFactory;
}
const internal_processStyles = (tag, processor) => {
  if (Array.isArray(tag.__emotion_styles)) {
    tag.__emotion_styles = processor(tag.__emotion_styles);
  }
};
const wrapper = [];
function internal_serializeStyles(styles) {
  wrapper[0] = styles;
  return serializeStyles(wrapper);
}
export {
  default3 as GlobalStyles,
  default2 as StyledEngineProvider,
  ThemeContext,
  css,
  styled as default,
  internal_processStyles,
  internal_serializeStyles,
  keyframes
};
//# sourceMappingURL=index.js.map
