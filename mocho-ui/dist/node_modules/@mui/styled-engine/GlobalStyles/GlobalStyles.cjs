"use strict";
require("react");
const index = require("../../../../_virtual/index.cjs");
const react = require("@emotion/react");
const require$$2 = require("react/jsx-runtime");
function isEmpty(obj) {
  return obj === void 0 || obj === null || Object.keys(obj).length === 0;
}
function GlobalStyles(props) {
  const {
    styles,
    defaultTheme = {}
  } = props;
  const globalStyles = typeof styles === "function" ? (themeInput) => styles(isEmpty(themeInput) ? defaultTheme : themeInput) : styles;
  return /* @__PURE__ */ require$$2.jsx(react.Global, {
    styles: globalStyles
  });
}
process.env.NODE_ENV !== "production" ? GlobalStyles.propTypes = {
  defaultTheme: index.object,
  styles: index.oneOfType([index.array, index.string, index.object, index.func])
} : void 0;
module.exports = GlobalStyles;
//# sourceMappingURL=GlobalStyles.cjs.map
