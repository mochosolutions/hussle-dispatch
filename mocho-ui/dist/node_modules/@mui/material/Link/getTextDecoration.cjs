"use strict";
Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: "Module" } });
const system = require("@mui/system");
const colorManipulator = require("../../../../_virtual/colorManipulator.cjs");
const colorTransformations = {
  primary: "primary.main",
  textPrimary: "text.primary",
  secondary: "secondary.main",
  textSecondary: "text.secondary",
  error: "error.main"
};
const transformDeprecatedColors = (color) => {
  return colorTransformations[color] || color;
};
const getTextDecoration = ({
  theme,
  ownerState
}) => {
  const transformedColor = transformDeprecatedColors(ownerState.color);
  const color = system.getPath(theme, `palette.${transformedColor}`, false) || ownerState.color;
  const channelColor = system.getPath(theme, `palette.${transformedColor}Channel`);
  if ("vars" in theme && channelColor) {
    return `rgba(${channelColor} / 0.4)`;
  }
  return colorManipulator.colorManipulatorExports.alpha(color, 0.4);
};
exports.colorTransformations = colorTransformations;
exports.default = getTextDecoration;
//# sourceMappingURL=getTextDecoration.cjs.map
