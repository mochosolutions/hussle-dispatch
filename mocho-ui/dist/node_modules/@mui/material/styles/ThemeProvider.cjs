"use strict";
const _extends = require("../../../@babel/runtime/helpers/esm/extends.cjs");
const objectWithoutPropertiesLoose = require("../../../@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.cjs");
require("react");
const index = require("../../../../_virtual/index.cjs");
const system = require("@mui/system");
const identifier = require("./identifier.cjs");
const require$$2 = require("react/jsx-runtime");
const _excluded = ["theme"];
function ThemeProvider(_ref) {
  let {
    theme: themeInput
  } = _ref, props = objectWithoutPropertiesLoose(_ref, _excluded);
  const scopedTheme = themeInput[identifier];
  let finalTheme = scopedTheme || themeInput;
  if (typeof themeInput !== "function") {
    if (scopedTheme && !scopedTheme.vars) {
      finalTheme = _extends({}, scopedTheme, {
        vars: null
      });
    } else if (themeInput && !themeInput.vars) {
      finalTheme = _extends({}, themeInput, {
        vars: null
      });
    }
  }
  return /* @__PURE__ */ require$$2.jsx(system.ThemeProvider, _extends({}, props, {
    themeId: scopedTheme ? identifier : void 0,
    theme: finalTheme
  }));
}
process.env.NODE_ENV !== "production" ? ThemeProvider.propTypes = {
  /**
   * Your component tree.
   */
  children: index.node,
  /**
   * A theme object. You can provide a function to extend the outer theme.
   */
  theme: index.oneOfType([index.object, index.func]).isRequired
} : void 0;
module.exports = ThemeProvider;
//# sourceMappingURL=ThemeProvider.cjs.map
