"use strict";
const system = require("@mui/system");
const index = require("../../../../_virtual/index.cjs");
const identifier = require("../styles/identifier.cjs");
const boxClasses = require("./boxClasses.cjs");
const ClassNameGenerator = require("../node_modules/@mui/utils/esm/ClassNameGenerator/ClassNameGenerator.cjs");
const createTheme = require("../styles/createTheme.cjs");
const defaultTheme = createTheme();
const Box = system.createBox({
  themeId: identifier,
  defaultTheme,
  defaultClassName: boxClasses.root,
  generateClassName: ClassNameGenerator.generate
});
process.env.NODE_ENV !== "production" ? Box.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * @ignore
   */
  children: index.node,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: index.elementType,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: index.oneOfType([index.arrayOf(index.oneOfType([index.func, index.object, index.bool])), index.func, index.object])
} : void 0;
module.exports = Box;
//# sourceMappingURL=Box.cjs.map
