"use strict";
const objectWithoutPropertiesLoose = require("../../../@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.cjs");
const _extends = require("../../../@babel/runtime/helpers/esm/extends.cjs");
const React = require("react");
const index = require("../../../../_virtual/index.cjs");
const clsx = require("../../../clsx/dist/clsx.cjs");
const styled = require("../styles/styled.cjs");
const ListContext = require("./ListContext.cjs");
const listClasses = require("./listClasses.cjs");
const require$$2 = require("react/jsx-runtime");
const DefaultPropsProvider = require("../DefaultPropsProvider/DefaultPropsProvider.cjs");
const composeClasses = require("../node_modules/@mui/utils/esm/composeClasses/composeClasses.cjs");
function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: "Module" } });
  if (e) {
    for (const k in e) {
      if (k !== "default") {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}
const React__namespace = /* @__PURE__ */ _interopNamespaceDefault(React);
const _excluded = ["children", "className", "component", "dense", "disablePadding", "subheader"];
const useUtilityClasses = (ownerState) => {
  const {
    classes,
    disablePadding,
    dense,
    subheader
  } = ownerState;
  const slots = {
    root: ["root", !disablePadding && "padding", dense && "dense", subheader && "subheader"]
  };
  return composeClasses(slots, listClasses.getListUtilityClass, classes);
};
const ListRoot = styled.default("ul", {
  name: "MuiList",
  slot: "Root",
  overridesResolver: (props, styles) => {
    const {
      ownerState
    } = props;
    return [styles.root, !ownerState.disablePadding && styles.padding, ownerState.dense && styles.dense, ownerState.subheader && styles.subheader];
  }
})(({
  ownerState
}) => _extends({
  listStyle: "none",
  margin: 0,
  padding: 0,
  position: "relative"
}, !ownerState.disablePadding && {
  paddingTop: 8,
  paddingBottom: 8
}, ownerState.subheader && {
  paddingTop: 0
}));
const List = /* @__PURE__ */ React__namespace.forwardRef(function List2(inProps, ref) {
  const props = DefaultPropsProvider.useDefaultProps({
    props: inProps,
    name: "MuiList"
  });
  const {
    children,
    className,
    component = "ul",
    dense = false,
    disablePadding = false,
    subheader
  } = props, other = objectWithoutPropertiesLoose(props, _excluded);
  const context = React__namespace.useMemo(() => ({
    dense
  }), [dense]);
  const ownerState = _extends({}, props, {
    component,
    dense,
    disablePadding
  });
  const classes = useUtilityClasses(ownerState);
  return /* @__PURE__ */ require$$2.jsx(ListContext.Provider, {
    value: context,
    children: /* @__PURE__ */ require$$2.jsxs(ListRoot, _extends({
      as: component,
      className: clsx.clsx(classes.root, className),
      ref,
      ownerState
    }, other, {
      children: [subheader, children]
    }))
  });
});
process.env.NODE_ENV !== "production" ? List.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: index.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: index.object,
  /**
   * @ignore
   */
  className: index.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: index.elementType,
  /**
   * If `true`, compact vertical padding designed for keyboard and mouse input is used for
   * the list and list items.
   * The prop is available to descendant components as the `dense` context.
   * @default false
   */
  dense: index.bool,
  /**
   * If `true`, vertical padding is removed from the list.
   * @default false
   */
  disablePadding: index.bool,
  /**
   * The content of the subheader, normally `ListSubheader`.
   */
  subheader: index.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: index.oneOfType([index.arrayOf(index.oneOfType([index.func, index.object, index.bool])), index.func, index.object])
} : void 0;
module.exports = List;
//# sourceMappingURL=List.cjs.map
