"use strict";
const index = require("../../../../_virtual/index.cjs");
const system = require("@mui/system");
const styled = require("../styles/styled.cjs");
const DefaultPropsProvider = require("../DefaultPropsProvider/DefaultPropsProvider.cjs");
const Stack = system.createStack({
  createStyledComponent: styled.default("div", {
    name: "MuiStack",
    slot: "Root",
    overridesResolver: (props, styles) => styles.root
  }),
  useThemeProps: (inProps) => DefaultPropsProvider.useDefaultProps({
    props: inProps,
    name: "MuiStack"
  })
});
process.env.NODE_ENV !== "production" ? Stack.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: index.node,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: index.elementType,
  /**
   * Defines the `flex-direction` style property.
   * It is applied for all screen sizes.
   * @default 'column'
   */
  direction: index.oneOfType([index.oneOf(["column-reverse", "column", "row-reverse", "row"]), index.arrayOf(index.oneOf(["column-reverse", "column", "row-reverse", "row"])), index.object]),
  /**
   * Add an element between each child.
   */
  divider: index.node,
  /**
   * Defines the space between immediate children.
   * @default 0
   */
  spacing: index.oneOfType([index.arrayOf(index.oneOfType([index.number, index.string])), index.number, index.object, index.string]),
  /**
   * The system prop, which allows defining system overrides as well as additional CSS styles.
   */
  sx: index.oneOfType([index.arrayOf(index.oneOfType([index.func, index.object, index.bool])), index.func, index.object]),
  /**
   * If `true`, the CSS flexbox `gap` is used instead of applying `margin` to children.
   *
   * While CSS `gap` removes the [known limitations](https://mui.com/joy-ui/react-stack/#limitations),
   * it is not fully supported in some browsers. We recommend checking https://caniuse.com/?search=flex%20gap before using this flag.
   *
   * To enable this flag globally, follow the [theme's default props](https://mui.com/material-ui/customization/theme-components/#default-props) configuration.
   * @default false
   */
  useFlexGap: index.bool
} : void 0;
module.exports = Stack;
//# sourceMappingURL=Stack.cjs.map
