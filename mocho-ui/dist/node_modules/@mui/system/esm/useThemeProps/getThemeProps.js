import resolveProps from "../../node_modules/@mui/utils/esm/resolveProps/resolveProps.js";
function getThemeProps(params) {
  const {
    theme,
    name,
    props
  } = params;
  if (!theme || !theme.components || !theme.components[name] || !theme.components[name].defaultProps) {
    return props;
  }
  return resolveProps(theme.components[name].defaultProps, props);
}
export {
  getThemeProps as default
};
//# sourceMappingURL=getThemeProps.js.map
