import deepmerge from "../node_modules/@mui/utils/esm/deepmerge/deepmerge.js";
function merge(acc, item) {
  if (!item) {
    return acc;
  }
  return deepmerge(acc, item, {
    clone: false
    // No need to clone deep, it's way faster.
  });
}
export {
  merge as default
};
//# sourceMappingURL=merge.js.map
