"use strict";
const _extends = require("../../../../@babel/runtime/helpers/esm/extends.cjs");
const objectWithoutPropertiesLoose = require("../../../../@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.cjs");
const createBreakpoints = require("./createBreakpoints.cjs");
const shape = require("./shape.cjs");
const createSpacing = require("./createSpacing.cjs");
const styleFunctionSx = require("../styleFunctionSx/styleFunctionSx.cjs");
const defaultSxConfig = require("../styleFunctionSx/defaultSxConfig.cjs");
const applyStyles = require("./applyStyles.cjs");
const deepmerge = require("../../node_modules/@mui/utils/esm/deepmerge/deepmerge.cjs");
const _excluded = ["breakpoints", "palette", "spacing", "shape"];
function createTheme(options = {}, ...args) {
  const {
    breakpoints: breakpointsInput = {},
    palette: paletteInput = {},
    spacing: spacingInput,
    shape: shapeInput = {}
  } = options, other = objectWithoutPropertiesLoose(options, _excluded);
  const breakpoints = createBreakpoints(breakpointsInput);
  const spacing = createSpacing(spacingInput);
  let muiTheme = deepmerge.default({
    breakpoints,
    direction: "ltr",
    components: {},
    // Inject component definitions.
    palette: _extends({
      mode: "light"
    }, paletteInput),
    spacing,
    shape: _extends({}, shape, shapeInput)
  }, other);
  muiTheme.applyStyles = applyStyles;
  muiTheme = args.reduce((acc, argument) => deepmerge.default(acc, argument), muiTheme);
  muiTheme.unstable_sxConfig = _extends({}, defaultSxConfig, other == null ? void 0 : other.unstable_sxConfig);
  muiTheme.unstable_sx = function sx(props) {
    return styleFunctionSx.default({
      sx: props,
      theme: this
    });
  };
  return muiTheme;
}
module.exports = createTheme;
//# sourceMappingURL=createTheme.cjs.map
