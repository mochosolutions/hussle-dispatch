"use strict";
const generateUtilityClass = require("../generateUtilityClass/generateUtilityClass.cjs");
function generateUtilityClasses(componentName, slots, globalStatePrefix = "Mui") {
  const result = {};
  slots.forEach((slot) => {
    result[slot] = generateUtilityClass.default(componentName, slot, globalStatePrefix);
  });
  return result;
}
module.exports = generateUtilityClasses;
//# sourceMappingURL=generateUtilityClasses.cjs.map
