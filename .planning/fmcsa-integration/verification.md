# fmcsa-integration — Verification Report

> Generated by /feature-verify on 2026-05-14
> Workspace type: feature (from /build)

## Summary
- Total must_haves checked: **58**
  - truths: 29
  - artifacts: 14
  - key_links: 15
- VERIFIED: **58**
- FAILED (BLOCKER): **0**
- UNCERTAIN (WARNING): **0**

**Verdict: SHIP**

Every story's must_haves contract has a corresponding code path or test assertion. No stubs (the one intentional stub, `saferWebApiProvider`, is contractually a stub per PRD § P1 and is covered by tests asserting the throw behavior).

---

## VERIFIED — US-01 (types and ports)

### truths
- **"Importing FmcsaSnapshot, FmcsaLookupResult, FmcsaService, FmcsaPort, CachePort compiles without any/as/!"** —
  - `types.ts:1-52` defines all five symbols cleanly.
  - `fmcsaService.ts:14-17` imports them via `import type` with no casts.
  - `npm run check-ts` exits 0 across the API package.

- **"FmcsaLookupResult discriminated union narrows on .status"** —
  - `fmcsaService.ts:146` reads `if (result.status === 'found')` and TS narrows `result.snapshot` access on the next line (no `!`, no `as`).
  - `__tests__/fmcsaPort.contract.test.ts:18` similarly narrows.

### artifacts
- `src/shared/fmcsa/types.ts` — 52 lines, defines 8 interfaces + 1 union type. Substantive.
- `src/shared/fmcsa/fmcsaPort.ts` — 6 lines, exports `FmcsaPort` interface mirroring `routeCalculatorPort.ts` shape.
- `src/shared/fmcsa/cachePort.ts` — 7 lines, exports `CachePort` interface scoped to `FmcsaSnapshot`.

### key_links
- **FmcsaService → FmcsaPort via type composition** — `fmcsaService.ts:17,43` imports and uses in deps.
- **FmcsaService → CachePort via type composition** — `fmcsaService.ts:16,44` imports and uses in deps.

---

## VERIFIED — US-02 (EventMap events)

### truths
- **"EventMap['fmcsa.lookup.completed'] resolves to typed payload with correlationId, identifier, result discriminated"** —
  - `eventMap.ts:216-222` defines the entry with the discriminated `result` union.
- **"EventMap['fmcsa.lookup.failed'] resolves to typed payload with correlationId, identifier, reason"** —
  - `eventMap.ts:223-227` defines the entry with reason union `'timeout' | 'rate_limit' | 'provider_error'`.

---

## VERIFIED — US-03 (env var)

### truths
- **"env.FMCSA_PROVIDER imports as 'mock' | 'safer-web' with default 'mock'"** —
  - `src/config/env.ts:57` — `FMCSA_PROVIDER: getEnv('FMCSA_PROVIDER', 'mock') as 'mock' | 'safer-web'`.
  - `.env.example:60` — documented alongside `STORAGE_BACKEND` / `SMS_BACKEND`.

---

## VERIFIED — US-04 (Redis cache adapter)

### truths
- **"set + get round-trip returns deep-equal snapshot"** — `__tests__/cachePort.test.ts` "round-trip behavior" suite (asserts deep equality including Date instance).
- **"del clears the entry"** — `cachePort.test.ts` "del" suite.
- **"get on missing key returns null"** — `cachePort.test.ts` "get on missing key" suite.
- **"Transport errors swallowed and logged via Logger; .get returns null on error"** — `redisCacheAdapter.ts:119-123` catches, logs warn, returns null. Set/del at L129-141 catch and log, return undefined.
- **"lastCheckedAt preserved as Date through round-trip"** — `redisCacheAdapter.ts:103` reads `lastCheckedAt: new Date(lastCheckedAt)` in `reviveSnapshot`; verified by round-trip test asserting `result.lastCheckedAt instanceof Date`.

### artifacts
- `src/shared/fmcsa/redisCacheAdapter.ts` — 144 lines: `RedisLike` type, type-guard chain, `createRedisCacheAdapter` factory. Substantive.
- `src/shared/fmcsa/__tests__/cachePort.test.ts` — 220 lines, 18+ test cases including parameterized type-guard rejection branches.

### key_links
- **redisCacheAdapter → RedisLike (and onward redisClient)** — `compositionRoot.ts:7-8,36` imports and constructs; `index.ts:3,12` passes `redisClient`. Real ioredis structurally satisfies `RedisLike`.
- **redisCacheAdapter implements CachePort** — `redisCacheAdapter.ts:108` return type is `CachePort`.

---

## VERIFIED — US-05 (providers + contract test)

### truths
- **"MockFmcsaProvider returns deterministic FmcsaSnapshot"** — `mockFmcsaProvider.test.ts` determinism group (3 cases asserting deep equality across repeated calls); module covers `mockFmcsaProvider.ts` at 100%.
- **"Same input → identical snapshot whether passed as 'MC-1234567' or '1234567' (provider treats input verbatim)"** — Verified at the SERVICE boundary in US-06 normalization (provider sees normalized digits); contract test exercises provider directly with already-normalized inputs.
- **"Reserved markers trigger failure modes"** — `mockFmcsaProvider.ts:47-55` matches `'TIMEOUT'` → error/timeout, `'RATELIMIT'` → error/rate_limit, `'NOTFOUND'` + `'0000000'` → not_found. `mockFmcsaProvider.test.ts` "failure markers" group covers all four for both mc and dot lookups (8 cases).
- **"SaferWebApiProvider throws 'not implemented'"** — `saferWebApiProvider.ts:6-7,11,14` defines `NOT_IMPLEMENTED_MESSAGE` and throws on both methods. `__tests__/saferWebApiProvider.test.ts` asserts both methods reject with `/not implemented/i`.
- **"fmcsaPort.contract.test.ts validates discriminated FmcsaLookupResult contract"** — `__tests__/fmcsaPort.contract.test.ts` exports `runFmcsaPortContract(name, makeProvider)` helper and registers it against the mock; 3 suite tests pass.

### artifacts
- `src/shared/fmcsa/mockFmcsaProvider.ts` — 62 lines, deterministic snapshot builder, marker matcher, exports factory. Substantive.
- `src/shared/fmcsa/saferWebApiProvider.ts` — 16 lines, throwing stub. Intentionally minimal per PRD § P1; covered by tests; not a violation.
- `src/shared/fmcsa/__tests__/mockFmcsaProvider.test.ts` — 133 lines, 12 tests.
- `src/shared/fmcsa/__tests__/fmcsaPort.contract.test.ts` — 63 lines, reusable contract helper + 3 registered tests.

### key_links
- **mockFmcsaProvider implements FmcsaPort** — `mockFmcsaProvider.ts:1,59` (import type + return type annotation).
- **saferWebApiProvider implements FmcsaPort** — `saferWebApiProvider.ts:1,9`.
- **fmcsaPort.contract.test.ts asserts FmcsaPort compliance** — file uses generic `provider: FmcsaPort` and runs full union narrowing against it.

---

## VERIFIED — US-06 (fmcsaService)

### truths
- **"Normalization: 'MC-1234567', 'mc 1234567', '  1234567  ' all reduce to '1234567'"** — `fmcsaService.ts:32` `normalize = (s) => s.replace(/\D/g, '')`; `validateAndBuildIdentifier` at L164-172 calls it. `fmcsaService.test.ts` normalization group asserts all four input variants produce identical `cache.get(key)` calls with `'fmcsa:mc:1234567'`.
- **"On found, snapshot written to both fmcsa:mc:{mc} AND fmcsa:dot:{dot} with 86400s TTL"** — `fmcsaService.ts:113-124` `cacheFoundResult` writes both keys when `dotNumber` is non-empty, with `CACHE_TTL_SECONDS = 86_400` (L26). Verified in fmcsaService.test.ts dual-key cache group.
- **"Second lookup of either identifier hits CachePort.get; provider NOT called"** — fmcsaService.test.ts "cache hit short-circuits provider" group (mc-key hit and dot-key hit both verified; `provider.lookupByMcNumber.not.toHaveBeenCalled()`).
- **"not_found and error results never cached"** — fmcsaService.test.ts "negative results not cached" group asserts `cache.set` never called for those branches.
- **"Service publishes EXACTLY ONE event per lookup: completed for found/not_found, failed for error"** — `fmcsaService.ts:62,77` `eventBus.publish('fmcsa.lookup.completed' | 'fmcsa.lookup.failed', ...)`. Events group in test (8+ cases).
- **"correlationId: caller-supplied flows verbatim; UUID generated when omitted"** — `fmcsaService.ts:132` `correlationId = opts.correlationId ?? uuid()`; test asserts both branches.
- **"Retry: up to 3 attempts with 1s/5s/30s backoff before surfacing error; not_found surfaces without retry"** — `fmcsaService.ts:27,94-109` retry loop with `RETRY_DELAYS_MS = [1_000, 5_000, 30_000]`; loop bounded by `attempt < RETRY_DELAYS_MS.length`. Test asserts: error→error→found returns found with 2 sleeps; error×4 exhausts with 3 sleeps; not_found never retries.
- **"opts.skipCache bypasses CachePort.get; still writes on success"** — `fmcsaService.ts:135` `if (opts.skipCache !== true)` guards the read; the write path (`cacheFoundResult`) runs unconditionally on found. Test asserts cache.get NOT called when skipCache=true; cache.set STILL called on found.
- **"invalidateCache(identifier) calls CachePort.del on the corresponding key"** — `fmcsaService.ts:180` `cache.del(buildKey(identifier))`. Test asserts del called with `fmcsa:mc:1234567` and `fmcsa:dot:9234567` respectively.

### artifacts
- `src/shared/fmcsa/fmcsaService.ts` — 182 lines, factory + helpers (`buildKey`, `normalize`, `defaultSleep`, `cacheFoundResult`, `callProviderWithRetry`, `lookupAndEmit`, `validateAndBuildIdentifier`). Substantive.
- `src/shared/fmcsa/__tests__/fmcsaService.test.ts` — 421 lines, 25 test cases across normalization, dual-key cache, cache hit, negative results, retry, events, skipCache, invalidateCache, validation.

### key_links
- **fmcsaService → FmcsaPort via deps.provider** — `fmcsaService.ts:17,43`.
- **fmcsaService → CachePort via deps.cache** — `fmcsaService.ts:16,44`.
- **fmcsaService → EventBus.publish via deps.eventBus** — `fmcsaService.ts:13,45,62,77` (import + dep + two publish call sites).

---

## VERIFIED — US-07 (composition + bootstrap)

### truths
- **"createFmcsaModule({ eventBus, logger, redis, providerKind }) returns { service: FmcsaService }"** — `compositionRoot.ts:14-23,34-44`. Confirmed by `__tests__/compositionRoot.test.ts` (3 cases).
- **"src/app.ts side-effect-imports '@/shared/fmcsa' alongside './audit' and './notifications'"** — `app.ts:33` shows `import '@/shared/fmcsa';` directly after the two existing side-effect imports.
- **"Barrel exposes only fmcsaService and types — no providers or adapter"** — `index.ts:16-24` exports `fmcsaService` instance plus type re-exports (`FmcsaSnapshot`, `FmcsaLookupResult`, etc.). No re-exports of provider factories or `createRedisCacheAdapter`.
- **"Module integrates without prisma, without DB writes, without HTTP endpoints"** — grep for `prisma|@prisma/client|express|Router` in `src/shared/fmcsa/` returns zero hits; dependency-cruiser `no-prisma-in-services` rule passes.
- **"When FMCSA_PROVIDER=safer-web, composition wires the stub; service construction does not throw; throw happens on first lookup"** — `compositionRoot.test.ts` "safer-web wiring constructs without throw" and "throws on first lookup invocation" cases both pass.

### artifacts
- `src/shared/fmcsa/compositionRoot.ts` — 44 lines, exhaustive `selectProvider` switch, factory wiring. Substantive.
- `src/shared/fmcsa/index.ts` — 24 lines, side-effect bootstrap from singletons + env. Minimal but correct.
- `src/app.ts` — 1-line side-effect import addition at L33; rest of file unchanged.

### key_links
- **compositionRoot → createMockFmcsaProvider / createSaferWebApiProvider via selectProvider switch** — `compositionRoot.ts:6,9,25-32`.
- **compositionRoot → createRedisCacheAdapter via direct construction** — `compositionRoot.ts:7,36`.
- **compositionRoot → createFmcsaService via factory call** — `compositionRoot.ts:4,37-42`.
- **index.ts → compositionRoot via createFmcsaModule(...) and exports fmcsaService** — `index.ts:6,9-15,16`.
- **app.ts → index.ts via side-effect import** — `app.ts:33`.

---

## Coverage cross-check (PRD AC #6)

| File | Lines | Branches | Functions |
|---|---|---|---|
| compositionRoot.ts | 100% | 100% | 100% |
| fmcsaService.ts | 95.52% | 95.65% | 83.33% |
| mockFmcsaProvider.ts | 100% | 90.9% | 100% |
| redisCacheAdapter.ts | 100% | 91.66% | 100% |
| saferWebApiProvider.ts | 100% | 100% | 100% |
| index.ts | 0% | 100% | 100% |
| **Module total** | **94.53%** | **93.05%** | **92.68%** |

- ✓ Module line coverage 94.53% exceeds the 90% target.
- ✓ Contract paths (port contract test + fmcsaService + mock provider + event emission) all ≥ 95.5% lines.
- `index.ts` at 0% is the only gap — it's a singleton bootstrap that executes only at app boot; intentionally not unit-testable in isolation. Acceptable per PRD.

## fmcsaService.ts function coverage note (83.33%)

The 83.33% function coverage on `fmcsaService.ts` is below the line-coverage of 95.52%. Investigated: the gap is the optional injected helpers (`uuid`, `sleep`) — the `??` fallback paths are exercised but the default-export functions (`defaultSleep`, `randomUUID`) themselves are passed but not called inside tests (tests inject their own). This is intentional test design — the tests assert via `sleep` jest.fn() calls, not real timing. Not a verification concern.

---

## Verdict

**SHIP** — all 58 must_haves entries trace to evidence; no BLOCKER, no WARNING; coverage above target; dependency-cruiser clean for the fmcsa module.
