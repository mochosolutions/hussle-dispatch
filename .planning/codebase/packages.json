{
  "generatedAt": "2026-03-25T00:00:00.000Z",
  "ecosystem": "js",
  "monorepo": true,
  "rootCommands": {
    "validateAll": null,
    "buildAll": null,
    "testAll": null
  },
  "packages": [
    {
      "serviceKey": "dispatch-api",
      "name": "hussle-app-dispatch-api",
      "path": "hussle-app-dispatch-api/",
      "type": "api",
      "stack": ["express", "prisma", "redis", "rabbitmq"],
      "manifest": "package.json",
      "commands": {
        "test": "cd hussle-app-dispatch-api && npx jest",
        "lint": "cd hussle-app-dispatch-api && npx eslint src/",
        "typecheck": "cd hussle-app-dispatch-api && npx tsc --noEmit",
        "build": "cd hussle-app-dispatch-api && npx tsc",
        "testRelated": "cd hussle-app-dispatch-api && npx jest --findRelatedTests",
        "validate": "cd hussle-app-dispatch-api && npm run validate"
      }
    },
    {
      "serviceKey": "dispatch-ui",
      "name": "hussle-app-dispatch-ui",
      "path": "hussle-app-dispatch-ui/",
      "type": "frontend",
      "stack": ["react", "vite", "mui", "redux-toolkit", "redux-saga", "formik"],
      "manifest": "package.json",
      "commands": {
        "test": "cd hussle-app-dispatch-ui && npx jest",
        "lint": "cd hussle-app-dispatch-ui && npx eslint .",
        "typecheck": "cd hussle-app-dispatch-ui && npx tsc --noEmit -p tsconfig.app.json",
        "build": "cd hussle-app-dispatch-ui && npx tsc -b && npx vite build",
        "testRelated": "cd hussle-app-dispatch-ui && npx jest --findRelatedTests",
        "validate": "cd hussle-app-dispatch-ui && npm run validate"
      }
    },
    {
      "serviceKey": "emails",
      "name": "@hussle/emails",
      "path": "hussle-emails/",
      "type": "library",
      "stack": ["react-email"],
      "manifest": "package.json",
      "commands": {
        "test": null,
        "lint": null,
        "typecheck": null,
        "build": "cd hussle-emails && npx tsc",
        "testRelated": null
      }
    },
    {
      "serviceKey": "extension",
      "name": "fleetcommand-extension",
      "path": "extension/",
      "type": "library",
      "stack": ["chrome-extension", "esbuild"],
      "manifest": "package.json",
      "commands": {
        "test": "cd extension && node --experimental-vm-modules node_modules/.bin/jest",
        "lint": null,
        "typecheck": null,
        "build": "cd extension && node build.mjs",
        "testRelated": "cd extension && npx jest --findRelatedTests"
      }
    }
  ]
}
