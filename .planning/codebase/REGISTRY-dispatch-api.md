# Registry: hussle-app-dispatch-api

> Auto-generated by /bootstrap on 2026-05-13.
> Express + Prisma modular monolith. 25 feature modules, 40 Prisma models, 30+ services, 60+ routes.

**Architecture:** see `hussle-app-dispatch-api/CLAUDE.md` for the full module pattern (mappers → services → repos via ports + per-module compositionRoot). The patterns below are extracted from real code; follow them exactly.

## Feature Modules

Per-module structure: `<feature>/{controllers/,services/,repositories/,routes/,validators/,types/,events/,compositionRoot.ts,__tests__/}`.

| Module | Purpose | Key Service | Has compositionRoot |
|---|---|---|---|
| `api-keys` | Org-scoped API key issuance + verification | apiKeyService | ✓ |
| `audit` | Append-only audit log + subscriber | auditService | ✓ |
| `auth` | JWT login, refresh, invitation flow, organization signup | userService, inviteService, organizationService | ✓ |
| `carrier-portal` | Magic-link onboarding (session, per-phase saves) | sessionService, phaseService | ✓ |
| `carriers` | Carrier CRUD + onboarding + approval + suspend + dispatch override | carrierService, carrierApprovalService, carrierSuspendService, carrierInviteService, dispatchOverrideService | ✓ |
| `contacts` | Contact CRUD per org | contactService | ✓ |
| `customers` | Customer CRUD + notification settings | customerService | ✓ |
| `dashboard` | KPIs, weekly gross, attention items | dashboardService | ✓ |
| `documents` | Presign + confirm + list + signature | documentService | ✓ |
| `driver-portal` | Driver-app session + check-in flow | (portal-specific) | ✓ |
| `drivers` | Driver CRUD + schedule + availability | driverService | ✓ |
| `expenses` | Truck expenses, recurring, driver-portal expenses | expenseService | ✓ |
| `ifta` | Fuel/state-miles tracking, IFTA report generation | iftaReportService, stateMileageService, stateMilesOverrideService | ✓ |
| `invoices` | Auto-generation, PDF, email, payment, document packet | invoiceService, invoiceBuilderService, pdfGenerationService, invoiceEmailService, documentPacketService | ✓ |
| `load-board` | DAT/Relay load ingestion endpoint (extension entrypoint) | (load-board ingest) | ✓ |
| `load-intel` | Intelligence feed, scoring, backhaul, chains | (intel + scoring services in `src/shared/scoring/`) | ✓ |
| `loads` | Load CRUD + state machine + stops + check calls + accessorials + weekly gross | loadService, loadStatusService, stopService, accessorialService, weeklyGrossService | ✓ |
| `maps` | AWS Location map style proxy, route calculator | mapsService | ✓ |
| `notifications` | Email/SMS dispatch + log + subscribers | notificationService | ✓ |
| `places` | Place CRUD + typeahead + geo lookup | placeService | ✓ |
| `settings` | Org settings | settingsService | ✓ |
| `settlements` | Driver settlement calculation + PDF | settlementService | ✓ |
| `short-links` | `/s/:slug` redirect resolver | shortLinkService | ✓ |
| `sms-prompts` | Twilio SMS prompt orchestration | smsPromptService | ✓ |
| `vehicles` | Vehicle CRUD per carrier | vehicleService | ✓ |

## Routes (entry surface)

23 feature-router files under `src/*/routes/*.ts`. Every router is created by `createXxxRouter(controllers)` and consumes pre-wired controllers from the module's composition root.

Mounted in `src/app.ts` under `/api/v1/<resource>`. Public portals:
- `/api/v1/carrier-portal/*` — invite-token auth via `requireAuthOrInviteToken`
- `/api/v1/driver-portal/*` — driver session token

Auth surfaces:
- `/api/v1/auth/*` — login, refresh, signup, organization
- API-key endpoints via `apiKeyAuth` / `sessionOrApiKeyAuth`

## Prisma Models (40 total)

Core domain models (in schema order):

| Model | Purpose |
|---|---|
| `Organization`, `OrgApiKey`, `OrgSettings` | Tenant + per-org config |
| `User`, `Membership`, `Invitation` | Identity + access |
| `AuditLog` | Append-only audit trail |
| `Carrier`, `Contact`, `Driver`, `Vehicle`, `CarrierNote`, `CarrierInviteToken`, `OnboardingSession` | Fleet domain |
| `TruckExpense` | Per-truck expense tracking |
| `Load`, `Stop`, `Place`, `LoadStatusHistory`, `CheckCall`, `AccessorialCharge`, `LoadTrackingToken`, `LoadNotificationOverride` | Dispatch domain |
| `Customer`, `CustomerNotificationSettings` | Broker-side |
| `Invoice`, `Document` | Billing + paperwork |
| `NotificationLog` | Email/SMS send log |
| `DriverAvailability`, `DriverScheduleOverride` | Driver scheduling |
| (+ 10 more for IFTA, settlements, short links, sms prompts) | |

Read `prisma/schema.prisma` for full field-level detail. NEVER hand-define entity types — derive from Prisma per CLAUDE.md.

## Shared Infrastructure

| Path | Purpose |
|---|---|
| `src/shared/errors/` | `CustomError` base + typed errors (`NotFoundError`, `BadRequestError`, `ConflictError`, `UnauthorizedError`, `ForbiddenError`, `RequestValidationError`, `GoneError`, `MissingEnvError`, plus domain-specific like `ActiveLoadsConflictError`, `InsuranceExpiredError`) |
| `src/shared/middleware/errorHandler.ts` | Centralized error → `{ errors: [...] }` JSON |
| `src/shared/middleware/validateRequest.ts` | Yup schema runner |
| `src/shared/middleware/apiKeyAuth.ts`, `sessionOrApiKeyAuth.ts`, `requireAuthOrInviteToken.ts` | Auth variants |
| `src/middleware/auth.ts` | `requireAuth`, `requireRole(ROLES[])` |
| `src/config/roles.ts` | `ROLES.ADMIN`, `ROLES.DISPATCHER`, `ROLES.VIEWER`, etc. |
| `src/shared/messaging/rabbitMqEventBus.ts` | Production typed event bus (`EventMap`) |
| `src/shared/messaging/inMemoryEventBus.ts` | Test-double event bus |
| `src/shared/messaging/sharedEventBus.ts` | Process-wide singleton |
| `src/shared/redisClient.ts` | ioredis singleton |
| `src/shared/utils/logger.ts` | `LoggerPort` injected through compositionRoots |
| `src/shared/pagination.ts` | `parsePaginationParams`, `paginateQuery` |
| `src/shared/onboardingGate.ts` | `checkCarrierOnboarding` |
| `src/shared/storage/index.ts` | S3 / local file storage provider |
| `src/shared/providers/puppeteerBrowserPool.ts` | PDF rendering |
| `src/shared/emails/<template>/<Template>Email.tsx` | React-Email transactional templates |
| `src/shared/scoring/` | Load intel scoring utilities |

## Representative Patterns

### Route pattern
_Source: `src/carriers/routes/carrierRoutes.ts:31-86`_

```typescript
export const createCarriersRouter = (
  controllers: CarrierControllers & InviteControllers & ApprovalControllers
    & OnboardingDetailControllers & DispatchOverrideControllers & SuspendControllers,
): express.Router => {
  const router = express.Router();

  router.post(
    '/',
    requireAuth,
    requireRole([ROLES.ADMIN, ROLES.DISPATCHER]),
    validateRequest(createCarrierValidator),
    controllers.createCarrier,
  );
  router.get('/', requireAuth, validateRequest(listCarriersValidator), controllers.listCarriers);
  router.get('/:id', requireAuth, validateRequest(carrierIdParamValidator), controllers.getCarrierById);
  // ... PATCH, DELETE, sub-resources
  return router;
};
```

**Conventions:** Router factory takes controllers as argument; `requireAuth` first, then `requireRole`, then `validateRequest(<yupSchema>)`, then handler.

### Service pattern (from existing `carrierService.ts`)
_Source: `src/carriers/services/carrierService.ts:36-100ish`_

```typescript
import { BadRequestError } from '@mocho/common';
import { CarrierStatus } from '@prisma/client';
import { ActiveLoadsConflictError, ForbiddenError, NotFoundError } from '@/shared/errors';
import { checkCarrierOnboarding } from '@/shared/onboardingGate';
import { parsePaginationParams, paginateQuery } from '@/shared/pagination';
import type { CarrierRepositoryPort, ... } from '../types/carrierTypes';
import type { CreateCarrierServiceInput, ListCarriersServiceInput, ... } from '../types/carrierServiceTypes';

// Services accept (input, deps) and return Promise<ServiceResult<T>> = { data, events }
// Services NEVER import @prisma/client directly — only port interfaces.
// Services NEVER import repositories directly — injected via compositionRoot.
```

### Composition root pattern
_Source: every `src/*/compositionRoot.ts`_

```typescript
export const createCarrierModule = (deps: {
  prisma: PrismaClient;
  logger: LoggerPort;
  eventBus: EventBusPort;
  inviteTokenRepo: InviteTokenQueryPort;  // cross-module read
}) => {
  const carrierRepo = carrierRepositoryPrisma(deps.prisma);
  const auditPort = carrierAuditPortPrisma(deps.prisma);
  const serviceDeps = { carrierRepo, auditPort, logger: deps.logger };

  const controllers = {
    createCarrier: createCarrierController({ ...serviceDeps, eventBus: deps.eventBus }),
    // ...
  };
  const queries = { findById: carrierRepo.findById, count: carrierRepo.count };
  return { controllers, queries, initializeSubscriber: ... };
};
```

### Validator pattern (Yup)
_Source: `src/<feature>/validators/*Validator.ts`_

```typescript
import * as yup from 'yup';

export const createCarrierValidator = yup.object({
  body: yup.object({
    name: yup.string().required().min(1).max(255),
    mcNumber: yup.string().matches(/^MC\d+$/).required(),
  }),
});
```

### Error class pattern
_Source: `src/shared/errors/commonErrors.ts`_

```typescript
export class NotFoundError extends CustomError {
  statusCode = 404;
  constructor(private entity: string, private id: string) {
    super(`${entity} with id ${id} not found`);
  }
  serializeErrors() { return [{ message: this.message }]; }
}
```

### Integration test pattern
_Source: `src/<feature>/__tests__/integration/<feature>Routes.integration.test.ts`_

```typescript
import request from 'supertest';
import { app } from '../../../app';

describe('POST /api/v1/carriers', () => {
  it('returns 201 with created carrier', async () => {
    const res = await request(app)
      .post('/api/v1/carriers')
      .set('Cookie', [`token=${validToken}`])
      .send({ name: 'Acme Trucking', mcNumber: 'MC123456' });
    expect(res.status).toBe(201);
  });
});
```

## Architecture Enforcement

`dependency-cruiser` (`npm run lint:deps`) blocks these patterns at CI:
- `no-prisma-in-services` — services may not `import from '@prisma/client'`
- `no-repo-implementations-in-services` — services may not import from `repositories/`
- `no-services-to-controllers` — services may not import controllers
- `no-circular` — no circular deps

Run `npm run validate` before completing any task.
