# Code Registry Index

> Auto-generated by /bootstrap on 2026-05-13.

| Package | Type | Registry File | Modules / Features | Patterns Captured |
|---|---|---|---|---|
| hussle-app-dispatch-api | api | `REGISTRY-dispatch-api.md` | 25 feature modules, 40 Prisma models | route, service, compositionRoot, validator, error class, integration test |
| hussle-app-dispatch-ui | frontend | `REGISTRY-dispatch-ui.md` | 18 features, 36 mocho components, 11 hooks | page/route, saga, entity-module, form (Formik+Yup+DynamicForm), component test |
| dat-load-scraper | worker (Chrome MV3) | `REGISTRY-dat-load-scraper.md` | popup + content scripts + service worker | content-script entry, popup, site-scraper |

## Quick Pointers

- **Adding a backend feature?** → `REGISTRY-dispatch-api.md` + `hussle-app-dispatch-api/CLAUDE.md` (full module template). Architecture is enforced by `dependency-cruiser` (`npm run lint:deps`).
- **Adding a frontend page?** → `REGISTRY-dispatch-ui.md` for layout/state patterns, `mocho/components/` for reusable UI primitives. `useDirtyFormBlocker` is mandatory on all drawer/form components.
- **Adding a load-board scraper?** → `REGISTRY-dat-load-scraper.md` + new module under `src/sites/<site>/` + manifest content_scripts entry.

## Validation Commands

See `packages.json` for canonical commands per package. Use `testRelated` for per-task validation; reserve full `test` + `typecheck` for per-story validation; never run a full monorepo build mid-story.

## Companion Files

- `ARCHITECTURE.md` — package topology, data flow, entry points (MACHO format).
- `ARCHITECTURE-gsd.md` — GSD-format system architecture (components, layers, event bus details — preserved from prior bootstrap).
- `STACK.md`, `STRUCTURE.md`, `CONVENTIONS.md`, `INTEGRATIONS.md`, `TESTING.md`, `CONCERNS.md` — GSD codebase maps. Use these for broader context; this REGISTRY set is the operational reference during `/build`.
