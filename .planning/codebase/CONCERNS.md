# Project Concerns

> Auto-generated by /bootstrap on 2026-03-01. Re-run /bootstrap to refresh.

## Warning
- No CLAUDE.md found at repository root — agents lack project-specific context (will be generated)
- No `.env.example` found — new developers lack setup guidance for environment variables
- dispatch-ui has no tests written yet — test infrastructure configured but no test files present
- Backend API package not yet scaffolded — docker-compose references `hussle-app-dispatch-api` but directory does not exist

## Info
- Monorepo has no root-level validate command — wave 3 runs per-package only
- No observability libraries detected — consider `/observability-setup` when API is built
- Backend infrastructure (Postgres, Redis) commented out in docker-compose — will need to be enabled
- No CI/CD configuration detected — consider adding GitHub Actions or similar
