# Architecture Overview

> Auto-generated by /bootstrap on 2026-03-01. Update manually as the project evolves.

## System Type
JS: monorepo with 2 packages (npm workspaces)

## Package Topology
| Package | Type | Stack | Depends On | Depended By |
|---------|------|-------|------------|-------------|
| mocho-ui | shared-ui | React, MUI, Vite, Storybook, Jest | — | dispatch-ui |
| dispatch-ui | frontend | React, MUI, Redux, Vite, Jest | mocho-ui | — |

## Technology Stack

### Runtime & Frameworks
- **React 18** — UI framework (both packages)
- **MUI v5** — Material UI component library
- **Redux Toolkit + Redux Saga** — state management
- **Formik + Yup** — form handling and validation
- **Vite** — build tool and dev server

### Data Layer
- **PostgreSQL** (planned — commented out in docker-compose)
- **Redis** (planned — commented out in docker-compose)
- **Axios** — HTTP client (dispatch-ui)

### Infrastructure
- **Docker Compose** — local dev (dispatch-ui on :5173, storybook on :6006)
- **npm workspaces** — monorepo management
- No CI/CD configuration detected

## Data Flow
- Client → dispatch-ui (React SPA, :5173) → API (not yet scaffolded, planned :3001)
- mocho-ui provides shared components consumed by dispatch-ui
- Backend services (API, Postgres, Redis) are planned but not yet active

## Key Patterns
- **MUI sx prop**: All styling via inline `sx` — no CSS modules or styled-components
- **Redux Toolkit + Saga**: State managed via slices, side effects via sagas
- **Barrel exports**: Components re-exported from index.ts files
- **Multi-entry library**: mocho-ui exports `./redux`, `./forms`, `./components`, etc.
- **Typed props**: All components use `<Name>Props` interfaces

## Entry Points
| Package | Dev Command | Main Entry |
|---------|-------------|------------|
| mocho-ui | `npm run dev -w mocho-ui` (Storybook :6006) | mocho-ui/src/index.ts |
| dispatch-ui | `npm run dev -w hussle-app-dispatch-ui` (Vite :5173) | hussle-app-dispatch-ui/src/main.tsx |
