# Project Conventions

> Auto-generated by /bootstrap on 2026-03-01. Re-run /bootstrap to refresh.

## Cross-Package Summary

| Convention | mocho-ui (shared-ui) | dispatch-ui (frontend) |
|------------|---------------------|----------------------|
| File naming | PascalCase components, camelCase utils | PascalCase components, camelCase utils |
| Component style | `React.FC<Props>`, default export | `React.FC<Props>` shared, arrow fn pages |
| Props interfaces | `<Name>Props`, exported | `<Name>Props`, exported |
| Imports | React → MUI → types → utils → components | React → External → Aliases → Relative |
| Type imports | Separate `import type` | Separate `import type` |
| Barrel exports | index.ts re-exports | index.ts re-exports |
| Styling | MUI `sx` prop only | MUI `sx` prop only |
| State mgmt | Redux Toolkit + Redux Saga | Redux Toolkit + Redux Saga |
| Forms | Formik + Yup | Formik + Yup (configured, not yet used) |
| Testing | Jest + RTL, co-located `.test.tsx` | Jest + RTL (configured, no tests yet) |
| Error handling | Typed enums + interfaces | ErrorBoundary + axios interceptors |

---

## @mocho/ui (mocho-ui)

| Convention | Pattern | Example |
|------------|---------|---------|
| File naming | PascalCase for components, camelCase for utils/hooks | `ConfirmDialog.tsx`, `useConfig.ts` |
| Component folders | PascalCase directory with `index.tsx` re-export | `/ConfirmDialog/index.tsx` |
| Test location | Co-located or `__tests__/` subdirectory | `ConfirmDialog.test.tsx` |
| Test naming | `.test.tsx` suffix | `ConfirmDialog.test.tsx` |
| Storybook | Co-located `.stories.tsx` | `ConfirmDialog.stories.tsx` |
| Barrel exports | Re-export with explicit names | `export { default as EmptyState } from './EmptyState'` |
| Import ordering | React → MUI → types → utils → components | See codebase |
| Type imports | Separate `import type` statements | `import type { ConfirmDialogProps }` |
| Props interfaces | Suffixed with `Props`, exported | `ConfirmDialogProps` |
| Error handling | Typed error enums + interface | `ImageUploadErrorType` enum |
| State mgmt | Redux Toolkit + Redux Saga | `createCrudSlice`, `createCrudSagas` |
| Styling | MUI `sx` prop, no CSS modules | `sx={{minWidth: 300}}` |
| Form fields | Discriminated union pattern (Formik + Yup) | `CharCounterField`, `SlugField` |
| Package exports | Multi-entry for tree-shaking | `./redux`, `./forms`, `./components` |

---

## hussle-app-dispatch-ui (dispatch-ui)

| Convention | Pattern | Example |
|------------|---------|---------|
| Component folders | PascalCase with index export | `components/MainCard/index.tsx` |
| Page components | PascalCase, default export | `pages/home/pages/HomePage.tsx` |
| Route files | PascalCase with `Routes` suffix | `pages/home/routes/HomeRoutes.tsx` |
| Redux slices | camelCase with `Slice` suffix | `uiSlice.ts` |
| Path aliases | Direct imports from modules | `import MainCard from 'components/MainCard'` |
| Shared components | `React.FC` with named export | `export const PageWrapper: React.FC<Props>` |
| Redux hooks | Typed exports from `store/index.ts` | `import { useSelector, useDispatch } from 'store'` |
| Store config | `configureStore` with saga, thunks disabled | `thunk: false` |
| Dual-slice pattern | Page slices + entity slices | `pages` + `entities` in root reducer |
| Error boundaries | Class-based with fallback UI | `ErrorBoundary` component |
| HTTP errors | Axios interceptor with 401 refresh | Token refresh → retry or logout |

---

## Notes

- **dispatch-ui** is early-stage: scaffolded structure, no feature modules or tests yet
- **mocho-ui** is mature: full component library with tests, stories, and multi-entry exports
- Both packages share MUI v5, Redux Toolkit + Saga, and Formik + Yup conventions
