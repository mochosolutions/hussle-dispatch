# Derived Values Removal — Story × File Matrix
_Generated by /build on 2026-05-25. Overlap (multiple cells in a row) = stories that touch the same file._

## High-overlap files (cross-story coordination required)

| File | US-02 | US-09 | US-11 | US-13 |
|---|---|---|---|---|
| `hussle-app-dispatch-api/src/loads/services/loadService.ts` | T-04 | T-25, T-26 | T-30 | T-37 |
| `hussle-app-dispatch-api/src/loads/services/loadStatusService.ts` | T-04 | T-26 | T-30 | T-37 |
| `hussle-app-dispatch-api/src/loads/repositories/loadRepositoryPrisma.ts` | T-04 | — | T-30 | — |

| File | US-06 | US-09 | US-15 |
|---|---|---|---|
| `hussle-app-dispatch-api/prisma/schema.prisma` | T-17 | T-24 | T-41 |

| File | US-04 | US-13 |
|---|---|---|
| `hussle-app-dispatch-api/src/loads/compositionRoot.ts` | — | T-36, T-37 |
| `hussle-app-dispatch-api/src/loads/services/calculateFinancials.ts` | — | T-37 |

| File | US-11 | US-12 | INT-02 |
|---|---|---|---|
| `hussle-app-dispatch-api/src/loads/controllers/transformers/loadTransformer.ts` | T-31 | — | T-47 |

| File | US-02 | INT-01 |
|---|---|---|
| `hussle-app-dispatch-api/src/carriers/controllers/transformers/carrierTransformer.ts` | T-05, T-06 | T-46 |

## Overlap resolution

All overlaps follow the sequential dependency chain (US-02 → US-09 → US-11 → US-13, and US-06 → US-09 → US-15). No `passes_state_to:` declaration needed — `Depends on:` enforces ordering and each story re-reads the upstream version from disk after the prior story's commit lands.

**Special case — `loadService.ts`:**
Four stories touch this file. Each modification is additive/replacing different regions:
- US-02 (T-04): swap reads of 7 Carrier compliance columns
- US-09 (T-25, T-26): add booking snapshot capture
- US-11 (T-30): replace persisted Load financial reads with computeLoadFinancials
- US-13 (T-37): remove 3 persist call sites

Sequential execution per the dep chain prevents conflicts. Confirm no `[~]` in-progress overlap with another story during /build.

**Special case — `schema.prisma`:**
Three migrations sequenced via prisma migrate dev. Each migration is independent (drops different columns / adds different ones). Run in story order: US-06 first (drop Carrier), then US-09 (add Load inputs), then US-15 (drop Load outputs).

## Single-story files (no coordination required)

Files appearing in exactly one story's `Files:` list are omitted from this matrix. They are listed in the task body's `Files:` array and don't require cross-story coordination.

## Auto-generated stories file coverage

- **INT-01** reads (no writes): Carrier transformers + UI Carrier types
- **INT-02** reads (no writes): Load transformers + dispatch terms validator/schema + UI api/loads
- **VER-01** reads only — no Files; checks must_haves truths against codebase
