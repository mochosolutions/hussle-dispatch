---
phase: 01-stabilize
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - hussle-app-dispatch-ui/src/features/carrier-portal/store/sagas/__tests__/savePhaseDataSaga.test.ts
  - hussle-app-dispatch-ui/src/features/carrier-portal/store/slices/__tests__/carrierPortalSlice.test.ts
  - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/CarrierPortalPage.test.tsx
  - hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/CostResultCard.test.tsx
  - hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/PresetTileSelector.test.tsx
  - hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/StateGrid.test.tsx
  - hussle-app-dispatch-ui/src/components/ConversationalForm/__tests__/InputRenderer.test.tsx
  - hussle-app-dispatch-ui/src/components/ConversationalForm/__tests__/SubQuestion.test.tsx
  - hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
  - .planning/phases/01-stabilize/01-VALIDATION.md
autonomous: true
requirements: [STAB-01, STAB-02, STAB-03, STAB-05, STAB-07, STAB-08, STAB-10, STAB-11, STAB-12, STAB-14]

must_haves:
  truths:
    - Every Wave 0 test file from VALIDATION.md exists on disk
    - Every scaffolded test runs (jest/playwright discovers it) and FAILS in a defined way (red — references not-yet-built behavior)
    - No scaffold imports from a non-existent path that would cause a TS/compile error blocking the rest of the suite
    - VALIDATION.md frontmatter `wave_0_complete` is flipped to `true` once scaffolds are committed (administrative Nyquist gate)
  artifacts:
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/store/sagas/__tests__/savePhaseDataSaga.test.ts
      provides: saga unit harness for existing 3 + new 2 workers
      contains: expectSaga, handleSaveCostAnalysis, handleSaveLanePreferences
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/store/slices/__tests__/carrierPortalSlice.test.ts
      provides: reducer tests (STAB-05 setCurrentPhase, new save action triples, lastSavedPhase rising-edge)
      contains: setCurrentPhase, saveCostAnalysisSuccess, lastSavedPhase
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/CarrierPortalPage.test.tsx
      provides: page-level test (dispatch + advance + snackbar + scroll)
      contains: enqueueSnackbar, scrollIntoView, completeOnboarding
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/CostResultCard.test.tsx
      provides: CostResultCard render + reduced-motion test
      contains: prefers-reduced-motion, breakEvenRpm, minimumRatePerMile
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/PresetTileSelector.test.tsx
      provides: chip click + custom-value reveal tests
      contains: chip, custom
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/StateGrid.test.tsx
      provides: 3-state cycle + a11y attr tests
      contains: aria-pressed, aria-label, role
    - path: hussle-app-dispatch-ui/src/components/ConversationalForm/__tests__/InputRenderer.test.tsx
      provides: presetTiles + stateGrid switch tests
      contains: presetTiles, stateGrid
    - path: hussle-app-dispatch-ui/src/components/ConversationalForm/__tests__/SubQuestion.test.tsx
      provides: 16/600 typography + border-color tests
      contains: fontSize, fontWeight, info.main
    - path: hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
      provides: Playwright STAB-14 spec stub (mirrors driver-portal-smoke pattern)
      contains: page.route, carrier-portal/session, PortalCompleteView
    - path: .planning/phases/01-stabilize/01-VALIDATION.md
      provides: VALIDATION frontmatter `wave_0_complete: true` after scaffolds land
      contains: "wave_0_complete: true"
  key_links:
    - from: hussle-app-dispatch-ui/src/features/carrier-portal/store/sagas/__tests__/savePhaseDataSaga.test.ts
      to: redux-saga-test-plan
      via: import { expectSaga } from 'redux-saga-test-plan'
      pattern: "expectSaga"
    - from: hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
      to: '@playwright/test'
      via: import { test, expect }
      pattern: "page.route"

threat_model:
  trust_boundaries: []
  notes: "Wave 0 creates test scaffolds only. No new endpoints, no auth surface, no PII handling. The Playwright spec uses fake tokens only (`e2e-test-token`) — never real invite tokens. Test files inherit relaxed `no-explicit-any` (warn) per project ESLint config."
---

<objective>
Create the full set of Wave 0 test scaffolds listed in `.planning/phases/01-stabilize/01-VALIDATION.md` § Wave 0 Requirements. Every scaffold MUST exist on disk and run (red is fine — green comes as feature plans land), so per-task verification in waves 1–5 can sample green/red continuously per VALIDATION.md § Sampling Rate.

Purpose: Establish Nyquist-compliant feedback continuity. Without these scaffolds, every subsequent task's `<verify><automated>` would point at a file that does not exist, and the harness would not be able to flip ⬜ → ❌ → ✅ as features land.

Output: 9 test files (8 jest + 1 playwright) committed to the repo plus the VALIDATION.md `wave_0_complete: true` flag flip. Each scaffold fails in a meaningful way (asserts behavior that exists today or will exist by end of phase). No new dependencies installed — Jest, RTL, `redux-saga-test-plan`, and Playwright are all already in `hussle-app-dispatch-ui/package.json` (verified RESEARCH.md § Standard Stack + § Environment Availability).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-stabilize/01-VALIDATION.md
@.planning/phases/01-stabilize/01-RESEARCH.md
@.planning/phases/01-stabilize/01-PATTERNS.md
@CLAUDE.md
@hussle-app-dispatch-ui/e2e/driver-portal-smoke.spec.ts
@hussle-app-dispatch-ui/src/features/carrier-portal/store/sagas/savePhaseDataSaga.ts
@hussle-app-dispatch-ui/src/features/carrier-portal/store/slices/carrierPortalSlice.ts

<interfaces>
<!-- These behaviors will exist after waves 1–5 land. The scaffolds reference them as `describe.skip` / `it.todo` / red expectations now, flip green later. -->

From savePhaseDataSaga.ts (TODAY): exports `savePhaseDataSaga` watcher that takeLatest's on `saveCompany`, `saveEquipment`, `saveDrivers`, `completeOnboarding`. New workers `handleSaveCostAnalysis` and `handleSaveLanePreferences` are added in Plan 02.

From carrierPortalSlice.ts (TODAY): exports `carrierPortalActions` with `saveCompany/Success/Failure`, `saveEquipment/Success/Failure`, `saveDrivers/Success/Failure`, `completeOnboarding/Success/Failure`, `setCurrentPhase` (currently silent-guarded on `state.session`), `sessionCompleted`. New actions `saveCostAnalysis/Success/Failure`, `saveLanePreferences/Success/Failure`, `phaseAdvanceConsumed`, plus new state field `lastSavedPhase: number | null` and lifted `currentPhase: number` are added in Plan 02.

From driver-portal-smoke.spec.ts: pattern for `page.route('**/api/v1/.../...', async (route) => route.fulfill({...}))` mocked-API e2e — mirror for carrier portal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create saga + slice + page scaffolds</name>
  <read_first>
    - hussle-app-dispatch-ui/src/features/carrier-portal/store/sagas/savePhaseDataSaga.ts (analog for saga test shape)
    - hussle-app-dispatch-ui/src/features/carrier-portal/store/slices/carrierPortalSlice.ts (analog for slice test shape)
    - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx (analog for page test shape)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 14 "Test files — saga, slice, component" (test conventions)
    - hussle-app-dispatch-ui/jest.config.* (to confirm test path/extension conventions)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/store/sagas/__tests__/savePhaseDataSaga.test.ts,
    hussle-app-dispatch-ui/src/features/carrier-portal/store/slices/__tests__/carrierPortalSlice.test.ts,
    hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/CarrierPortalPage.test.tsx
  </files>
  <action>
    Create three test scaffold files. Each MUST be discoverable by Jest (correct `.test.ts(x)` suffix, correct directory) and MUST contain at minimum one failing or pending test per VALIDATION.md Per-Task Verification Map.

    File 1 — `savePhaseDataSaga.test.ts`:
    - Use `redux-saga-test-plan`'s `expectSaga` (already in deps — verified RESEARCH.md § Standard Stack).
    - `describe('savePhaseDataSaga workers', () => { ... })` with one `it.todo` per existing worker (`handleSaveCompany`, `handleSaveEquipment`, `handleSaveDrivers`, `handleCompleteOnboarding`) and one `it` per new worker (`handleSaveCostAnalysis`, `handleSaveLanePreferences`) that uses `expectSaga(handleSaveCostAnalysis, action)` and asserts `put(saveCostAnalysisSuccess())` on success path — these MUST currently fail because the actions don't yet exist. If TS won't compile, use `it.todo('dispatches saveCostAnalysisSuccess on success')` placeholder instead.
    - Cover STAB-01 saga path verification.

    File 2 — `carrierPortalSlice.test.ts`:
    - Import `carrierPortalReducer` and `carrierPortalActions` from `'../carrierPortalSlice'`.
    - `describe('carrierPortalSlice', () => { ... })` with these tests (use `it.todo` for any that reference state fields not yet added):
      - `it('setCurrentPhase no longer guards on session — STAB-05')` — call reducer with no session, expect `state.currentPhase === 3` (will fail today because currentPhase lives inside session).
      - `it('saveCompanySuccess sets lastSavedPhase=1 — STAB-01 rising-edge')` — failing today.
      - `it.todo('saveCostAnalysisSuccess sets lastSavedPhase=4')` — placeholder.
      - `it.todo('saveLanePreferencesSuccess sets lastSavedPhase=5')` — placeholder.
      - `it.todo('phaseAdvanceConsumed clears lastSavedPhase to null')` — placeholder.
      - `it.todo('completeOnboardingSuccess populates session.completedAt from API payload — STAB-03 transition gate')` — placeholder. This reducer is the trigger for `PortalCompleteView` rendering (Plan 06 BLOCKER 2 fix). Verify in Plan 02 that the existing `completeOnboardingSuccess` reducer reads `completedAt` from `action.payload.session.completedAt` (or equivalent) and writes it to `state.session.completedAt`; flip this `it.todo` to a real assertion when Plan 02 lands.
    - Cover STAB-05 and STAB-01 reducer-level verification.

    File 3 — `CarrierPortalPage.test.tsx`:
    - Use `@testing-library/react` + a mock Redux store (`@reduxjs/toolkit configureStore` with the carrierPortalSlice reducer); render the page under a Provider.
    - Mock `notistack` `enqueueSnackbar` via `jest.mock('notistack', () => ({ enqueueSnackbar: jest.fn(), ... }))`.
    - Mock `Element.prototype.scrollIntoView = jest.fn()`.
    - `describe('CarrierPortalPage', () => { ... })`:
      - `it.todo('Save & Continue dispatches saveCompany when validation passes — STAB-01')`
      - `it.todo('Save & Continue fires snackbar + scrolls to first error when validation fails — STAB-02')`
      - `it.todo('Final phase dispatches completeOnboarding (not sessionCompleted) — STAB-03')`
      - `it.todo('lastSavedPhase rising-edge advances currentPhase by 1 — STAB-01')`
      - `it.todo('renders PortalCompleteView when session.completedAt is non-null — STAB-03 transition gate')` — placeholder. Plan 06 BLOCKER 2 fix: this asserts that after `completeOnboardingSuccess` populates `session.completedAt`, the page renders `PortalCompleteView` via the existing render-condition.
      - `it.todo('Phase 4 thread is replaced by CostResultCard once all 6 cost-analysis fields are populated, and CTA dispatches saveCostAnalysis — STAB-08 wiring')` — placeholder. Plan 06 BLOCKER 3 fix: conditional render in `CarrierPortalPage` when `currentPhase === 4` AND all 6 fields populated.
    - Cover STAB-01, STAB-02, STAB-03 page-level verification.

    All three files MUST compile under `npm run check-ts` — no `any`, no `as`, no `!` per project ESLint. Use `it.todo` liberally to avoid TS errors against not-yet-existing exports. Use `eslint-disable` is forbidden; if a type doesn't exist yet, use `it.todo` placeholder.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && npx jest --listTests --testPathPattern='(savePhaseDataSaga|carrierPortalSlice|CarrierPortalPage)\.test\.(ts|tsx)$' 2>&1 | grep -c 'test\.' | grep -q '^3$' && cd hussle-app-dispatch-ui && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist at the exact paths in `<files>`
    - `npx jest --listTests` discovers all 3 files
    - `npx tsc --noEmit` passes (no compile errors blocking other tests)
    - Each file has at least one `it`/`it.todo` per STAB requirement it covers
  </acceptance_criteria>
  <done>3 scaffold files committed; Jest discovers them; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create component + InputRenderer + SubQuestion scaffolds</name>
  <read_first>
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/index.tsx (existing component to test)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx (existing component to test)
    - hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx (existing component to test)
    - hussle-app-dispatch-ui/src/components/ConversationalForm/InputRenderer.tsx (existing component to test)
    - .planning/phases/01-stabilize/01-UI-SPEC.md § Typography Table 1 (16/600 spec for SubQuestion test)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 14 (test conventions)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/CostResultCard.test.tsx,
    hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/PresetTileSelector.test.tsx,
    hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/StateGrid.test.tsx,
    hussle-app-dispatch-ui/src/components/ConversationalForm/__tests__/InputRenderer.test.tsx,
    hussle-app-dispatch-ui/src/components/ConversationalForm/__tests__/SubQuestion.test.tsx
  </files>
  <action>
    Create five component test scaffold files using `@testing-library/react` + `renderWithTheme`-style helper (or inline `ThemeProvider` wrap from `hussle-app-dispatch-ui/src/themes/`).

    File 1 — `CostResultCard.test.tsx`:
    - Component does not yet exist (deleted, rebuilt in Plan 05). Use `it.todo` for all tests so the file compiles.
    - `describe('CostResultCard', () => { it.todo('renders break-even RPM and minimum rate from inputs — STAB-08'); it.todo('respects prefers-reduced-motion — renders final values immediately'); it.todo('animates count-up via requestAnimationFrame when motion enabled'); })`.

    File 2 — `PresetTileSelector.test.tsx`:
    - Component DOES exist (`features/carrier-portal/components/PresetTileSelector/index.tsx`, 73 lines per RESEARCH.md).
    - Real tests (not todo): import and render with sample presets, simulate click on a chip, assert `onChange` called with the chip's value; click "Custom" chip, assert `TextField` appears.
    - `describe('PresetTileSelector', () => { it('renders one chip per preset — STAB-07'); it('calls onChange with preset value when chip clicked'); it('reveals custom-value TextField when custom chip clicked'); })`.

    File 3 — `StateGrid.test.tsx`:
    - Component DOES exist (`features/carrier-portal/components/StateGrid/index.tsx`, 132 lines).
    - Real tests for cycle: render, click 'CA' tile, expect onChange called with `{ CA: 'PREFERRED' }`; click again → `{ CA: 'AVOIDED' }`; click third time → `{}` (neutral removes key).
    - `it.todo('each tile has role="button" — STAB-10 a11y')` and `it.todo('aria-pressed reflects preference state — STAB-10 a11y')` — these will fail today (a11y attrs land in Plan 07).
    - `describe('StateGrid', () => { ... })`.

    File 4 — `InputRenderer.test.tsx`:
    - Component DOES exist. Real test asserts current behavior: render `<InputRenderer>` with a question whose `inputType` is `'stateGrid'`, assert a `StateGrid` is rendered (already wired today).
    - `it.todo('routes inputType="presetTiles" to PresetTileSelector — STAB-12')` — will fail today because the placeholder stub renders instead.
    - `describe('InputRenderer', () => { it('routes inputType="stateGrid" to StateGrid'); it.todo('routes inputType="presetTiles" to PresetTileSelector — STAB-12'); })`.

    File 5 — `SubQuestion.test.tsx`:
    - Component DOES exist (`components/ConversationalForm/SubQuestion.tsx`).
    - `it('renders with fontSize: 16 and fontWeight: 600 — STAB-11/UI-SPEC Table 1')` — query the label element, assert its computed style or sx props match 16/600. Will FAIL today because L55-59 pins 18px.
    - `it('renders blue/green/red/grey left borders based on borderColor prop — STAB-11')` — assert via `getByTestId` or style query.
    - `describe('SubQuestion', () => { ... })`.

    All 5 files MUST pass `npx tsc --noEmit`. Use Material-UI `ThemeProvider` from `hussle-app-dispatch-ui/src/themes/` if the components require a theme context. Use `userEvent` (`@testing-library/user-event`) for clicks per CLAUDE.md test conventions.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && npx jest --listTests --testPathPattern='(CostResultCard|PresetTileSelector|StateGrid|InputRenderer|SubQuestion)\.test\.tsx$' 2>&1 | grep -c 'test\.tsx' | grep -q '^5$' && cd hussle-app-dispatch-ui && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at the exact paths in `<files>`
    - Jest discovers all 5
    - `npx tsc --noEmit` passes
    - PresetTileSelector + StateGrid tests are real (not all `it.todo`) since components exist
    - CostResultCard test uses `it.todo` only (component doesn't exist yet)
  </acceptance_criteria>
  <done>5 component scaffold files committed; Jest discovers them; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Create Playwright e2e scaffold and flip VALIDATION.md wave_0_complete flag</name>
  <read_first>
    - hussle-app-dispatch-ui/e2e/driver-portal-smoke.spec.ts (verbatim pattern — 30 lines)
    - hussle-app-dispatch-ui/playwright.config.ts (config)
    - .planning/phases/01-stabilize/01-RESEARCH.md § "Playwright e2e skeleton (STAB-14)" (full skeleton)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 13 (e2e analog excerpt)
    - .planning/phases/01-stabilize/01-VALIDATION.md (frontmatter `wave_0_complete: false` → flip to `true`)
  </read_first>
  <files>
    hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts,
    .planning/phases/01-stabilize/01-VALIDATION.md
  </files>
  <action>
    Two coordinated edits — create the Playwright scaffold AND flip the administrative gate flag once all 9 scaffolds are committed.

    **A. Create the Playwright e2e scaffold** mirroring `e2e/driver-portal-smoke.spec.ts`. Use ONLY fake tokens (`'e2e-test-token'`) — never a real invite token (security note: RESEARCH.md § Security Domain calls this out).

    Structure:
    - `import { expect, test } from '@playwright/test';`
    - `const VALID_TOKEN = 'e2e-test-token';`
    - `test.describe('Carrier portal — full 6-phase flow', () => { ... })`.
    - `test.beforeEach` registers `page.route()` mocks for:
      - `GET **/api/v1/carrier-portal/session` → 200 seeded session `{ session: { id: 's1', currentPhase: 1, completedPhases: [], completedAt: null }, carrier: { id: 'c1', name: 'E2E Test Carrier', email: 'test@example.com' }, answers: {} }`
      - `POST **/api/v1/carrier-portal/{company|equipment|drivers|cost-analysis|lane-preferences}` → 200 `{ data: {} }`
      - `POST **/api/v1/carrier-portal/session/complete` → 200 with `completedAt: new Date().toISOString()`
      - `PUT **/api/v1/carrier-portal/session/answer` → 200 `{ data: {} }`
    - One real `test('invite → portal renders phase 1', async ({ page }) => { ... })` — navigate to `/carrier-portal/${VALID_TOKEN}`, assert the Company heading is visible (smoke baseline). This MUST pass today (carrier portal page loads with token from URL).
    - One `test.skip('invite → portal → all 6 phases → submit reaches completion view — STAB-14')` placeholder for the full flow — to flip from `.skip` to enabled in Plan 08.

    The spec MUST be discoverable by `npx playwright test --list`. Do not include `page.route` patterns that would short-circuit the smoke test (the `/session` mock IS needed for the smoke test to render anything).

    Constraints:
    - No real backend dependency — all API calls mocked via `page.route()` (matches existing pattern per RESEARCH.md Pitfall 7).
    - Mark the full-flow test with `test.skip` so it doesn't break CI before Plan 08.
    - Use `await expect(...).toBeVisible({ timeout: 15000 })` (15s timeout, matches analog).

    **B. Flip VALIDATION.md `wave_0_complete: false` → `wave_0_complete: true`** in the frontmatter of `.planning/phases/01-stabilize/01-VALIDATION.md`. This is an administrative Nyquist gate — every Wave 0 scaffold MUST be committed and discoverable before flipping the flag. The flag flip is the final action of this plan; do it AFTER confirming Tasks 1 + 2 + the Playwright spec are all in place. No other VALIDATION.md content changes — only the frontmatter line `wave_0_complete: false` → `wave_0_complete: true`.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && npx playwright test --list 2>&1 | grep -q 'carrier-portal-full.spec.ts' && grep -q 'wave_0_complete: true' .planning/phases/01-stabilize/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts`
    - `npx playwright test --list` discovers the spec
    - Contains a `test.beforeEach` with at least 7 `page.route()` calls (1 session + 5 phase saves + 1 complete; auto-save optional)
    - Contains one passing smoke `test()` and one `test.skip()` placeholder for the full flow
    - No real invite tokens in the file (grep `'e2e-test-token'` succeeds; grep for any 32+ char alphanumeric token fails)
    - VALIDATION.md frontmatter updated: `wave_0_complete: true` (verified by `grep -q 'wave_0_complete: true' .planning/phases/01-stabilize/01-VALIDATION.md`)
  </acceptance_criteria>
  <done>Playwright scaffold committed; spec is discoverable; smoke test runs; VALIDATION.md `wave_0_complete: true` flag flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Wave 0 creates test scaffolds only. No new endpoints, no new auth surface, no new persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | e2e/carrier-portal-full.spec.ts | mitigate | Use only fake tokens (`'e2e-test-token'`) in the spec — never a real invite token. Enforced by code review and the acceptance criteria grep. RESEARCH.md § Security Domain explicitly calls this out. |
| T-01-02 | Spoofing | Test scaffolds | accept | Tests run only in dev/CI; no production exposure; no auth path is exercised at the network boundary. |

No HIGH-severity threats. Wave 0 produces test scaffolds, not running production code.
</threat_model>

<verification>
After all 3 tasks complete:
- `cd hussle-app-dispatch-ui && npx jest --listTests --testPathPattern='(savePhaseDataSaga|carrierPortalSlice|CarrierPortalPage|CostResultCard|PresetTileSelector|StateGrid|InputRenderer|SubQuestion)\.test\.(ts|tsx)$' | wc -l` returns `8`
- `cd hussle-app-dispatch-ui && npx playwright test --list | grep -c carrier-portal-full.spec.ts` returns `1`
- `cd hussle-app-dispatch-ui && npx tsc --noEmit -p tsconfig.app.json` exits 0
- Per-task verification under VALIDATION.md § Per-Task Verification Map can now reference real files (flipping `❌ W0` → `❌ red` for each STAB row that has a scaffold)
- `cd hussle-app-dispatch-ui && npm test -- --testPathPattern=carrier-portal 2>&1 | grep -E '(passing|failing|todo)'` runs to completion (some tests may fail/todo — that's expected; the suite is wired)
- `grep -q 'wave_0_complete: true' .planning/phases/01-stabilize/01-VALIDATION.md` exits 0 (administrative Nyquist gate flipped)
</verification>

<success_criteria>
- 9 scaffold files exist at the exact paths in VALIDATION.md § Wave 0 Requirements
- All scaffolds compile (`npx tsc --noEmit` passes)
- Jest discovers all 8 unit/component scaffolds
- Playwright discovers the 1 e2e scaffold
- One smoke Playwright test passes (proves the spec wiring works) and the full-flow test is `test.skip()` until Plan 08
- VALIDATION.md frontmatter updated: `wave_0_complete: true` (administrative Nyquist gate flipped; verified by `grep -q 'wave_0_complete: true' .planning/phases/01-stabilize/01-VALIDATION.md`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-01-SUMMARY.md`.
</output>
