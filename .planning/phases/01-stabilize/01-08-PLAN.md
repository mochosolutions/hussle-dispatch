---
phase: 01-stabilize
plan: 08
type: execute
wave: 5
depends_on: [06, 07]
files_modified:
  - hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
  - .planning/phases/01-stabilize/SMOKE-CHECKLIST.md
  - .planning/phases/01-stabilize/01-VALIDATION.md
  - Jenkinsfile.build
autonomous: false
requirements: [STAB-14, STAB-15]

must_haves:
  truths:
    - "`e2e/carrier-portal-full.spec.ts` (the Plan-01 scaffold) is flipped from `test.skip(...)` to an enabled `test(...)` that walks Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5 → Phase 6 → Submit → asserts PortalCompleteView is visible (STAB-14)"
    - "All API calls in the spec are mocked via `page.route()` (matches `driver-portal-smoke.spec.ts` pattern); no real backend dependency"
    - "Only fake tokens (`'e2e-test-token'`) — never a real invite token (security note, RESEARCH.md § Security Domain)"
    - "`npx playwright test e2e/carrier-portal-full.spec.ts` exits 0 locally"
    - "`Jenkinsfile.build` runs the Playwright suite — either (a) `npm run validate` already invokes `test:e2e` (verified by inspecting the validate script content) OR (b) `Jenkinsfile.build` is edited to add `npm run test:e2e` after `validate` and the edit is committed. Exactly one of (a) or (b) MUST be true — no third option (W2 fix)."
    - "`.planning/phases/01-stabilize/SMOKE-CHECKLIST.md` exists as a committed checklist for STAB-15 manual verification (committed in unchecked form by Claude; final sign-off requires user to walk the flow on a real phone and tick boxes)"
    - "`.planning/phases/01-stabilize/01-VALIDATION.md` frontmatter `nyquist_compliant` is flipped to `true` once Tasks 1 and 2 ship — administrative gate confirming Per-Task Verification Map fully green (BLOCKER 1)"
    - "Phase 1 ROADMAP success criteria 1–5 all checkable after this plan ships"
  artifacts:
    - path: hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
      provides: full 6-phase mocked e2e walkthrough
      contains: "PortalCompleteView"
      min_lines: 80
    - path: .planning/phases/01-stabilize/SMOKE-CHECKLIST.md
      provides: STAB-15 manual smoke checklist
      contains: "Phase 1"
    - path: .planning/phases/01-stabilize/01-VALIDATION.md
      provides: VALIDATION frontmatter `nyquist_compliant: true` after Phase 1 closes
      contains: "nyquist_compliant: true"
    - path: Jenkinsfile.build
      provides: CI hook for Playwright e2e (may already include via npm run validate — investigate)
      contains: "playwright"
  key_links:
    - from: hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
      to: hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
      via: Playwright navigates to /carrier-portal/:token, drives the real UI
      pattern: "page.goto"

threat_model:
  trust_boundaries: []
  notes: "E2e spec uses ONLY fake tokens (`'e2e-test-token'`) — never real invite tokens in test artifacts (RESEARCH.md § Security Domain row 5). All API mocks via `page.route()` so no real backend is reached. CI artifacts (videos, traces) may contain the fake token — acceptable. Manual smoke checklist is a markdown file with no embedded secrets."
---

<objective>
Land the Phase-1 safety net: the full Playwright e2e (STAB-14) and the manual smoke checklist (STAB-15). After this plan, Phase 1 hits its exit criteria — Phase 2 cannot legally begin until both gates are green per ROADMAP § Phase 1.

This plan is `autonomous: false` because:
- Task 3 includes a `checkpoint:human-verify` that asks the user to actually walk the flow on a phone and tick the SMOKE-CHECKLIST boxes. Claude cannot self-verify a real-device sensory experience.
- The Jenkinsfile.build edit may or may not be needed (depends on whether `npm run validate` already runs Playwright); Claude inspects the existing build stage and decides — the decision is deterministic per the W2 two-branch rule in Task 2.

Purpose: ROADMAP Phase 1 success criterion 5 ("Playwright e2e green in CI") + STAB-15 manual verification. These together unlock Phase 2 (Security & Hygiene).

Output: 1 modified Playwright spec (flipped from `.skip` to enabled), 1 new SMOKE-CHECKLIST.md, 1 conditionally-modified Jenkinsfile.build, 1 VALIDATION.md frontmatter flag flip (`nyquist_compliant: true` — BLOCKER 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-stabilize/01-RESEARCH.md
@.planning/phases/01-stabilize/01-PATTERNS.md
@.planning/phases/01-stabilize/01-UI-SPEC.md
@.planning/phases/01-stabilize/01-VALIDATION.md
@CLAUDE.md
@hussle-app-dispatch-ui/e2e/driver-portal-smoke.spec.ts
@hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
@hussle-app-dispatch-ui/playwright.config.ts
@Jenkinsfile.build

<interfaces>
After Wave 4 (Plans 02–07), the page is fully wired:
- Token in URL → page fetches session via mocked `/carrier-portal/session`
- Each phase renders questions from `QUESTIONS_BY_PHASE[currentPhase]`
- Save & Continue dispatches saga → mocked POST returns 200 → `saveXxxSuccess` reducer fires → useEffect advances
- Final phase dispatches `completeOnboarding()` → mocked POST → `completeOnboardingSuccess` populates `session.completedAt` → page render-branch swaps to `PortalCompleteView`
- Phase 4 conditionally renders `CostResultCard` once all 6 cost fields are populated (Plan 06 Task 3 wiring)

Plan-01 scaffold contents at `e2e/carrier-portal-full.spec.ts`:
- `test.beforeEach` registers all 7 `page.route()` mocks
- One passing smoke `test()` that navigates and asserts Company heading
- One `test.skip(...)` placeholder for the full flow

This plan flips the skip to enabled and fills in the per-phase interactions.

Driver-portal-smoke analog (`e2e/driver-portal-smoke.spec.ts`, 30 lines) — pattern for `page.route` mocks + `expect(...).toBeVisible({ timeout: 15000 })`.

`PortalCompleteView` (referenced by selector lookup) — likely component at `features/carrier-portal/components/PortalCompleteView/` or rendered inline in `CarrierPortalPage` when `session.completedAt` is set. Grep to find the exact heading/text to assert on.

`Jenkinsfile.build` at repo root — read first to determine whether it already runs e2e via `npm run validate`. The dispatch-ui side has a `validate` script in `hussle-app-dispatch-ui/package.json` — inspect the script content to determine if `test:e2e` is invoked.

VALIDATION.md frontmatter (BLOCKER 1): currently `nyquist_compliant: false`. Plan 01 flipped `wave_0_complete: true`. Plan 08 (this plan) is the close-out — it flips `nyquist_compliant: true` once the Per-Task Verification Map's last ❌ rows go green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip carrier-portal-full.spec.ts skip → enabled; fill in 6-phase interactions</name>
  <read_first>
    - hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts (Plan-01 scaffold)
    - hussle-app-dispatch-ui/e2e/driver-portal-smoke.spec.ts (verbatim pattern)
    - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx (final state after Plan 06 — to identify selectors)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/PortalLayout/index.tsx (header + breadcrumb structure)
    - hussle-app-dispatch-ui/src/features/carrier-portal/questions/*.ts (each phase's question IDs + types to interact with)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/PortalCompleteView/ (or similar — grep for the completion heading text)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/PortalFooterBar/index.tsx (Continue button selector — likely `getByRole('button', { name: /continue/i })`)
    - .planning/phases/01-stabilize/01-RESEARCH.md § "Playwright e2e skeleton (STAB-14)" (full skeleton — verbatim)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 13 (analog excerpt)
  </read_first>
  <files>
    hussle-app-dispatch-ui/e2e/carrier-portal-full.spec.ts
  </files>
  <action>
    Replace the `test.skip(...)` block scaffolded in Plan 01 with a real `test('invite → portal → all 6 phases → submit reaches completion view', async ({ page }) => { ... })`.

    Approach: per-phase loop, parameterized over the 6 phases. For each phase:
    1. Assert the phase heading is visible (`expect(page.getByRole('heading', { name: new RegExp(phaseLabel, 'i') })).toBeVisible({ timeout: 15000 })`).
    2. Fill the required questions:
       - **Phase 1 (Company)**: fill `business name` text input with `'E2E Carrier'`; pick a tax classification from a select; fill DOT/MC numbers with valid-format strings; fill required text fields with minimum-acceptable values. Use `page.getByLabel(...)` or `page.locator('[data-question-id="company.name"] input')` selectors (the `data-question-id` attribute is present on every question card from Plan 06).
       - **Phase 2 (Equipment)**: minimum-required fields (consult `equipmentQuestions.ts` for the schema).
       - **Phase 3 (Drivers)**: minimum-required fields.
       - **Phase 4 (Cost Analysis)**: click one preset chip per question (each chip is a button). `page.locator('[data-question-id="costAnalysis.truckPayment"] button:has-text("$1,200")').click()`. Repeat for the 5 other costAnalysis questions. For Q1, optionally skip by clicking the "I own it outright" yes/no sub-question (yes path sets truckPayment to 0 in CostResultCard, but the API still receives the entered value — Plan 06's payload mapping does not zero it out; the spec MUST still submit a value). After all 6 fields are populated, the page swaps from PhaseForm to CostResultCard (Plan 06 Task 3) — assert CostResultCard is visible, then click the Continue CTA on CostResultCard to advance.
       - **Phase 5 (Lane Preferences)**: click 2-3 state tiles in the StateGrid (`page.locator('[data-state="CA"]').click()`). Per Plan 05 `required: false`, the page can also be Continued without selecting any.
       - **Phase 6 (Documents)**: documents auto-save; assert the Submit button is visible.
    3. Click `Continue` / `Save & Continue` / `Submit` (depending on phase) and `await expect(page.getByRole('heading', { name: new RegExp(nextPhaseLabel, 'i') })).toBeVisible({ timeout: 10000 })` to confirm advance.

    After Phase 6 Submit, assert `PortalCompleteView` is visible. The completion view heading copy comes from UI-SPEC § Copywriting — likely "All set" or "You're done" or similar (grep the UI to confirm exact text; if absent, assert on `getByText(/all set|submitted|complete|thanks/i)`).

    Required `page.route()` mocks (verify they're already in `beforeEach` from Plan 01 — extend if missing):
    - `GET **/api/v1/carrier-portal/session` → 200 with seeded session
    - `POST **/api/v1/carrier-portal/{company|equipment|drivers|cost-analysis|lane-preferences}` → 200 `{ data: {} }`
    - `POST **/api/v1/carrier-portal/session/complete` → 200 with `completedAt`
    - `PUT **/api/v1/carrier-portal/session/answer` → 200 (auto-save during typing)
    - `POST **/api/v1/carrier-portal/documents/**` → 200 (Phase 6 document uploads — match the actual path used by the documents phase)

    Use `test.describe.configure({ retries: 1 })` if the suite is flaky on CI; default to no retries unless needed.

    Defer the dispatcher-side "approve" step per RESEARCH.md A4 / Open Question #1 / RESEARCH.md recommendation: stop at PortalCompleteView. Add a top-of-file comment explaining why ("Per RESEARCH.md A4: 'approve' in STAB-14 is interpreted as carrier-side submit reaching PortalCompleteView. Dispatcher-side approval is a separate concern.").

    Use ONLY the fake token `'e2e-test-token'` — security note in RESEARCH.md § Security Domain row 5.

    Constraints:
    - No `console.log`.
    - No `await page.waitForTimeout(...)` arbitrary sleeps — always wait on assertions (`expect(...).toBeVisible({ timeout: ... })`).
    - Use `page.getByRole`, `page.getByLabel`, `page.getByText` preferentially over CSS selectors per Playwright best practices; fall back to `data-question-id="..."` locator when the visible label is non-unique.
    - Spec MUST run on the existing Playwright config (Chromium desktop; iPhone SE mobile per `playwright.config.ts`).
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -v '^[[:space:]]*//' e2e/carrier-portal-full.spec.ts | grep -v "^[[:space:]]*\*" | grep -c "test\.skip" | grep -q '^0$' && grep -c "'cost-analysis'\|'lane-preferences'" e2e/carrier-portal-full.spec.ts | awk '$1 >= 2 {exit 0} {exit 1}' && npx playwright test e2e/carrier-portal-full.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - No `test.skip(` in the spec (grep returns 0 outside comments)
    - All 6 phase saves + completion mocked via `page.route()`
    - Only fake tokens in the file (no 32+ char hex sequences)
    - `npx playwright test e2e/carrier-portal-full.spec.ts` exits 0 locally (1 passing test minimum)
    - Spec walks Phase 1 → 6 → asserts a completion view selector
    - Phase 4 walk-through asserts CostResultCard is visible before clicking its Continue CTA (BLOCKER 3 path coverage)
  </acceptance_criteria>
  <done>Spec runs green; STAB-14 satisfied locally; Phase 4 CostResultCard path covered.</done>
</task>

<task type="auto">
  <name>Task 2: Create SMOKE-CHECKLIST.md + deterministic Jenkinsfile.build audit (W2) + VALIDATION.md nyquist_compliant flip (BLOCKER 1)</name>
  <read_first>
    - Jenkinsfile.build (existing build stages)
    - hussle-app-dispatch-ui/package.json (the "validate" + "test:e2e" scripts — inspect the validate script content)
    - .planning/ROADMAP.md (Phase 1 success criteria)
    - .planning/REQUIREMENTS.md (STAB-15 wording)
    - .planning/phases/01-stabilize/01-VALIDATION.md (frontmatter `nyquist_compliant: false` → flip to `true`)
    - .planning/phases/01-stabilize/01-RESEARCH.md § Open Question #2 (committed checklist recommendation)
  </read_first>
  <files>
    .planning/phases/01-stabilize/SMOKE-CHECKLIST.md,
    .planning/phases/01-stabilize/01-VALIDATION.md,
    Jenkinsfile.build
  </files>
  <action>
    Three coordinated artifacts.

    **A. Create `SMOKE-CHECKLIST.md`** at `.planning/phases/01-stabilize/SMOKE-CHECKLIST.md`. Markdown structure:

    - Header: "Phase 1: Stabilize — Manual Smoke Checklist (STAB-15)"
    - One subsection per device tested (iPhone Safari, Android Chrome, desktop Chrome) with checkboxes:
      - `[ ]` Navigate to invite link
      - `[ ]` Phase 1 (Company) — fill all required fields, tap Save & Continue, confirm advance
      - `[ ]` Phase 2 (Equipment) — same
      - `[ ]` Phase 3 (Drivers) — same
      - `[ ]` Phase 4 (Cost Analysis) — pick preset chips, advance, confirm CostResultCard renders correctly (break-even + minimum rate values plausible)
      - `[ ]` Phase 5 (Lane Preferences) — select 2-3 state tiles, confirm cycle works, advance
      - `[ ]` Phase 6 (Documents) — upload at least one document, tap Submit, confirm completion view
      - `[ ]` Validation failure path: tap Continue with required fields blank; confirm snackbar appears + page scrolls to first error
      - `[ ]` Reduced-motion path: with iOS Settings → Accessibility → Motion → Reduce Motion enabled, confirm count-up animations on CostResultCard are skipped
    - "Sign-off" subsection with:
      - `Reviewer:` ___
      - `Date:` ___
      - `Build commit SHA:` ___
      - `CI hook route taken (Jenkinsfile branch — (a) validate already runs e2e, OR (b) Jenkinsfile.build edited to add test:e2e step):` ___
      - `Notes / blockers:` (free text)

    Commit the file in unchecked state. User completes it during execution and ticks the boxes when they walk the flow.

    **B. `Jenkinsfile.build` audit — DETERMINISTIC TWO-BRANCH (W2 fix):**

    Step 1 (primary check): `grep -q '"test:e2e"' hussle-app-dispatch-ui/package.json` — confirms the npm script exists. If this fails, STOP and notify the user — the `test:e2e` script must be added to package.json first (out of scope for this plan; flag as a blocker).

    Step 2 (branch decision — exactly ONE must apply):

    **Branch (a): `npm run validate` already invokes `test:e2e`.**
    Test: read `hussle-app-dispatch-ui/package.json` and inspect the `"validate"` script value. If it contains `test:e2e` (e.g., `"validate": "npm run lint && npm run lint:deps && npm run check-ts && npm run test && npm run test:e2e"`), branch (a) is satisfied — no Jenkinsfile edit needed. Record in SMOKE-CHECKLIST under "CI hook route taken" the literal line from package.json that proves it.

    **Branch (b): `npm run validate` does NOT invoke `test:e2e`.**
    Test: inverse of branch (a). If validate's script value omits `test:e2e`, then edit `Jenkinsfile.build` to add a Playwright step after the existing validate step. Use:
    ```groovy
    sh 'cd hussle-app-dispatch-ui && npx playwright install --with-deps && npm run test:e2e'
    ```
    Commit the Jenkinsfile edit. Record in SMOKE-CHECKLIST the exact line number added to Jenkinsfile.

    Exactly one of (a) or (b) MUST apply — no third option (W2 fix). Both checks are scriptable; the executor runs them deterministically. If branch (a) is unclear (e.g., validate script chains another script that may or may not include e2e), default to branch (b) — explicit is safer than implicit.

    Constraints:
    - Do NOT add `--reporter=html` or any flag that creates persistent artifacts; default reporter is fine.
    - Do NOT remove any existing stage from `Jenkinsfile.build`.
    - If Jenkinsfile structure is non-obvious (declarative pipeline with many stages), pause via the checkpoint in Task 3 rather than guessing — but FIRST attempt the deterministic two-branch flow above.

    **C. Flip VALIDATION.md `nyquist_compliant: false` → `nyquist_compliant: true` (BLOCKER 1):**

    Once Task 1 (Playwright spec enabled + green locally) and Task 2 Parts A+B (smoke checklist + Jenkinsfile audit) are committed, edit `.planning/phases/01-stabilize/01-VALIDATION.md` frontmatter:
    - Change `nyquist_compliant: false` → `nyquist_compliant: true`.
    - Also flip the Validation Sign-Off checklist `**Approval:** pending` to `**Approval:** approved — Plan 08 close-out`.
    - Update the Per-Task Verification Map: change any remaining `❌ W0` status entries for STAB-14 to `✅ green` (and STAB-15 stays `⬜ pending` until Task 3 user sign-off).

    Do this as the FINAL action of this task — after both the spec and the CI hook are in place. The flag is the administrative Nyquist gate: it signals that the feedback continuity contract is fully satisfied.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-stabilize/SMOKE-CHECKLIST.md && grep -q "Phase 1: Stabilize" .planning/phases/01-stabilize/SMOKE-CHECKLIST.md && grep -q "CostResultCard\|cost analysis" .planning/phases/01-stabilize/SMOKE-CHECKLIST.md && grep -q "CI hook route taken" .planning/phases/01-stabilize/SMOKE-CHECKLIST.md && grep -q '"test:e2e"' hussle-app-dispatch-ui/package.json && (grep -E '"validate"\s*:\s*"[^"]*test:e2e[^"]*"' hussle-app-dispatch-ui/package.json || grep -E "playwright|test:e2e" Jenkinsfile.build) && grep -q "nyquist_compliant: true" .planning/phases/01-stabilize/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `SMOKE-CHECKLIST.md` exists at `.planning/phases/01-stabilize/SMOKE-CHECKLIST.md`
    - Contains per-device checkbox subsections (iOS, Android, desktop minimum)
    - Contains a validation-failure-path checkbox
    - Contains a reduced-motion checkbox
    - Contains a sign-off section including a "CI hook route taken" line documenting the (a)/(b) branch chosen
    - `"test:e2e"` script exists in `hussle-app-dispatch-ui/package.json` (W2 primary check)
    - Exactly ONE of: (a) `hussle-app-dispatch-ui/package.json`'s `"validate"` script value contains `test:e2e` (regex `"validate"\s*:\s*"[^"]*test:e2e[^"]*"`), OR (b) `Jenkinsfile.build` was edited to add `npm run test:e2e` after the existing validate step. NOT both, NOT neither (W2 deterministic two-branch fix).
    - VALIDATION.md frontmatter updated: `nyquist_compliant: true` (BLOCKER 1 — verified by `grep -q 'nyquist_compliant: true' .planning/phases/01-stabilize/01-VALIDATION.md`)
    - VALIDATION.md Approval line flipped to "approved — Plan 08 close-out"
    - Per-Task Verification Map STAB-14 status flipped to ✅ green
  </acceptance_criteria>
  <done>Smoke checklist committed; CI hook for Playwright in place via deterministic (a) or (b) branch; VALIDATION.md nyquist_compliant flag flipped (BLOCKER 1); STAB-15 ready for user walk-through.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User walks SMOKE-CHECKLIST on a phone + ticks boxes — STAB-15 sign-off</name>
  <what-built>
    - Plan 06: Full carrier-portal page wiring (Save & Continue → saga → API → advance; validation snackbar + scroll-to-error; completeOnboarding dispatch; Phase-4 CostResultCard conditional render)
    - Plan 07: All input components wired (PresetTileSelector, StateGrid a11y, SubQuestion typography)
    - Plan 08 Task 1: Playwright e2e green locally (STAB-14)
    - Plan 08 Task 2: SMOKE-CHECKLIST.md committed + CI hook confirmed/added via deterministic branch + VALIDATION.md `nyquist_compliant: true` flag flipped (BLOCKER 1)
  </what-built>
  <how-to-verify>
    1. **`[ASSUMED]` constants acknowledgment (W4 — REQUIRED FIRST STEP):**
       Confirm the constants in `hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/computeCostAnalysis.ts`:
       - `ASSUMED_MONTHLY_MILES = 8000`
       - `MIN_PROFIT_MARGIN = 0.25` (25%)
       Per RESEARCH.md A1, these are reverse-engineered from `.planning-legacy/carrier-onboarding/designs/interview-cost-analysis.md` sample numbers (break-even $1.94, minimum $2.44).
       Either:
       - ACCEPT the defaults — record `ASSUMED constants accepted as-is: 8000 miles / 25% margin` in SMOKE-CHECKLIST.md notes, OR
       - OVERRIDE with new values — update `computeCostAnalysis.ts`, run `npm test -- CostResultCard`, and record the new values in SMOKE-CHECKLIST.md notes.
       This step MUST be acknowledged before the phone walk-through begins — the cost numbers shown in Phase 4 depend on it.

    2. Boot the dev stack locally: `docker compose up` (or `cd hussle-app-dispatch-ui && npm run dev` if running standalone) and the API in dev mode.
    3. Generate a real invite token via the carrier-portal flow OR use a dev seed script.
    4. Open the invite link on iPhone Safari. Walk all 6 phases. Tick boxes in `.planning/phases/01-stabilize/SMOKE-CHECKLIST.md` as you go.
    5. Repeat on Android Chrome.
    6. Repeat on desktop Chrome (sanity).
    7. Validation failure path: on any phase, tap Save & Continue with required fields blank. Confirm a snackbar appears AND the page scrolls to (and focuses) the first error field. Tick the box.
    8. Reduced-motion path: enable iOS Settings → Accessibility → Motion → Reduce Motion. Re-walk Phase 4 (Cost Analysis) and confirm the CostResultCard renders final values immediately (no count-up animation). Tick the box.
    9. Commit the completed SMOKE-CHECKLIST.md with your sign-off, date, build commit SHA, and CI hook route taken (a) or (b).
    10. Confirm `npx playwright test e2e/carrier-portal-full.spec.ts` exits 0 in CI (check Jenkins build for the Phase-1 closing commit).

    Phase 1 exit criteria (ROADMAP):
    - [ ] Criterion 1: Carrier completes 6-phase flow end-to-end on a phone — verified via SMOKE-CHECKLIST
    - [ ] Criterion 2: Save & Continue dispatches saga path with completeOnboarding final — verified via tests + smoke
    - [ ] Criterion 3: Validation surfaces (snackbar + scroll) — verified via SMOKE-CHECKLIST
    - [ ] Criterion 4: Phase metadata lives in single constants.ts — verified via grep + tests
    - [ ] Criterion 5: Playwright e2e green in CI — verified via Jenkins build

    Once all 5 boxes are checked, mark Phase 1 complete in `.planning/ROADMAP.md` and `.planning/STATE.md`. Phase 2 (Security & Hygiene) becomes unblocked.
  </how-to-verify>
  <files>
    .planning/phases/01-stabilize/SMOKE-CHECKLIST.md
  </files>
  <action>
    Pause for human verification. The executor presents the SMOKE-CHECKLIST.md and the Playwright CI status, then waits for the user to:
    1. FIRST acknowledge the `[ASSUMED]` constants (W4 — accept defaults OR override; record decision in SMOKE-CHECKLIST notes),
    2. Walk the flow on a real phone and tick the boxes per the steps in <how-to-verify> above,
    3. Sign off in SMOKE-CHECKLIST.md (Reviewer + Date + Build commit SHA + CI hook route + Notes filled in).
    After user signs off, the executor commits the completed checklist and updates ROADMAP / STATE.md to mark Phase 1 complete.
  </action>
  <verify>
    <automated>grep -E "^Reviewer:\s*\S+" .planning/phases/01-stabilize/SMOKE-CHECKLIST.md && grep -E "^Date:\s*\S+" .planning/phases/01-stabilize/SMOKE-CHECKLIST.md && grep -E "^Build commit SHA:\s*\S+" .planning/phases/01-stabilize/SMOKE-CHECKLIST.md && grep -E "ASSUMED constants" .planning/phases/01-stabilize/SMOKE-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `[ASSUMED]` constants explicitly acknowledged in SMOKE-CHECKLIST.md notes (accepted or overridden) — W4 fix
    - All device-section checkboxes in SMOKE-CHECKLIST.md are checked (iOS Safari, Android Chrome, desktop Chrome minimum)
    - Validation-failure-path checkbox checked
    - Reduced-motion checkbox checked
    - Sign-off section completed (Reviewer, Date, Build commit SHA, CI hook route taken filled in)
    - Playwright CI run for the Phase-1 closing commit is green
    - ROADMAP and STATE.md updated to reflect Phase 1 complete + Phase 2 ready-to-plan
  </acceptance_criteria>
  <resume-signal>
    Type "approved" with: (1) SMOKE-CHECKLIST commit SHA, (2) Jenkins build URL, AND (3) explicit `[ASSUMED]` constants decision ("accepted defaults: 8000 miles / 25% margin" OR "overrode to: X miles / Y% margin"). OR describe issues for the executor to fix and re-verify.
  </resume-signal>
  <done>SMOKE-CHECKLIST signed off (with explicit `[ASSUMED]` constants acknowledgment per W4); Phase 1 exit criteria 1-5 all green; Phase 2 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (e2e — fake) | Spec uses fake `'e2e-test-token'` only — all API mocked via `page.route()`; no real backend reached. |
| (manual smoke — real) | User walks a real dev environment with a real invite token. Local-dev — no production exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information disclosure | Playwright test artifacts (videos/traces) leaking real tokens | mitigate | Spec uses fake `'e2e-test-token'` only — never a real invite token. Acceptance criterion grep-asserts no 32+ char hex sequences. RESEARCH.md § Security Domain row 5. |
| T-08-02 | Information disclosure | SMOKE-CHECKLIST.md leaking real customer/carrier data | accept | Checklist sign-off captures device + build commit SHA + free-text notes; user is responsible for not pasting real carrier PII into the notes field. Checklist is gitignored from production deployments (lives in `.planning/`). |

No HIGH-severity threats — Phase 1 is a stabilization phase per RESEARCH.md § Security Domain.
</threat_model>

<verification>
- `cd hussle-app-dispatch-ui && npx playwright test e2e/carrier-portal-full.spec.ts` exits 0 (≥1 passing test, 0 failing)
- `.planning/phases/01-stabilize/SMOKE-CHECKLIST.md` exists and is committed
- Exactly one CI hook branch holds: either (a) `npm run validate` already includes `test:e2e` (regex match in package.json), or (b) `Jenkinsfile.build` was edited to add `npm run test:e2e` after validate (W2 fix)
- `grep -q 'nyquist_compliant: true' .planning/phases/01-stabilize/01-VALIDATION.md` exits 0 (BLOCKER 1 administrative gate flipped)
- ROADMAP Phase 1 success criteria 1-5 all checkable
- User has completed and committed SMOKE-CHECKLIST with sign-off, including explicit `[ASSUMED]` constants acknowledgment (W4 — verified at Task 3 checkpoint)
</verification>

<success_criteria>
- STAB-14: Playwright e2e green locally and in CI (carrier-portal-full.spec.ts walks invite → 6 phases → completion view, including Phase-4 CostResultCard path)
- STAB-15: Manual smoke checklist filled in and committed, with `[ASSUMED]` constants acknowledgment (W4)
- Phase 1 ROADMAP success criteria 1-5 all green
- VALIDATION.md `nyquist_compliant: true` flag flipped (BLOCKER 1)
- Phase 2 (Security & Hygiene) becomes unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-08-SUMMARY.md`. Then update `.planning/STATE.md` to mark Phase 1 complete and Phase 2 Ready-to-plan.
</output>
</content>
</invoke>