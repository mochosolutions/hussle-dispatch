---
phase: 01-stabilize
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - hussle-app-dispatch-ui/src/features/carrier-portal/questions/lanePreferencesQuestions.ts
autonomous: true
requirements: [STAB-09]

must_haves:
  truths:
    - "`lanePreferencesQuestions.ts` exports a `QuestionDefinition[]` with at least one `stateGrid` question for `lanePreferences.statePreferences`"
    - "Field ID matches API validator (`statePreferences` per lanePreferencesValidator.ts:28-41)"
    - "Scope is minimum per UI-SPEC § Component Inventory C — only `statePreferences` ships in Phase 1; the other API-accepted fields (`homeBaseCity`, `homeBaseState`, `maxDaysOut`, `freightPreferences`, `preferredLanes`) are deferred per RESEARCH.md A8"
  artifacts:
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/questions/lanePreferencesQuestions.ts
      provides: Phase 5 question schema (stateGrid)
      contains: "lanePreferences.statePreferences"
      exports: ["lanePreferencesQuestions"]
  key_links:
    - from: hussle-app-dispatch-ui/src/features/carrier-portal/questions/lanePreferencesQuestions.ts
      to: hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts
      via: "import type { QuestionDefinition }"
      pattern: "import type.*QuestionDefinition"

threat_model:
  trust_boundaries: []
  notes: "Static TS data file. No new endpoint, no auth path, no PII handling. Question label is a literal string — no XSS surface."
---

<objective>
Create the Phase 5 (Lane Preferences) question schema. Minimum scope per UI-SPEC § Component Inventory C and RESEARCH.md Open Question #4 / A8: ship the primary `stateGrid` question only. The API validator at `hussle-app-dispatch-api/src/carrier-portal/validators/lanePreferencesValidator.ts:28-41` accepts 6 fields total (`homeBaseCity`, `homeBaseState`, `maxDaysOut`, `preferredLanes`, `statePreferences`, `freightPreferences`); only `statePreferences` is required in Phase 1 to satisfy STAB-09 ("Lane Preferences phase exists end-to-end with at least one functional question"). Other fields are explicitly deferred per the user-decision-fidelity rule (deferred ideas in CONTEXT.md / RESEARCH.md should not be planned).

Purpose: STAB-09 — the file simply needs to exist so Phase 5 has a question to render. The `StateGrid` component (already built — 132 lines, wired into InputRenderer at line 410 today) renders this question type automatically.

Output: 1 new file. Hooks into `QUESTIONS_BY_PHASE[5]` via Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-stabilize/01-RESEARCH.md
@.planning/phases/01-stabilize/01-PATTERNS.md
@.planning/phases/01-stabilize/01-UI-SPEC.md
@CLAUDE.md
@hussle-app-dispatch-ui/src/features/carrier-portal/questions/companyQuestions.ts
@hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts

<interfaces>
From companyQuestions.ts: analog file structure to mirror (PATTERNS.md § 5).

From questionSchema.ts: `QuestionDefinition` discriminated union — `inputType: 'stateGrid'` variant exists today (verified — InputRenderer routes it at line 410).

API validator (`hussle-app-dispatch-api/src/carrier-portal/validators/lanePreferencesValidator.ts:28-41`): body accepts `statePreferences: Array<{ state: string; preference: 'PREFERRED' | 'AVOIDED' }>`.

StateGrid component contract (`features/carrier-portal/components/StateGrid/index.tsx:1-30` or thereabouts): renders 50-state grid with 3-state cycle, value/onChange interface (Plan 07 adds a11y attrs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lanePreferencesQuestions schema (stateGrid only)</name>
  <read_first>
    - hussle-app-dispatch-ui/src/features/carrier-portal/questions/companyQuestions.ts (analog file structure)
    - hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts (QuestionDefinition type)
    - hussle-app-dispatch-api/src/carrier-portal/validators/lanePreferencesValidator.ts (API field-name truth)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 5 (skeleton — verbatim)
    - .planning/phases/01-stabilize/01-RESEARCH.md § Code Examples — Lane preferences questions
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/questions/lanePreferencesQuestions.ts
  </files>
  <action>
    Create the file mirroring `companyQuestions.ts` structure (PATTERNS.md § 5). Phase constant = 5. Single question:

    ```typescript
    import type { QuestionDefinition } from 'components/ConversationalForm/questionSchema';

    const PHASE = 5;

    export const lanePreferencesQuestions: QuestionDefinition[] = [
      {
        id: 'lanePreferences.statePreferences',
        phase: PHASE,
        inputType: 'stateGrid',
        label: 'Which states do you prefer? Which do you avoid?',
        hint: 'Tap to cycle through neutral, preferred, and avoided.',
        required: false,
      },
    ];
    ```

    Copy phrasing per UI-SPEC § Copywriting (direct, calm, second-person, no exclamation marks).

    Constraints:
    - No new types in this plan — `inputType: 'stateGrid'` already exists in the `QuestionDefinition` union (verified — InputRenderer routes it today at L410).
    - Field ID `lanePreferences.statePreferences` is the UI namespace; the saga payload mapping in Plan 06 strips the prefix to send `statePreferences` as the API body key.
    - `required: false` — UI-SPEC says state preferences are optional (carrier can submit without selecting any).
    - No `any`, no `as`, no `!`.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && test -f src/features/carrier-portal/questions/lanePreferencesQuestions.ts && grep -q "lanePreferences.statePreferences" src/features/carrier-portal/questions/lanePreferencesQuestions.ts && grep -q "'stateGrid'" src/features/carrier-portal/questions/lanePreferencesQuestions.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `hussle-app-dispatch-ui/src/features/carrier-portal/questions/lanePreferencesQuestions.ts`
    - Exports `lanePreferencesQuestions: QuestionDefinition[]` with at least 1 entry
    - The entry has `id: 'lanePreferences.statePreferences'`, `phase: 5`, `inputType: 'stateGrid'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Lane preferences schema exists; TS compiles; STAB-09 satisfied for minimum-scope shipment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static TS data file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | lanePreferencesQuestions.ts | accept | Static literal. No user-controlled input. |

No HIGH-severity threats.
</threat_model>

<verification>
- `cd hussle-app-dispatch-ui && npm run check-ts` exits 0
- `cd hussle-app-dispatch-ui && npm run lint -- --quiet` clean
</verification>

<success_criteria>
- STAB-09: `lanePreferencesQuestions.ts` exists with at least one `stateGrid` question
- TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-05-SUMMARY.md`.
</output>
