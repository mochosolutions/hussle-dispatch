---
phase: 01-stabilize
plan: 07
type: execute
wave: 4
depends_on: [04, 06]
files_modified:
  - hussle-app-dispatch-ui/src/components/ConversationalForm/InputRenderer.tsx
  - hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx
  - hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx
autonomous: true
requirements: [STAB-07, STAB-10, STAB-11, STAB-12]

must_haves:
  truths:
    - "`InputRenderer.tsx` `inputType: 'presetTiles'` case renders `<PresetTileSelector presets={question.presets} value={...} onChange={...} />` instead of the `PresetTilesPlaceholder` stub at lines 293-307 (STAB-12)"
    - "`PresetTileSelector` component is wired and verified (STAB-07 — component already built; this plan flips it on)"
    - "`StateGrid` tiles have `role='button'`, `aria-pressed={preference !== undefined}`, and `aria-label='{StateName}: {Neutral|Preferred|Avoided}, tap to cycle'`; keyboard handler (Enter/Space) triggers the same cycle as click (STAB-10 a11y attrs)"
    - "`SubQuestion.tsx` label rendering uses `fontSize: 16, fontWeight: 600` (not 18px) — Table 1 row 'Body strong / Sub-question' (STAB-11; UI-SPEC § Typography Table 1)"
  artifacts:
    - path: hussle-app-dispatch-ui/src/components/ConversationalForm/InputRenderer.tsx
      provides: presetTiles case wired to PresetTileSelector; placeholder stub deleted
      contains: "<PresetTileSelector"
    - path: hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx
      provides: typography demoted to 16/600 per UI-SPEC Table 1
      contains: "fontSize: 16"
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx
      provides: a11y attrs on each tile
      contains: "aria-pressed"
  key_links:
    - from: hussle-app-dispatch-ui/src/components/ConversationalForm/InputRenderer.tsx
      to: hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/index.tsx
      via: import + JSX render
      pattern: "PresetTileSelector"

threat_model:
  trust_boundaries: []
  notes: "UI-only changes — wiring an existing component into a switch, fixing typography pixels, adding ARIA attrs. No new endpoints, no auth, no PII handling. No new XSS surface (all rendered content is theme-styled MUI components)."
---

<objective>
Wire the last component-level gaps:
- `InputRenderer` actually renders `<PresetTileSelector>` for `inputType: 'presetTiles'` — replaces the existing placeholder stub at lines 293-307 (STAB-12). This is the keystone for STAB-07 working end-to-end (the component exists, this flips it on).
- `StateGrid` tiles get the missing ARIA attrs (`role`, `aria-pressed`, `aria-label`) + keyboard handler — STAB-10.
- `SubQuestion` typography demotes from 18px to 16/600 per UI-SPEC Table 1 — STAB-11.

These three changes touch different files in different modules; they all flip Plan-01 test scaffolds from red/todo to green.

Purpose: Phase 4 (cost analysis) becomes interactive — without this plan, the `presetTiles` cases render placeholder text. Phase 5 (lane preferences) tiles become screen-reader-navigable. Sub-questions match the design contract.

Output: 3 modified files. `InputRenderer.test.tsx`, `StateGrid.test.tsx`, `SubQuestion.test.tsx` from Plan 01 flip green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-stabilize/01-RESEARCH.md
@.planning/phases/01-stabilize/01-PATTERNS.md
@.planning/phases/01-stabilize/01-UI-SPEC.md
@CLAUDE.md
@hussle-app-dispatch-ui/src/components/ConversationalForm/InputRenderer.tsx
@hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx
@hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx
@hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/index.tsx
@hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts

<interfaces>
After Plan 04, `questionSchema.ts` exports `PresetOption` and `QuestionDefinition` includes `presets?: PresetOption[]` on the `presetTiles` variant.

`PresetTileSelector` props (existing — `features/carrier-portal/components/PresetTileSelector/index.tsx`): `{ presets: PresetOption[]; value: number | null; onChange: (value: number | null) => void }`.

`InputRenderer.tsx:293-307` — current placeholder stub `PresetTilesPlaceholder` that renders "PresetTiles (built in T-31)" text. Delete and replace.

`InputRenderer.tsx:410` — existing `'stateGrid'` case (analog — mirror its shape for the `'presetTiles'` case).

`SubQuestion.tsx:55-59` — current typography pin (`fontSize: '18px'`). Demote to 16/600.

`StateGrid.tile` JSX (`StateGrid/index.tsx`, ~line 80-120 — confirm by reading) — each tile renders an MUI `Box` or `ButtonBase`. Add `role='button'`, `aria-pressed`, `aria-label`, and `onKeyDown` for Enter/Space.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire presetTiles case in InputRenderer + delete placeholder stub</name>
  <read_first>
    - hussle-app-dispatch-ui/src/components/ConversationalForm/InputRenderer.tsx (line 410 — stateGrid case analog; lines 293-307 — PresetTilesPlaceholder stub to delete)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/index.tsx (props shape)
    - hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts (presets? field shape after Plan 04)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 9 (InputRenderer pattern)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/components/ConversationalForm/InputRenderer.tsx
  </files>
  <action>
    Two coordinated edits.

    **A. Delete the `PresetTilesPlaceholder` stub** at lines 293-307 (or wherever it lives — grep `PresetTilesPlaceholder` in the file). The component definition AND the corresponding `case 'presetTiles': return <PresetTilesPlaceholder ... />` line both go.

    **B. Add a real `'presetTiles'` case** mirroring the `'stateGrid'` case at line 410. Approximate shape:
    ```typescript
    case 'presetTiles': {
      const value = typeof formikField.value === 'number' ? formikField.value : null;
      return (
        <PresetTileSelector
          presets={question.presets ?? []}
          value={value}
          onChange={(next) => formikHelpers.setValue(next)}
        />
      );
    }
    ```
    Use whatever access path InputRenderer uses today for `formikField` / `formikHelpers` (the `stateGrid` case at line 410 is the verbatim analog — copy its data wiring). The fallback `question.presets ?? []` keeps the type narrow without `as`.

    Add `import { PresetTileSelector } from 'features/carrier-portal/components/PresetTileSelector';` to the import block (path alias `features/*` resolves per tsconfig).

    Constraints:
    - No `as`, no `!`, no `any`. The `as` migration override is allowed ONLY for `formik.errors[name] as string | undefined` — not here.
    - `case 'presetTiles': { ... }` block-scoped to avoid TS variable-narrowing conflicts with sibling cases (per existing pattern in the same switch).
    - Verify `InputRenderer.test.tsx` (Plan 01) `it.todo('routes inputType="presetTiles" to PresetTileSelector — STAB-12')` flips to a real test that passes.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -v '^[[:space:]]*//' src/components/ConversationalForm/InputRenderer.tsx | grep -v "^[[:space:]]*\*" | grep -c "PresetTilesPlaceholder" | grep -q '^0$' && grep -q "<PresetTileSelector" src/components/ConversationalForm/InputRenderer.tsx && grep -q "from 'features/carrier-portal/components/PresetTileSelector'" src/components/ConversationalForm/InputRenderer.tsx && npx tsc --noEmit -p tsconfig.app.json && npx jest --testPathPattern='InputRenderer\.test\.tsx' --silent 2>&1 | grep -E '(passed|failed)' | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `PresetTilesPlaceholder` no longer appears in `InputRenderer.tsx` (grep returns 0 outside comments)
    - `<PresetTileSelector` appears in `InputRenderer.tsx` JSX
    - `case 'presetTiles':` returns the real component
    - `InputRenderer.test.tsx` STAB-12 expectation flips green
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>presetTiles case wired; placeholder stub deleted; STAB-07 and STAB-12 satisfied; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Demote SubQuestion typography to 16/600 + add StateGrid a11y attrs</name>
  <read_first>
    - hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx (full file — lines 55-59 typography fix; line 36 confirms data-question-id stays)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx (full file — tile JSX around lines 80-120; add a11y attrs and key handler)
    - .planning/phases/01-stabilize/01-UI-SPEC.md (search for "Table 1" — confirm 16/600 spec; search for "Accessibility" — confirm StateGrid a11y requirements)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 10 + § 11 (SubQuestion + StateGrid patterns)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx,
    hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx
  </files>
  <action>
    Two independent edits in different files.

    **A. `SubQuestion.tsx` (lines 55-59 area) — STAB-11:**
    Change the label `Typography`/`Box` `sx` from `fontSize: '18px'` (or whatever pixel value is hardcoded today) to `fontSize: 16, fontWeight: 600`. Use MUI numeric tokens (no `'px'` suffix string) for `fontSize: 16`; that resolves to `16px` through the theme. Visual differentiation is preserved by the 4px colored left border + category tag + indent — per UI-SPEC Table 1 row note.

    Do NOT remove the `data-question-id={questionId}` attribute on line 36 — that's the scroll-to-error hook from Plan 06.

    **B. `StateGrid/index.tsx` tile JSX — STAB-10:**
    Add three ARIA attributes and a keyboard handler to each tile element:
    - `role="button"` (or use MUI `ButtonBase` which provides this automatically — check what the file uses today)
    - `aria-pressed={preference !== undefined}` — `true` when the state is `'PREFERRED'` or `'AVOIDED'`, `false` when neutral
    - `aria-label={`${stateName}: ${labelForPreference(preference)}, tap to cycle`}` — where `labelForPreference(undefined) = 'Neutral'`, `('PREFERRED') = 'Preferred'`, `('AVOIDED') = 'Avoided'`
    - `tabIndex={0}` so it's keyboard-focusable
    - `onKeyDown` handler: if `e.key === 'Enter' || e.key === ' '`, call `e.preventDefault()` then invoke the same cycle handler the `onClick` calls

    Extract a small helper `labelForPreference(preference: 'PREFERRED' | 'AVOIDED' | undefined): string` near the top of the file rather than inlining the ternary in the `aria-label` template. Pure function — no side effects.

    Constraints:
    - No `as`, no `!`, no `any`. MUI `sx` only (no `style={{}}`, no styled-components).
    - Keep all existing visual styling — only add a11y attrs and the keyboard handler.
    - Do NOT change the 3-state cycle logic; just expose it via keyboard.
    - `SubQuestion.test.tsx` and `StateGrid.test.tsx` from Plan 01 should flip green.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -q "fontSize:\s*16" src/components/ConversationalForm/SubQuestion.tsx && grep -q "fontWeight:\s*600" src/components/ConversationalForm/SubQuestion.tsx && grep -v '^[[:space:]]*//' src/components/ConversationalForm/SubQuestion.tsx | grep -v "^[[:space:]]*\*" | grep -c "fontSize:\s*['\"]18px['\"]" | grep -q '^0$' && grep -q "aria-pressed" src/features/carrier-portal/components/StateGrid/index.tsx && grep -q "aria-label" src/features/carrier-portal/components/StateGrid/index.tsx && grep -q "onKeyDown" src/features/carrier-portal/components/StateGrid/index.tsx && npx tsc --noEmit -p tsconfig.app.json && npx jest --testPathPattern='(SubQuestion|StateGrid)\.test\.tsx' --silent 2>&1 | grep -E '(passed|failed)' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `SubQuestion.tsx` no longer contains `fontSize: '18px'` (grep returns 0 outside comments)
    - `SubQuestion.tsx` contains `fontSize: 16` and `fontWeight: 600`
    - `StateGrid` tile JSX contains `aria-pressed`, `aria-label`, `onKeyDown` attrs
    - `SubQuestion.test.tsx` STAB-11 expectation passes
    - `StateGrid.test.tsx` STAB-10 a11y expectations pass
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SubQuestion typography fixed; StateGrid a11y attrs added; both test scaffolds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | UI-only changes — wiring, typography, ARIA attrs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering (XSS) | StateGrid `aria-label` interpolation | mitigate | `stateName` is a static schema constant (e.g., `'California'`); never user-controlled. MUI escapes attribute values by default. |

No HIGH-severity threats.
</threat_model>

<verification>
- `cd hussle-app-dispatch-ui && npm run check-ts` exits 0
- `cd hussle-app-dispatch-ui && npm run lint -- --quiet` clean
- `cd hussle-app-dispatch-ui && npm test -- --testPathPattern='(InputRenderer|SubQuestion|StateGrid|PresetTileSelector)\.test\.tsx' --silent` shows all four tests green
- Grep audit: `grep -r "PresetTilesPlaceholder" hussle-app-dispatch-ui/src` returns nothing
</verification>

<success_criteria>
- STAB-07: PresetTileSelector wired (component already existed; this flips it on via InputRenderer)
- STAB-10: StateGrid has `role`, `aria-pressed`, `aria-label`, keyboard handler
- STAB-11: SubQuestion typography matches UI-SPEC Table 1 (16/600)
- STAB-12: InputRenderer routes `presetTiles` → PresetTileSelector
- Four component test scaffolds flip green
- TypeScript compiles; lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-07-SUMMARY.md`.
</output>
