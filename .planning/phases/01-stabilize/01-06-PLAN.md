---
phase: 01-stabilize
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04, 05]
files_modified:
  - hussle-app-dispatch-ui/src/features/carrier-portal/constants.ts
  - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
autonomous: true
requirements: [STAB-01, STAB-02, STAB-03, STAB-04, STAB-08]

must_haves:
  truths:
    - "`constants.ts QUESTIONS_BY_PHASE[4]` imports real `costAnalysisQuestions` (placeholder `[]` from Plan 03 replaced)"
    - "`constants.ts QUESTIONS_BY_PHASE[5]` imports real `lanePreferencesQuestions`"
    - "`CarrierPortalPage/index.tsx` imports `PHASE_LABELS`, `TOTAL_PHASES`, `QUESTIONS_BY_PHASE` from `../../constants` and contains no inline `const PHASE_LABELS = [...]` declaration (STAB-04 page side)"
    - "`handleSubmit` dispatches per-phase save actions (1→saveCompany, 2→saveEquipment, 3→saveDrivers, 4→saveCostAnalysis, 5→saveLanePreferences) via a `buildSaveActionForPhase` helper; final phase (6) dispatches `completeOnboarding()` (STAB-01 + STAB-03)"
    - "A `useEffect` on `lastSavedPhase` advances `currentPhase` when `lastSavedPhase === currentPhase` and dispatches `phaseAdvanceConsumed()` to clear the signal (STAB-01 rising-edge per RESEARCH.md Pattern 2)"
    - "After `completeOnboardingSuccess` fires (Plan 02 reducer populates `state.session.completedAt`), `CarrierPortalPage` renders `PortalCompleteView` because `session.completedAt` is non-null. The existing render-condition (`grep -n 'session.completedAt\\|session\\.completedAt' src/features/carrier-portal/pages/CarrierPortalPage/index.tsx` — already present today) is preserved by this plan, NOT removed (BLOCKER 2 transition gate)."
    - "`handleContinue` fires notistack `enqueueSnackbar` with `preventDuplicate: true` + stable key `'phase-validation-error'` on validation failure AND calls `scrollIntoView()` on `[data-question-id=<firstErrorId>]` (STAB-02 verbatim from UI-SPEC § Scroll-to-error)"
    - "`prefers-reduced-motion` honored for both `handleContinue` scroll and page-advance scroll (UI-SPEC § Animations)"
    - "No file in the page or page handlers dispatches `sessionCompleted(...)` (STAB-03 — reducer stays for server-payload reduction, but page-side dispatch is gone)"
    - "Saga payload mapping strips UI namespace prefixes (`costAnalysis.truckPayment` → `truckPayment`) per RESEARCH.md A7"
    - "Phase 4 question thread is replaced by `<CostResultCard inputs={...} firstName={...} onContinue={...} />` once all 6 cost-analysis fields are populated in formik state. The CTA's `onContinue` dispatches `saveCostAnalysis(payload)` via the same `buildSaveActionForPhase(4, ...)` path (BLOCKER 3 wiring)."
  artifacts:
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/constants.ts
      provides: real costAnalysisQuestions + lanePreferencesQuestions wired into QUESTIONS_BY_PHASE
      contains: "costAnalysisQuestions"
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
      provides: saga-driven Save & Continue, snackbar + scroll-to-error, completeOnboarding dispatch, constants-imported labels, conditional CostResultCard render for Phase 4
      contains: "enqueueSnackbar"
  key_links:
    - from: hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
      to: hussle-app-dispatch-ui/src/features/carrier-portal/store/slices/carrierPortalSlice.ts
      via: "dispatch(carrierPortalActions.saveCostAnalysis(...)), useSelector(selectLastSavedPhase)"
      pattern: "(saveCostAnalysis|saveLanePreferences|completeOnboarding|selectLastSavedPhase|phaseAdvanceConsumed)"
    - from: hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
      to: notistack
      via: enqueueSnackbar call on validation failure
      pattern: "enqueueSnackbar"
    - from: hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
      to: hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx
      via: "Conditional render when currentPhase === 4 AND all 6 cost-analysis fields populated. CostResultCard receives `inputs` as a prop extracted from formik.values (no Redux) and `onContinue` wired to dispatch saveCostAnalysis (BLOCKER 3 + 4)"
      pattern: "CostResultCard"

threat_model:
  trust_boundaries:
    - "client→API: page dispatches saga actions that POST to /carrier-portal/{phase} with bearer token in Authorization header"
  notes: "Trust boundary unchanged from existing saveCompany pattern. The new dispatches reuse the same authenticateCarrierToken middleware and portalHeaders helper. Validation snackbar copy is a literal string — never echoes user input (UI-SPEC § Validation feedback). No new auth path, no new PII handling."
---

<objective>
Wire `CarrierPortalPage` end-to-end against the Plan-02 slice/saga foundation, the Plan-03 constants module, and the Plan-04/05 question schemas. After this plan:
- Save & Continue saves to the API via dispatched sagas (STAB-01 full).
- Validation failures surface visibly (toast + scroll-to-error, reduced-motion honored) (STAB-02).
- Final phase calls `completeOnboarding()`; the existing `session.completedAt` render-branch swaps the page to `PortalCompleteView` (STAB-03 full; BLOCKER 2 gate).
- Page imports phase metadata from `constants.ts` (STAB-04 page side).
- `QUESTIONS_BY_PHASE` placeholder arrays replaced with real schemas.
- Phase 4 question thread is replaced by `CostResultCard` once all 6 cost fields are answered; CTA dispatches `saveCostAnalysis` (BLOCKER 3 wiring; STAB-08 integration).

This is the keystone plan for STAB-01..STAB-03 and the integration site for STAB-08. All three saga/slice mechanisms exist (Plan 02), all four constants/schemas exist (Plans 03/04/05), and `CostResultCard` is built (Plan 04) — this plan glues them into the page.

Output: 2 modified files. `CarrierPortalPage.test.tsx` scaffolded in Plan 01 flips green for STAB-01/02/03/08 expectations (including the BLOCKER 2 `PortalCompleteView` transition test and the BLOCKER 3 CostResultCard conditional render test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-stabilize/01-RESEARCH.md
@.planning/phases/01-stabilize/01-PATTERNS.md
@.planning/phases/01-stabilize/01-UI-SPEC.md
@CLAUDE.md
@hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
@hussle-app-dispatch-ui/src/features/carrier-portal/constants.ts
@hussle-app-dispatch-ui/src/features/carrier-portal/store/slices/carrierPortalSlice.ts
@hussle-app-dispatch-ui/src/features/carrier-portal/store/selectors/portalSelectors.ts
@hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx

<interfaces>
After Plan 02: `carrierPortalActions` exports `saveCostAnalysis(payload)`, `saveLanePreferences(payload)`, `phaseAdvanceConsumed()`, `completeOnboarding()`. State has top-level `currentPhase: number` and `lastSavedPhase: number | null`. Selectors `selectCurrentPhase`, `selectLastSavedPhase`. `completeOnboardingSuccess` reducer populates `state.session.completedAt` from the API response payload (BLOCKER 2 gate).

After Plan 03: `constants.ts` exports `PHASE_LABELS` (6 strings) and `TOTAL_PHASES === 6`; `QUESTIONS_BY_PHASE[4]` and `[5]` are placeholder `[]`.

After Plans 04/05: `costAnalysisQuestions` and `lanePreferencesQuestions` exist. `CostResultCard` is a PURE component accepting `inputs: CostInputs`, `firstName?: string`, `onContinue: () => void` as props — no Redux (BLOCKER 4).

Bugs to fix (per RESEARCH.md § Anti-Patterns + PATTERNS.md § 7):
- `CarrierPortalPage:25-33` — hardcoded `PHASE_LABELS` / `questionsByPhase`
- `CarrierPortalPage:76-88` — `handleSubmit` bypasses saga
- `CarrierPortalPage:81-83` — dispatches `sessionCompleted` instead of `completeOnboarding`
- `CarrierPortalPage:188-198` — `handleContinue` swallows validation

Existing `session.completedAt` render branch (BLOCKER 2 gate): grep the page file for `completedAt` to locate the existing conditional render that swaps to `PortalCompleteView`. The branch is already present today (page-level: when `session?.completedAt` is non-null, render `<PortalCompleteView ... />` instead of the question thread). This plan PRESERVES that branch — Plan 02's `completeOnboardingSuccess` reducer is what fills the gate value.

`enqueueSnackbar` is imported as `import { enqueueSnackbar } from 'notistack';` (already in deps).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire constants to real schemas + migrate page constants imports</name>
  <read_first>
    - hussle-app-dispatch-ui/src/features/carrier-portal/constants.ts (placeholders from Plan 03)
    - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx (lines 25-33 — inline hardcode)
    - hussle-app-dispatch-ui/src/features/carrier-portal/questions/costAnalysisQuestions.ts (from Plan 04)
    - hussle-app-dispatch-ui/src/features/carrier-portal/questions/lanePreferencesQuestions.ts (from Plan 05)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 6 (target constants.ts shape)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/constants.ts,
    hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
  </files>
  <action>
    Two sequential edits.

    **1. `constants.ts` — replace placeholders:**
    Add imports at top: `import { costAnalysisQuestions } from './questions/costAnalysisQuestions';` and `import { lanePreferencesQuestions } from './questions/lanePreferencesQuestions';`. In `QUESTIONS_BY_PHASE`, replace `4: []` with `4: costAnalysisQuestions` and `5: []` with `5: lanePreferencesQuestions`. Remove the planning comment about placeholders.

    **2. `CarrierPortalPage/index.tsx` — STAB-04 page side:**
    Add `import { PHASE_LABELS, TOTAL_PHASES, QUESTIONS_BY_PHASE } from '../../constants';` to the import block (per CLAUDE.md import order — internal group, alphabetized). Remove the inline declarations at lines 25-33 (`const PHASE_LABELS`, `const TOTAL_PHASES`, `const questionsByPhase`). Replace every in-file reference: `PHASE_LABELS` stays the same, `TOTAL_PHASES` stays the same, `questionsByPhase` becomes `QUESTIONS_BY_PHASE`. If a downstream consumer rejects readonly arrays from `as const`, spread to a fresh array (`[...QUESTIONS_BY_PHASE[currentPhase]]`) at the call site — do NOT add `as` casts.

    Do NOT modify `handleSubmit` / `handleContinue` yet — that's Task 2a/2b.

    Constraints: `import type` for type-only imports. No `as`, no `!`, no `any`.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -q "import { costAnalysisQuestions }" src/features/carrier-portal/constants.ts && grep -q "import { lanePreferencesQuestions }" src/features/carrier-portal/constants.ts && grep -v '^[[:space:]]*//' src/features/carrier-portal/constants.ts | grep -v "^[[:space:]]*\*" | grep -E "^\s*[45]:\s*\[\s*\]" | grep -vq . && grep -v '^[[:space:]]*//' src/features/carrier-portal/pages/CarrierPortalPage/index.tsx | grep -v "^[[:space:]]*\*" | grep -c "const PHASE_LABELS\s*=" | grep -q '^0$' && grep -q "from '../../constants'" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `constants.ts` no longer has placeholder `4: []` / `5: []` entries
    - `constants.ts` imports both real schemas
    - `CarrierPortalPage/index.tsx` no longer has a top-level `const PHASE_LABELS =`
    - `CarrierPortalPage/index.tsx` imports from `'../../constants'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Constants fully wired; page imports from constants; TS compiles.</done>
</task>

<task type="auto">
  <name>Task 2a: Saga-dispatch handleSubmit rewrite + lastSavedPhase rising-edge useEffect (STAB-01 + STAB-03)</name>
  <read_first>
    - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx (lines 76-88 — handleSubmit bug; 81-83 — sessionCompleted dispatch bug; full file for handler context)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 7 (target handlers + useEffect — verbatim excerpts)
    - .planning/phases/01-stabilize/01-RESEARCH.md § Pattern 2 (rising-edge mechanism)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
  </files>
  <action>
    Three sub-edits — saga dispatch path + final-phase completion + rising-edge advance. **Do NOT touch `handleContinue` — that is Task 2b.**

    **A. `buildSaveActionForPhase` helper (file-local) — STAB-01 saga dispatch:**
    Add a helper function (top of file, after imports) that returns the right action creator call for a given phase. It also strips the UI-namespace prefix from form keys so the API receives bare field names (per RESEARCH.md A7).
    - 1 → `saveCompany(stripPrefix(values, 'company.'))`
    - 2 → `saveEquipment(stripPrefix(values, 'equipment.'))`
    - 3 → `saveDrivers(stripPrefix(values, 'drivers.'))`
    - 4 → `saveCostAnalysis(stripPrefix(values, 'costAnalysis.'))`
    - 5 → `saveLanePreferences(stripPrefix(values, 'lanePreferences.'))`

    `stripPrefix`: pure helper, takes `(values: Record<string, unknown>, prefix: string)` and returns a new object with keys that start with `prefix` re-keyed without the prefix (drop keys that don't match). Type the return as `Record<string, unknown>`.

    **B. Replace `handleSubmit` (lines 76-88):**
    - If `currentPhase < TOTAL_PHASES`: `dispatch(buildSaveActionForPhase(currentPhase, formik.values))`.
    - Else (currentPhase === TOTAL_PHASES, i.e. Phase 6 Documents — no per-phase save action; documents auto-save during the phase): `dispatch(carrierPortalActions.completeOnboarding())`.
    - Wrap in `useCallback` with deps `[currentPhase, dispatch, formik.values]`.

    **C. Add rising-edge `useEffect` for phase advance — STAB-01 + STAB-03:**
    ```typescript
    const lastSavedPhase = useSelector(selectLastSavedPhase);
    useEffect(() => {
      if (lastSavedPhase === null) return;
      if (lastSavedPhase !== currentPhase) return;
      if (currentPhase < TOTAL_PHASES) {
        dispatch(carrierPortalActions.setCurrentPhase(currentPhase + 1));
        if (typeof window !== 'undefined') {
          const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
          window.scrollTo({ top: 0, behavior: prefersReducedMotion ? 'auto' : 'smooth' });
        }
      }
      // For currentPhase === TOTAL_PHASES, completeOnboardingSuccess (Plan 02) sets lastSavedPhase = 6
      // AND populates state.session.completedAt from the API payload. The page's EXISTING
      // render branch on session.completedAt swaps to PortalCompleteView — DO NOT remove that
      // branch (BLOCKER 2 transition gate). This effect simply clears the rising-edge signal.
      dispatch(carrierPortalActions.phaseAdvanceConsumed());
    }, [lastSavedPhase, currentPhase, dispatch]);
    ```

    **D. Remove `sessionCompleted(...)` dispatches — STAB-03:**
    Grep the file for `sessionCompleted(` and delete every call site (there should be only one, at the old line 81-83). The reducer stays in the slice (it's used to reduce server payloads), but the page must not dispatch it.

    **E. Verify `session.completedAt` render branch is intact — BLOCKER 2 gate:**
    Before committing, grep the page file: `grep -n 'session.completedAt\|session\\?\\.completedAt' src/features/carrier-portal/pages/CarrierPortalPage/index.tsx` MUST return at least one match — confirming the existing branch that renders `<PortalCompleteView />` when `session?.completedAt` is non-null is preserved by this task. If the match is zero (the branch was inadvertently removed or never existed), STOP and reinstate the branch using the pattern:
    ```tsx
    if (session?.completedAt) {
      return <PortalCompleteView ... />;
    }
    ```
    placed at the top of the render body before the question thread.

    Constraints:
    - `useSelector` and `useDispatch` from `store` (path alias).
    - `useCallback` for handlers with full dep arrays.
    - No `any`, no `as` (the migration-allowed `formik.errors[name] as string | undefined` is permitted only there — not in this task).
    - No `console.log`.
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -q "buildSaveActionForPhase\|saveCostAnalysis\b" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "lastSavedPhase" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "phaseAdvanceConsumed" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "completeOnboarding" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "completedAt" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -v '^[[:space:]]*//' src/features/carrier-portal/pages/CarrierPortalPage/index.tsx | grep -v "^[[:space:]]*\*" | grep -c "sessionCompleted(" | grep -q '^0$' && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `buildSaveActionForPhase` helper exists in the page file
    - `handleSubmit` dispatches saga actions (no longer calls `setCurrentPhase` directly)
    - Final phase dispatches `completeOnboarding()` (not `sessionCompleted`)
    - `useEffect` on `lastSavedPhase` exists with the rising-edge advance + `phaseAdvanceConsumed()` clear
    - No `sessionCompleted(` dispatch remains in the page file
    - `session.completedAt` render-branch is preserved (grep `'completedAt'` in the page file returns ≥1 match) — BLOCKER 2 gate
    - The `CarrierPortalPage.test.tsx` scaffold case `it.todo('renders PortalCompleteView when session.completedAt is non-null — STAB-03 transition gate')` is flipped to a real test asserting: when state has `session: { completedAt: '2026-...' }`, the rendered output contains `PortalCompleteView` (component name or its identifying text per UI-SPEC) — BLOCKER 2 gate test
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Saga dispatch path wired; completeOnboarding dispatched; rising-edge effect added; sessionCompleted dispatch removed; `session.completedAt` render branch preserved + tested (BLOCKER 2 gate).</done>
</task>

<task type="auto">
  <name>Task 2b: Validation surfacing — handleContinue snackbar + scroll-to-error + data-question-id audit (STAB-02)</name>
  <read_first>
    - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx (lines 188-198 — handleContinue bug; AFTER Task 2a edits are committed)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 7 (target handlers — verbatim excerpts)
    - .planning/phases/01-stabilize/01-RESEARCH.md § Pattern 3 (snackbar + scroll-to-error)
    - .planning/phases/01-stabilize/01-UI-SPEC.md (search for "Scroll-to-error" and "Validation feedback")
    - hussle-app-dispatch-ui/src/components/ConversationalForm/QuestionCard.tsx (verify line 28 — data-question-id attr)
    - hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx (verify line 36 — data-question-id attr)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/PhaseForm/index.tsx (verify question render paths expose data-question-id per RESEARCH.md Pitfall 3)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
  </files>
  <action>
    Single concern: surface validation failures visibly (snackbar + scroll-to-error) and honor reduced-motion. **Do NOT touch `handleSubmit` / `useEffect` / saga dispatch — those landed in Task 2a.**

    **A. Replace `handleContinue` (lines 188-198) — STAB-02:**
    Use the verbatim pattern from UI-SPEC § "Scroll-to-error" and PATTERNS.md § 7:
    - Run `formik.validateForm()`; set all touched.
    - If errors:
      - Call `enqueueSnackbar('Please answer the highlighted questions before continuing.', { variant: 'warning', anchorOrigin: { vertical: 'top', horizontal: 'center' }, autoHideDuration: 4000, preventDuplicate: true, key: 'phase-validation-error' })`.
      - Find first error id, query `document.querySelector` with `[data-question-id="..."]`.
      - Compute `prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches`.
      - Call `el.scrollIntoView({ behavior: prefersReducedMotion ? 'auto' : 'smooth', block: 'center' })`.
      - Focus the first input/textarea/`[role="button"]` inside via `el.querySelector('input, textarea, [role="button"]')` + `input.focus({ preventScroll: true })`.
    - Else: `await formik.submitForm()`.

    Wrap in `useCallback` with deps `[formik]` (or fully — `[formik.validateForm, formik.submitForm, formik.errors, ...]` per CLAUDE.md exhaustive-deps).

    Add `import { enqueueSnackbar } from 'notistack';` to the import block if not already present from Task 2a.

    **B. `data-question-id` coverage audit per RESEARCH.md Pitfall 3:**
    Grep `QuestionCard.tsx`, `SubQuestion.tsx`, and `PhaseForm.tsx` for `data-question-id`:
    ```
    grep -n 'data-question-id' hussle-app-dispatch-ui/src/components/ConversationalForm/QuestionCard.tsx
    grep -n 'data-question-id' hussle-app-dispatch-ui/src/components/ConversationalForm/SubQuestion.tsx
    grep -n 'data-question-id' hussle-app-dispatch-ui/src/features/carrier-portal/components/PhaseForm/index.tsx
    ```
    Both `QuestionCard.tsx:28` and `SubQuestion.tsx:36` are confirmed in RESEARCH.md. If a question type bypasses these wrappers (raw MUI input rendered directly inside PhaseForm), wrap the outer container with `data-question-id={question.id}` on the outermost `Box`/`div`. If the gap is non-trivial, document it in the SUMMARY for Plan 07 to address — do NOT block on it.

    Constraints:
    - `useCallback` for handler with full dep array.
    - No `any`, no `as`, no `!`.
    - No `console.log`.
    - Snackbar `preventDuplicate: true` + stable `key: 'phase-validation-error'` is REQUIRED so rapid-tap on Continue doesn't stack multiple toasts (W1 fix).
    - `prefers-reduced-motion` honored on the `scrollIntoView` call (W1 fix).
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -q "enqueueSnackbar" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "preventDuplicate" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "phase-validation-error" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "scrollIntoView" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "prefers-reduced-motion" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "data-question-id" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `handleContinue` calls `enqueueSnackbar` on validation failure with `preventDuplicate: true` and stable `key: 'phase-validation-error'`
    - `handleContinue` calls `scrollIntoView` on `[data-question-id="..."]` for first error
    - `prefers-reduced-motion` honored in the scroll call (`behavior: 'auto'` when reduced, `'smooth'` otherwise)
    - Focus moved to first input inside the error question (with `preventScroll: true`)
    - `data-question-id` audit complete — confirmed on `QuestionCard.tsx`, `SubQuestion.tsx`, and any raw render paths in `PhaseForm.tsx`. Any gaps documented in SUMMARY.
    - `CarrierPortalPage.test.tsx` STAB-02 case (`it.todo('Save & Continue fires snackbar + scrolls to first error when validation fails — STAB-02')`) flipped to passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>handleContinue surfaces validation failures via snackbar + scroll-to-error; reduced-motion honored; data-question-id audit complete; STAB-02 page test green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CostResultCard conditional render for Phase 4 (BLOCKER 3 — STAB-08 integration)</name>
  <read_first>
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx (PURE component from Plan 04 — accepts `inputs: CostInputs`, `firstName?: string`, `onContinue: () => void` as props; exports `CostInputs` interface)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/computeCostAnalysis.ts (for reference — the helper is used inside CostResultCard, not at the call site)
    - hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx (final state after Tasks 1, 2a, 2b)
    - hussle-app-dispatch-ui/src/features/carrier-portal/questions/costAnalysisQuestions.ts (6 field IDs to extract)
    - .planning/phases/01-stabilize/01-UI-SPEC.md § "Phase 4 / CostResultCard" (when to swap from question thread to result card)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 12 (CostResultCard rendering context)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx
  </files>
  <action>
    Wire `CostResultCard` into `CarrierPortalPage` so that during Phase 4, once all 6 cost-analysis fields are populated in formik state, the question thread is replaced by `<CostResultCard ... />`. CTA dispatches the existing Phase 4 save chain.

    **A. Import the component and its types:**
    ```typescript
    import CostResultCard from '../../components/CostResultCard';
    import type { CostInputs } from '../../components/CostResultCard';
    ```
    (Or whatever the actual export form is — match Plan 04's Task 2 export shape: default export of the component, named export of `CostInputs` and `CostResultCardProps` interfaces.)

    **B. Extract cost inputs from formik state:**
    Read the 6 cost-analysis fields from `formik.values` using the prefixed IDs from `costAnalysisQuestions.ts`:
    ```typescript
    const costFields = [
      'costAnalysis.truckPayment',
      'costAnalysis.insuranceCost',
      'costAnalysis.fuelCostPerGallon',
      'costAnalysis.milesPerGallon',
      'costAnalysis.maintenanceMonthlyCost',
      'costAnalysis.otherMonthlyCosts',
    ] as const;
    const allCostFieldsPopulated = costFields.every((id) => {
      const v = formik.values[id];
      return typeof v === 'number' || (typeof v === 'string' && v.trim() !== '');
    });
    const costInputs: CostInputs = {
      truckPayment: Number(formik.values['costAnalysis.truckPayment'] ?? 0),
      insuranceCost: Number(formik.values['costAnalysis.insuranceCost'] ?? 0),
      fuelCostPerGallon: Number(formik.values['costAnalysis.fuelCostPerGallon'] ?? 0),
      milesPerGallon: Number(formik.values['costAnalysis.milesPerGallon'] ?? 1),
      maintenanceMonthlyCost: Number(formik.values['costAnalysis.maintenanceMonthlyCost'] ?? 0),
      otherMonthlyCosts: Number(formik.values['costAnalysis.otherMonthlyCosts'] ?? 0),
      ownsOutright: Boolean(formik.values['costAnalysis.ownsOutright']),
    };
    ```
    Use `Number()` casts (NOT `as number`) — values may come from text inputs as strings. Guard `milesPerGallon` against 0 (default 1) to avoid divide-by-zero in `computeCostAnalysis`. No `any`, no `as` per CLAUDE.md — the bracket-access yields `unknown`, narrow via `Number()` / `Boolean()` / typeof checks.

    **C. Conditional render in the page body:**
    In the JSX where the question thread for `currentPhase === 4` is rendered, add a branch:
    ```tsx
    {currentPhase === 4 && allCostFieldsPopulated ? (
      <CostResultCard
        inputs={costInputs}
        firstName={session?.carrier?.firstName ?? carrier?.firstName ?? undefined}
        onContinue={handleSubmit}
      />
    ) : (
      <PhaseForm ... />
    )}
    ```
    The `onContinue` prop is wired to the existing `handleSubmit` from Task 2a — which for `currentPhase === 4` dispatches `buildSaveActionForPhase(4, formik.values) → saveCostAnalysis(...)`. NO new dispatch logic is needed; we reuse the existing path. Locate the actual firstName source by reading the page's existing session/carrier destructure — adjust the optional chain to whatever shape the page has access to. If `firstName` is unavailable, pass `undefined` (CostResultCard handles the no-name fallback per Plan 04 Task 2).

    Preserve the `session.completedAt` render branch (BLOCKER 2 gate) — it must be evaluated BEFORE the Phase-4 branch (PortalCompleteView short-circuits everything else).

    **D. Component test for BLOCKER 3:**
    Update `CarrierPortalPage.test.tsx` — flip the `it.todo('Phase 4 thread is replaced by CostResultCard once all 6 cost-analysis fields are populated, and CTA dispatches saveCostAnalysis — STAB-08 wiring')` placeholder to a real test:
    - Render the page with seeded state: `currentPhase: 4`, formik values with all 6 cost fields populated.
    - Assert `CostResultCard` is in the DOM (query by a stable text/role from CostResultCard — e.g., `getByText(/break-even/i)` or by a `data-testid="cost-result-card"` added to the component's outer Box during Plan 04 Task 2 if needed).
    - Assert `PhaseForm` is NOT in the DOM at this phase.
    - Simulate clicking the Continue button on CostResultCard; assert `saveCostAnalysis` action was dispatched (use a mock store + `store.getActions()` from `redux-mock-store`, OR use `expectSaga` style assertion — match the pattern used elsewhere in the test file).
    - Render WITH only 5 of the 6 fields populated; assert `CostResultCard` is NOT in the DOM, `PhaseForm` IS.

    Constraints:
    - No `any`, no `as`, no `!`.
    - The `useMemo` wrapper around `costInputs` is optional; recompute on every render is fine for a 6-field object.
    - Do NOT import `useSelector(selectAnswers)` for the extraction — formik state is the source of truth here (Plan 04 BLOCKER 4 contract).
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -q "CostResultCard" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "allCostFieldsPopulated\|costInputs" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && grep -q "currentPhase === 4" src/features/carrier-portal/pages/CarrierPortalPage/index.tsx && npx tsc --noEmit -p tsconfig.app.json && npx jest --testPathPattern='CarrierPortalPage\.test\.tsx' --silent 2>&1 | grep -E "(CostResultCard|cost-analysis|Phase 4)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `CarrierPortalPage/index.tsx` imports `CostResultCard` and its `CostInputs` type
    - Page extracts the 6 cost-analysis fields from `formik.values` (using `Number()` casts, no `as`)
    - Page computes `allCostFieldsPopulated` boolean
    - When `currentPhase === 4 && allCostFieldsPopulated`, JSX renders `<CostResultCard inputs={...} firstName={...} onContinue={handleSubmit} />` in place of `<PhaseForm />`
    - The `session.completedAt` short-circuit branch (BLOCKER 2 gate) is evaluated BEFORE the Phase-4 branch
    - `CarrierPortalPage.test.tsx` STAB-08 wiring case flipped from `it.todo` to passing: asserts CostResultCard appears when 6 fields populated, PhaseForm appears when <6 populated, and Continue click dispatches `saveCostAnalysis`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CostResultCard wired into CarrierPortalPage with conditional Phase-4 render; CTA dispatches saveCostAnalysis via existing handleSubmit path; STAB-08 integration test green (BLOCKER 3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Page dispatches saga actions POSTing to `/carrier-portal/{phase}` with `Authorization: Bearer <token>` (existing path — unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (XSS) | `enqueueSnackbar` message | mitigate | Snackbar message is a literal string — never user-input echo (UI-SPEC § Validation feedback). |
| T-06-02 | Information disclosure | Validation error keys leaking via `data-question-id` | accept | Question IDs are static schema constants (`'company.name'`, `'costAnalysis.truckPayment'`), not user data — no PII surface. |
| T-06-03 | Tampering | Saga payload prefix stripping | mitigate | `stripPrefix` is a pure helper; API validators (`validateRequest` middleware) reject any unexpected fields per RESEARCH.md § Security Domain row V5. |
| T-06-04 | Information disclosure | CostResultCard firstName interpolation | mitigate | `firstName` sourced from server-validated session/carrier data; MUI Typography escapes by default. RESEARCH.md § Security Domain. |

No HIGH-severity threats — no new auth path, no new endpoint, no new PII handling.
</threat_model>

<verification>
- `cd hussle-app-dispatch-ui && npm run check-ts` exits 0
- `cd hussle-app-dispatch-ui && npm run lint -- --quiet` clean on modified files
- `cd hussle-app-dispatch-ui && npm test -- --testPathPattern=carrier-portal --silent` shows CarrierPortalPage tests green for STAB-01/02/03/08 expectations + BLOCKER 2 (`PortalCompleteView` transition) + BLOCKER 3 (CostResultCard conditional render)
- `grep -c sessionCompleted hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx` returns 0 (or only in comments)
- `grep -c enqueueSnackbar hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx` returns ≥ 1
- `grep -c CostResultCard hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx` returns ≥ 2 (import + JSX render site)
- `grep -c completedAt hussle-app-dispatch-ui/src/features/carrier-portal/pages/CarrierPortalPage/index.tsx` returns ≥ 1 (BLOCKER 2 render-branch preserved)
</verification>

<success_criteria>
- STAB-01: Save & Continue dispatches saga; advance via useEffect rising-edge on `lastSavedPhase`
- STAB-02: Validation snackbar fires (with `preventDuplicate`) + scroll-to-first-error works; reduced-motion honored
- STAB-03: Final phase dispatches `completeOnboarding()`; no page-side `sessionCompleted` dispatch; `session.completedAt` render-branch swaps to `PortalCompleteView` (BLOCKER 2 gate, verified by test)
- STAB-04 (page side): `CarrierPortalPage` imports `PHASE_LABELS` / `TOTAL_PHASES` / `QUESTIONS_BY_PHASE` from `constants.ts`
- STAB-08 (integration): CostResultCard renders conditionally on Phase 4 when all 6 fields populated; CTA dispatches `saveCostAnalysis` via existing handleSubmit path (BLOCKER 3 wiring)
- Page-level test scaffold flips green for all four cases (STAB-01/02/03/08 + BLOCKER 2 + BLOCKER 3)
- TypeScript compiles; lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-06-SUMMARY.md`.
</output>
</content>
</invoke>