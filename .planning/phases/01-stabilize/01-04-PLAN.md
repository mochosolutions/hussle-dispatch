---
phase: 01-stabilize
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - hussle-app-dispatch-ui/src/features/carrier-portal/questions/costAnalysisQuestions.ts
  - hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx
  - hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts
autonomous: true
requirements: [STAB-06, STAB-08]

must_haves:
  truths:
    - "`costAnalysisQuestions.ts` exports an array of `QuestionDefinition` containing exactly 6 questions matching API validator field names: `costAnalysis.truckPayment`, `costAnalysis.insuranceCost`, `costAnalysis.fuelCostPerGallon`, `costAnalysis.milesPerGallon`, `costAnalysis.maintenanceMonthlyCost`, `costAnalysis.otherMonthlyCosts`"
    - "Each question has `inputType: 'presetTiles'` and a `presets: PresetOption[]` array (per RESEARCH.md § Code Examples — Cost analysis questions)"
    - "Q1 (truckPayment) has a `subQuestions` array with one yesNo sub-question `costAnalysis.ownsOutright` (`borderColor: 'green'`) — when true, `CostResultCard` treats truckPayment as 0"
    - "`CostResultCard/index.tsx` rebuilt — renders break-even RPM and minimum rate per UI-SPEC § Component Inventory A using the formula in RESEARCH.md § Cost Analysis Calculations (ASSUMED_MONTHLY_MILES = 8000, MIN_PROFIT_MARGIN = 0.25)"
    - "`CostResultCard` is a PURE component: it accepts `inputs: CostInputs`, `firstName?: string`, `onContinue: () => void` as PROPS — it does NOT call `useSelector` and has no Redux dependency. Data extraction happens at the call site in `CarrierPortalPage` (Plan 06) — BLOCKER 4 disambiguation."
    - "`CostResultCard` respects `prefers-reduced-motion: reduce` — renders final values immediately, skipping RAF count-up animation (UI-SPEC § Animations + RESEARCH.md Pitfall 6)"
    - "`CostResultCard` uses MUI `sx` only (no styled-components, no inline `style={{}}`); typography uses Table 2 isolation-zone values (24/30/48/60, weight 700) ONLY inside this component — never reused elsewhere"
    - "`questionSchema.ts` exports a `PresetOption` interface (`{ value: number; label: string }`) and adds optional `presets?: PresetOption[]` field to `QuestionDefinition` so `costAnalysisQuestions.ts` and `Plan 07`'s InputRenderer can consume it"
  artifacts:
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/questions/costAnalysisQuestions.ts
      provides: 6-question presetTiles schema for Phase 4
      contains: "truckPayment"
      exports: ["costAnalysisQuestions"]
    - path: hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx
      provides: dark-bg full-viewport result card with break-even + minimum rate
      contains: "breakEvenRpm"
      min_lines: 80
    - path: hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts
      provides: PresetOption interface + presets?: PresetOption[] field on QuestionDefinition
      contains: "PresetOption"
  key_links:
    - from: hussle-app-dispatch-ui/src/features/carrier-portal/questions/costAnalysisQuestions.ts
      to: hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts
      via: "import type { QuestionDefinition, PresetOption } from 'components/ConversationalForm/questionSchema'"
      pattern: "import type.*PresetOption.*questionSchema"
    - from: hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx
      to: (callers — Plan 06 CarrierPortalPage extraction site)
      via: "Accepts `inputs: CostInputs` as a prop. The caller (CarrierPortalPage in Plan 06) extracts the 6 cost-analysis fields from formik state (`formik.values['costAnalysis.truckPayment']`, etc.) and passes them as the `inputs` prop. CostResultCard does NOT call `useSelector` — it is a pure presentational component. BLOCKER 4 disambiguation."
      pattern: "interface CostResultCardProps"

threat_model:
  trust_boundaries: []
  notes: "No new endpoints, no auth path. Cost calculations are pure UI-side JS (RESEARCH.md § Architectural Responsibility Map). No PII. Question labels are static TS strings — MUI escapes by default. No XSS surface."
---

<objective>
Build the missing Phase 4 (Cost Analysis) UI pieces: the 6-question presetTiles schema (STAB-06) and the rebuilt full-viewport `CostResultCard` (STAB-08). Extend `QuestionDefinition` with a `presets?: PresetOption[]` field so the schema can carry tile data inline (researcher recommendation — RESEARCH.md Pitfall 5: type as a concrete `PresetOption` interface, not `any`).

This plan is independent of Plan 03 (constants) and Plan 05 (lane preferences) — different files, different concerns — so it runs in Wave 2 in parallel. The cost analysis schema gets wired into `QUESTIONS_BY_PHASE` in Plan 06 (page wiring); the result card gets rendered after the user finishes Phase 4 (Plan 06 also handles that hook-up). `CostResultCard` does NOT need to be in the InputRenderer switch — it is rendered separately from the question thread after submit.

**BLOCKER 4 disambiguation (props vs Redux):** `CostResultCard` is a pure presentational component. It accepts `inputs: CostInputs` as a prop and does NOT call `useSelector(selectAnswers)`. Data extraction happens at the call site (Plan 06, in `CarrierPortalPage`) where the 6 fields are read from formik state and passed as the `inputs` prop. This keeps the component pure, testable, and Redux-free.

Purpose: STAB-06 (schema) + STAB-08 (result card) — both are net-new UI deliverables; the result card was deleted (git status: `D CostResultCard/index.tsx`) and must be rebuilt from the legacy design + UI-SPEC.

Output: 3 modified/new files. Component tests scaffolded in Plan 01 flip green for `CostResultCard.test.tsx` (the formula + reduced-motion expectations).

`[ASSUMED]` baseline constants (RESEARCH.md A1): `ASSUMED_MONTHLY_MILES = 8000`, `MIN_PROFIT_MARGIN = 0.25`. These are reverse-engineered from the sample numbers in `.planning-legacy/carrier-onboarding/designs/interview-cost-analysis.md` (break-even $1.94, minimum $2.44). The executor SHOULD verbally confirm with the user before completing this plan — but defaults are locked if user opts out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-stabilize/01-RESEARCH.md
@.planning/phases/01-stabilize/01-PATTERNS.md
@.planning/phases/01-stabilize/01-UI-SPEC.md
@CLAUDE.md
@hussle-app-dispatch-ui/src/features/carrier-portal/questions/companyQuestions.ts
@hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts
@hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/index.tsx
@hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx
@.planning-legacy/carrier-onboarding/designs/interview-cost-analysis.md

<interfaces>
<!-- Existing exports today -->

From companyQuestions.ts (analog for question schema shape — see PATTERNS.md § 4):
```typescript
import type { QuestionDefinition } from 'components/ConversationalForm/questionSchema';
const PHASE = 1;
const TAX_CLASSIFICATION_OPTIONS = [/*...*/];
export const companyQuestions: QuestionDefinition[] = [
  { id: 'company.name', phase: PHASE, inputType: 'text', label: 'Business name', required: true },
  // ...
];
```

From questionSchema.ts (TODAY): exports `QuestionDefinition` discriminated union over `inputType`. Variant for `'presetTiles'` exists in name but has no `presets` field — placeholder rendered by InputRenderer at lines 293-307. `PresetOption` is currently declared internally inside `PresetTileSelector/index.tsx` (RESEARCH.md Pitfall 5) — promote it to a shared export here.

From PresetTileSelector/index.tsx (TODAY, 73 lines): props shape is `{ presets: PresetOption[]; value: number | null; onChange: (value: number | null) => void }`. Internal `PresetOption` interface to promote.

API validator field names (`hussle-app-dispatch-api/src/carrier-portal/validators/costAnalysisValidator.ts:5-22`) — UI field IDs must map to these exact API body keys: `truckPayment`, `insuranceCost`, `fuelCostPerGallon`, `milesPerGallon`, `maintenanceMonthlyCost`, `otherMonthlyCosts`. The UI field IDs use a `costAnalysis.` prefix for namespacing; the saga payload mapping (Plan 06) strips the prefix.

UI-SPEC § Typography Table 2 (CostResultCard isolation zone — ONLY use inside CostResultCard):
- Expense tile value: 24/700
- Result heading: 30 (xs: 24)/700
- Break-even value: 48 (xs: 36)/700
- Minimum rate value: 60 (xs: 44)/700, `color: 'success.light'`

UI-SPEC § Color:
- `CostResultCard` uses `bgcolor: '#0F172A'` (slate-900) full-viewport
- Text: `color: 'common.white'` for heading/values; `color: 'grey.300'` for body subtext; `color: 'grey.400'` for overlines/disclaimer
- Minimum-rate value: `color: 'success.light'` (only place green is used as a primary display value)
- CTA: filled `color: 'success.main'` with white text, full-width on mobile, 280px max on desktop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend questionSchema and create costAnalysisQuestions schema</name>
  <read_first>
    - hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts (to add PresetOption + presets? field)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/PresetTileSelector/index.tsx (internal PresetOption shape to promote)
    - hussle-app-dispatch-ui/src/features/carrier-portal/questions/companyQuestions.ts (analog — verbatim schema shape)
    - hussle-app-dispatch-api/src/carrier-portal/validators/costAnalysisValidator.ts (API field-name truth)
    - .planning-legacy/carrier-onboarding/designs/interview-cost-analysis.md (label + hint copy)
    - .planning/phases/01-stabilize/01-RESEARCH.md § Code Examples — Cost analysis questions (preset values reference)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 4 (analog excerpt + sub-question pattern)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts,
    hussle-app-dispatch-ui/src/features/carrier-portal/questions/costAnalysisQuestions.ts
  </files>
  <action>
    Two coordinated changes.

    **1. `questionSchema.ts` (MODIFY):**
    - Export a new `PresetOption` interface: `export interface PresetOption { value: number; label: string }`.
    - Add an optional `presets?: PresetOption[]` field to the `QuestionDefinition` union variant for `inputType: 'presetTiles'`. If `QuestionDefinition` is a discriminated union, add the field on the `'presetTiles'` discriminant only; if it's a single interface with `inputType` literal, add as optional top-level field but type as `PresetOption[] | undefined`. Verify the type checks against the existing `PresetTileSelector` props shape — if PresetTileSelector internally declares its own `PresetOption`, remove the duplicate and import from `questionSchema.ts` (RESEARCH.md Pitfall 5).

    **2. `features/carrier-portal/questions/costAnalysisQuestions.ts` (NEW):**
    Mirror `companyQuestions.ts` structure (PATTERNS.md § 4). Phase constant = 4. Build six questions matching the API validator field names exactly (per RESEARCH.md A7 — saga payload mapping in Plan 06 strips the `costAnalysis.` UI prefix to send `truckPayment` etc. as the API body keys).

    Preset values (RESEARCH.md § Code Examples — reverse-engineered from legacy design):
    - `truckPayment`: $800 / $1,200 / $1,500 / $2,000
    - `insuranceCost`: $800 / $1,200 / $1,500 / $2,000
    - `fuelCostPerGallon`: $3.50 / $3.75 / $4.00 / $4.25
    - `milesPerGallon`: 5.5 / 6.0 / 6.5 / 7.0
    - `maintenanceMonthlyCost`: $300 / $500 / $800 / $1,200
    - `otherMonthlyCosts`: $100 / $250 / $500

    Q1 (`costAnalysis.truckPayment`) carries a `subQuestions` array with one entry — yesNo sub-question `costAnalysis.ownsOutright` with `borderColor: 'green'` (optional follow-up per UI-SPEC § Color border-color semantics).

    Labels and hints — use direct, calm, second-person copy per UI-SPEC § Copywriting (e.g., `label: "What's your monthly truck payment?"`, `hint: 'Pre-populated from Equipment phase if entered there.'`). Source the exact phrasing from `.planning-legacy/carrier-onboarding/designs/interview-cost-analysis.md`. If a label is ambiguous in the legacy source, follow the existing pattern in `companyQuestions.ts`.

    Each question:
    - `id: 'costAnalysis.<fieldName>'`
    - `phase: 4`
    - `inputType: 'presetTiles'`
    - `presets: <CONST>`
    - `required: true` for Q1–Q5; Q6 (`otherMonthlyCosts`) is `required: false`

    Constraints:
    - Use `interface` (or the existing schema shape — match what `companyQuestions.ts` uses).
    - Use `import type { QuestionDefinition, PresetOption } from 'components/ConversationalForm/questionSchema';`.
    - No `any`, no `as`, no `!`.

    Flag in a top-of-file comment: `// [ASSUMED] Preset values reverse-engineered from .planning-legacy/carrier-onboarding/designs/interview-cost-analysis.md (RESEARCH.md A1). Confirm with user before phase ships.`
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && grep -q "export interface PresetOption" src/components/ConversationalForm/questionSchema.ts && grep -q "presets" src/components/ConversationalForm/questionSchema.ts && test -f src/features/carrier-portal/questions/costAnalysisQuestions.ts && grep -c "costAnalysis\." src/features/carrier-portal/questions/costAnalysisQuestions.ts | awk '$1 >= 6 {exit 0} {exit 1}' && grep -q "ownsOutright" src/features/carrier-portal/questions/costAnalysisQuestions.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `questionSchema.ts` exports `PresetOption` and the `presetTiles` variant accepts `presets?: PresetOption[]`
    - `costAnalysisQuestions.ts` exists and exports `costAnalysisQuestions: QuestionDefinition[]` (or `readonly QuestionDefinition[]`)
    - Array contains exactly 6 questions with IDs `costAnalysis.truckPayment` / `.insuranceCost` / `.fuelCostPerGallon` / `.milesPerGallon` / `.maintenanceMonthlyCost` / `.otherMonthlyCosts`
    - Q1 has a `subQuestions` array containing `costAnalysis.ownsOutright` with `borderColor: 'green'`
    - `npx tsc --noEmit` exits 0
    - `[ASSUMED]` comment present at top of file
  </acceptance_criteria>
  <done>questionSchema extended; costAnalysisQuestions schema created with 6 questions + sub-question; TS compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild CostResultCard component (PURE — props-only, no Redux; full-viewport, dark, RAF count-up, reduced-motion gate)</name>
  <read_first>
    - .planning/phases/01-stabilize/01-UI-SPEC.md (full file — typography Table 2, color, animations, copy)
    - .planning/phases/01-stabilize/01-RESEARCH.md § Cost Analysis Calculations (formula + constants)
    - .planning/phases/01-stabilize/01-PATTERNS.md § 12 (CostResultCard analog + animation gate)
    - .planning-legacy/carrier-onboarding/designs/interview-cost-analysis.md (legacy visual reference)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/StateGrid/index.tsx (sibling sx pattern — RESEARCH.md Pattern Map closest analog)
    - hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/CostResultCard.test.tsx (scaffold from Plan 01 — expectations to satisfy)
  </read_first>
  <files>
    hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx
  </files>
  <action>
    Rebuild the deleted `CostResultCard` per UI-SPEC § Component Inventory A. **BLOCKER 4: the component is PURE — props-only, no `useSelector`, no Redux import. Data extraction happens at the call site in Plan 06.**

    **Props (PURE — no Redux):**
    ```typescript
    interface CostInputs {
      truckPayment: number;
      insuranceCost: number;
      fuelCostPerGallon: number;
      milesPerGallon: number;
      maintenanceMonthlyCost: number;
      otherMonthlyCosts: number;
      ownsOutright?: boolean;
    }

    interface CostResultCardProps {
      inputs: CostInputs;
      firstName?: string;        // for the heading "Here's your real cost picture, {firstName}."
      onContinue: () => void;
    }
    ```

    Export `CostInputs` and `CostResultCardProps` from this file so the Plan 06 caller can import the types for the prop-passing site.

    **DO NOT import `react-redux`, `useSelector`, or `selectAnswers` in this file.** This is enforced by the test asserting that no `useSelector` reference exists in the component file (BLOCKER 4 disambiguation).

    **Calculation helper** (export so Plan 06 can also import for tests, and to keep the component file lean):
    Place the `computeCostAnalysis` function in a sibling helper file `features/carrier-portal/components/CostResultCard/computeCostAnalysis.ts` (separate concerns; pure function easy to unit-test). Reference RESEARCH.md § Cost Analysis Calculations for the formula:
    ```
    truckPayment = ownsOutright ? 0 : inputs.truckPayment
    monthlyFixed = truckPayment + insurance + maintenance + other
    fuelCostPerMile = fuelCostPerGallon / milesPerGallon
    monthlyVariable = fuelCostPerMile * ASSUMED_MONTHLY_MILES
    totalMonthlyExpenses = monthlyFixed + monthlyVariable
    breakEvenRpm = totalMonthlyExpenses / ASSUMED_MONTHLY_MILES
    minimumRatePerMile = breakEvenRpm * (1 + MIN_PROFIT_MARGIN)
    ```
    Constants at the top of `computeCostAnalysis.ts`:
    `export const ASSUMED_MONTHLY_MILES = 8000;` and `export const MIN_PROFIT_MARGIN = 0.25;` plus a `// [ASSUMED]` comment naming RESEARCH.md A1.

    **Visual contract (UI-SPEC § Component Inventory A + Color + Typography Table 2):**
    - Outer container: full-viewport `Box` with `bgcolor: '#0F172A'`, `minHeight: '100vh'`, content inner `maxWidth: 700`, centered, `px: { xs: 3, md: 4 }`, `pt: 6`.
    - Heading: `sx={{ fontSize: { xs: 24, md: 30 }, fontWeight: 700, lineHeight: 1.2, color: 'common.white', mb: 4 }}` with text `Here's your real cost picture, {firstName}.` (if no firstName, drop the comma + name and use `Here's your real cost picture.`).
    - Three expense tiles (Monthly Fixed, Monthly Variable, Fuel-per-mile): 3-column responsive grid (`Grid container spacing={2}` or `Stack direction={{ xs: 'column', md: 'row' }} spacing={2}`); each tile shows label overline (12/600/uppercase, `color: 'grey.400'`) + value `24/700` `color: 'common.white'`. Values formatted with USD (e.g., `$1,200`, `$0.62`).
    - Break-even card: large value `sx={{ fontSize: { xs: 36, md: 48 }, fontWeight: 700, lineHeight: 1, color: 'common.white' }}` formatted as `$1.94` (2-decimal); overline `BREAK-EVEN RATE PER MILE` `color: 'grey.400'` 12/600; one-line description `color: 'grey.300'` 14/400.
    - Minimum rate card: even larger value `sx={{ fontSize: { xs: 44, md: 60 }, fontWeight: 700, lineHeight: 1, color: 'success.light' }}`; overline `MINIMUM TO BOOK` `color: 'grey.400'` 12/600; description `color: 'grey.300'` 14/400.
    - Disclaimer (`color: 'grey.400'` 12/400) at bottom: `Based on ${ASSUMED_MONTHLY_MILES.toLocaleString()} miles/month and a ${(MIN_PROFIT_MARGIN * 100).toFixed(0)}% profit margin.`.
    - CTA button: `<Button variant="contained" color="success" fullWidth sx={{ maxWidth: { md: 280 }, mt: 6 }}>Continue</Button>` calling `onContinue`. Button label per UI-SPEC § Copywriting (use the literal label from the spec).

    **Animation (UI-SPEC § Animations + RESEARCH.md Pitfall 6):**
    - On mount, check `window.matchMedia('(prefers-reduced-motion: reduce)').matches`.
    - If `true`: render final values immediately (`displayBreakEven = breakEvenRpm`, `displayMinimum = minimumRatePerMile`). NO `requestAnimationFrame`.
    - If `false`: use `requestAnimationFrame` count-up loop over ~1500ms from 0 to the final value, easing `easeOutCubic` or linear (UI-SPEC does not mandate a specific easing — use `t => 1 - Math.pow(1 - t, 3)`). Apply to BOTH break-even and minimum values (synchronously).
    - Clean up RAF on unmount (`cancelAnimationFrame`).
    - **Do NOT import `framer-motion`** — UI-SPEC § Animations explicitly says use RAF. RESEARCH.md § Don't Hand-Roll confirms.

    **Number formatting:**
    Use `new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2 })` for the per-mile values; `new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 })` for whole-dollar expense tile values. During count-up, use the same formatter on each frame's interpolated number.

    Constraints:
    - MUI `sx` only — no styled-components, no inline `style={{}}`, no Tailwind.
    - No `console.log`; no `any`; no `as`; no `!`.
    - **No `useSelector`, no Redux imports** (BLOCKER 4) — enforced via grep gate in `<verify>` below.
    - Use semantic Typography helpers where they fit (`Meta` for overlines, `BodyMuted` for grey body), but raw `Typography` with `sx={{ fontSize, fontWeight }}` IS allowed for Table-2 isolation-zone display values (per UI-SPEC § Typography Table 2 + PATTERNS.md § Shared Patterns — Typography helpers).
    - Component file should be ~80–150 lines. If it gets larger, extract sub-components.

    Update the test file `CostResultCard.test.tsx` from Plan 01 (currently all `it.todo`): flip the three `it.todo` cases to real tests asserting (1) renders break-even and minimum from given inputs, (2) reduced-motion renders final values immediately (mock `window.matchMedia` to return `matches: true`), (3) RAF mode starts at 0 and reaches final value (use jest fake timers or assert by waiting for last RAF). Render the component with raw `inputs` props — NO Redux Provider needed (component is pure).
  </action>
  <verify>
    <automated>cd hussle-app-dispatch-ui && test -f src/features/carrier-portal/components/CostResultCard/index.tsx && test -f src/features/carrier-portal/components/CostResultCard/computeCostAnalysis.ts && grep -q "ASSUMED_MONTHLY_MILES" src/features/carrier-portal/components/CostResultCard/computeCostAnalysis.ts && grep -q "prefers-reduced-motion" src/features/carrier-portal/components/CostResultCard/index.tsx && grep -v '^[[:space:]]*//' src/features/carrier-portal/components/CostResultCard/index.tsx | grep -v "^[[:space:]]*\*" | grep -c "framer-motion" | grep -q '^0$' && grep -v '^[[:space:]]*//' src/features/carrier-portal/components/CostResultCard/index.tsx | grep -v "^[[:space:]]*\*" | grep -c "useSelector\|react-redux" | grep -q '^0$' && npx tsc --noEmit -p tsconfig.app.json && npx jest --testPathPattern='CostResultCard\.test\.tsx' --silent 2>&1 | grep -E '(passed|failed)' | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `CostResultCard/index.tsx` exists with the props shape above
    - `CostInputs` and `CostResultCardProps` interfaces are exported so Plan 06 can import them
    - Component does NOT import `useSelector` or `react-redux` (BLOCKER 4 grep gate)
    - `computeCostAnalysis.ts` exists, exports `ASSUMED_MONTHLY_MILES`, `MIN_PROFIT_MARGIN`, and `computeCostAnalysis` pure function
    - Component does NOT import `framer-motion` (grep gate)
    - `prefers-reduced-motion` check present in the component
    - Dark background `#0F172A`, MUI `sx` only (no `styled-components`, no inline `style={{}}` — grep gate optional but recommended in human review)
    - `CostResultCard.test.tsx` flips from all `it.todo` to ≥3 passing assertions (or 2 passing + 1 RAF behavior that's hard to test in jsdom — mark that one `it.skip` with a comment). Tests pass `inputs` directly as a prop — no Redux Provider required.
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CostResultCard rebuilt as pure component (no Redux); computeCostAnalysis extracted; reduced-motion handled; test scaffold flipped green; STAB-08 satisfied (subject to user confirmation on `[ASSUMED]` constants).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | All-UI work. No request handling, no auth path, no PII persisted, no new API surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (XSS) | CostResultCard text rendering | mitigate | MUI Typography escapes by default. Heading interpolates `firstName` from carrier data — `firstName` is server-validated. RESEARCH.md § Security Domain confirms not a Phase 1 risk. |
| T-04-02 | Information disclosure | Cost calculation results | accept | Output is visible only to the carrier in their own portal session. No cross-tenant leakage path. |

No HIGH-severity threats.
</threat_model>

<verification>
- `cd hussle-app-dispatch-ui && npm run check-ts` exits 0
- `cd hussle-app-dispatch-ui && npm run lint -- --quiet` clean on modified files
- `cd hussle-app-dispatch-ui && npm test -- --testPathPattern='(costAnalysisQuestions|CostResultCard)' --silent` runs to completion; CostResultCard tests show ≥3 passing
- `grep -c 'costAnalysis\.' hussle-app-dispatch-ui/src/features/carrier-portal/questions/costAnalysisQuestions.ts` returns ≥ 7 (6 question IDs + ownsOutright sub-question)
- `grep -q 'PresetOption' hussle-app-dispatch-ui/src/components/ConversationalForm/questionSchema.ts`
- `grep -c 'useSelector\|react-redux' hussle-app-dispatch-ui/src/features/carrier-portal/components/CostResultCard/index.tsx` returns 0 (BLOCKER 4 — component is pure)
</verification>

<success_criteria>
- STAB-06: `costAnalysisQuestions.ts` exists with 6 presetTiles questions matching API validator field names + ownsOutright sub-question
- STAB-08: `CostResultCard` rebuilt per UI-SPEC § Component Inventory A as a PURE component (props-only, no Redux); RAF count-up with reduced-motion gate; isolation-zone typography scoped exclusively inside the component
- `questionSchema.ts` exports `PresetOption` + `presets?:` field for Plan 07's InputRenderer to consume
- TypeScript compiles; lint passes; component test scaffold flipped green
- `[ASSUMED]` constants flagged for user confirmation during execution
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-04-SUMMARY.md`.
</output>
</content>
</invoke>