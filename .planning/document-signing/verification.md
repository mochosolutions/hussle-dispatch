# document-signing — Verification Report

> Generated by /feature-verify on 2026-05-15 (re-run, replaces prior VER-01)
> Workspace type: feature (from /build)
> Scope: 19 must_haves blocks across 21 stories (US-01..US-15, INT-01, VER-01, FIX-01..FIX-04)
> Methodology: file/grep evidence per truth/artifact/key_link entry. Architecture flipped mid-flight from templates-as-code to templates-as-data (FIX-01..FIX-04, 2026-05-15) — superseded entries called out explicitly.

## Summary
- Total must_haves entries checked: ~70 (truths + artifacts + key_links across 19 blocks)
- VERIFIED: 56
- VERIFIED-AS-SUPERSEDED: 7 (intentional regressions from FIX cycle)
- FAILED (BLOCKER): 0
- UNCERTAIN (WARNING): 7

**Verdict:** SHIP

---

## VERIFIED (56)

### US-01 — signature port + types
- **US-01 / artifact / `src/shared/signatures/types.ts`** — present, 92 lines, exports `CreateSubmissionInput`, `SubmissionRef`, 5-variant `SubmissionStatus` discriminated union, `SignedArtifacts`, `SignatureService` (5 methods incl. `refreshEmbedUrl` added by US-11) at `hussle-app-dispatch-api/src/shared/signatures/types.ts:1-91`.
- **US-01 / artifact / `signatureProviderPort.ts`** — present, 14 lines, 5-method interface (incl. `refreshEmbedUrl`) at `hussle-app-dispatch-api/src/shared/signatures/signatureProviderPort.ts:8-14`.
- **US-01 / truth / SubmissionStatus discriminated union** — variants are individual `interface` types unioned into `SubmissionStatus`, all carry `providerSubmissionId` at `types.ts:26-60`.
- **US-01 / truth / no `any`/`as`/`!`** — file uses `import type`, no assertions.
- **US-01 / key_link / SignatureService → SignatureProviderPort** — `signatureService.ts:14,46` declares `provider: SignatureProviderPort`.

### US-02 — EventMap
- **US-02 / truth / 8 events typed in EventMap** — verified at `hussle-app-dispatch-api/src/shared/messaging/eventMap.ts:228-280` (`agreement.generated`, `agreement.signed`, `agreement.declined`, `agreement.expired`, `agreement.voided`, `agreement.finalized`, `signature.submission.created`, `signature.submission.failed`).

### US-03 — env vars
- **US-03 / truth / SIGNATURE_PROVIDER union with 'mock' default** — `env.ts:58` casts to `'mock' | 'docuseal'`.
- **US-03 / truth / DOCUSEAL_BASE_URL/API_KEY/WEBHOOK_SECRET resolve to string** — `env.ts:59-61`.
- **US-03 / truth / WATCHDOG_INTERVAL_MIN=5, STALE_THRESHOLD_MIN=10 defaults** — `env.ts:63-67` `parseInt(getEnv(..., '5'/'10'), 10)`.
- **US-03 / .env.example documented** — `hussle-app-dispatch-api/.env.example:85-102` documents all six vars (SIGNATURE_PROVIDER, DOCUSEAL_*, AGREEMENT_WATCHDOG_*).

### US-04 — Prisma model + migration
- **US-04 / artifact / schema.prisma Agreement model + enums** — `prisma/schema.prisma:1527-1573`. AgreementStatus has 6 values (DRAFT/PENDING/SIGNED/VOIDED/EXPIRED/DECLINED), AgreementTemplateKey has DISPATCH_AGREEMENT, model has unique `providerSubmissionId`, three indexes including `[status, updatedAt]`, FKs to Organization + Carrier + User (×2).
- **US-04 / artifact / migration committed** — `prisma/migrations/20260514204734_add_agreement_model/` present.
- **US-04 / truth / Agreement, AgreementStatus, AgreementTemplateKey importable from @prisma/client** — re-exported via `agreementTypes.ts:1-4`.
- **US-04 / truth / unique providerSubmissionId** — `schema.prisma:1547` `@unique`.
- **US-04 / truth / Carrier.agreements back-relation** — `schema.prisma:635 agreements Agreement[]`.
- **US-04 / key_link / Agreement → Carrier (FK on carrierId)** — `schema.prisma:1566`.
- **US-04 / key_link / Agreement → Organization (FK on organizationId)** — `schema.prisma:1565`.

### US-05 — providers + contract test
- **US-05 / artifact / mockSignatureProvider.ts** — present, 173 lines at `src/shared/signatures/mockSignatureProvider.ts`. Deterministic SHA-256 ID, in-memory Map, failure markers (TIMEOUT@/RATELIMIT@/NOTFOUND/DECLINE@), test helpers.
- **US-05 / artifact / docusealProvider.ts** — present, 311 lines (post-FIX-01 shape).
- **US-05 / artifact / mockSignatureProvider.test.ts** — present at `__tests__/mockSignatureProvider.test.ts`.
- **US-05 / artifact / signatureProviderContract.test.ts** — present, exports `runSignatureProviderContract`.
- **US-05 / artifact / docusealProvider.test.ts** — present, 14 tests post-FIX-01.
- **US-05 / truth / mock createSubmission deterministic** — `mockSignatureProvider.ts:44-49` SHA-256 of input → `mock_${digest.slice(0,16)}`.
- **US-05 / truth / mock failure markers** — `mockSignatureProvider.ts:56-61, 81-83, 89-92`.
- **US-05 / truth / mock embed URL `/dev/sign/{id}`** — `mockSignatureProvider.ts:75`.
- **US-05 / truth / DocuSeal POST {baseUrl}/api/submissions with X-Auth-Token** — `docusealProvider.ts:130-160`. (Body shape detail superseded by FIX-01 — see below.)
- **US-05 / truth / DocuSeal retries on 5xx with 1s/5s/30s backoff; 4xx surfaces immediately** — `docusealProvider.ts:24, 87-127` (`callWithRetry` loop, 4xx throws at line 110 without sleep).
- **US-05 / key_link / mockSignatureProvider implements SignatureProviderPort** — return type at `mockSignatureProvider.ts:52`.
- **US-05 / key_link / docusealProvider implements SignatureProviderPort** — return type at `docusealProvider.ts:75`.

### US-07 — signatureService
- **US-07 / artifact / signatureService.ts** — present, 161 lines at `src/shared/signatures/signatureService.ts`.
- **US-07 / artifact / signatureService.test.ts** — present in `__tests__/`.
- **US-07 / truth / correlationId generation when omitted** — `signatureService.ts:130 opts?.correlationId ?? uuid()`.
- **US-07 / truth / signature.submission.created emitted on success** — `signatureService.ts:58-75, 137`.
- **US-07 / truth / retry 3x with 1000/5000/30000 then fail event** — `signatureService.ts:24, 96-123`. After exhaustion at `attempt === RETRY_DELAYS_MS.length`, `emitFailed` then rethrow at line 104-107.
- **US-07 / truth / fire-and-forget event publish** — `eventBus.publish(...).catch(logger.warn)` at `signatureService.ts:63-74, 82-94`.
- **US-07 / truth / get/void/fetch proxy without retry/events** — `signatureService.ts:141-159` no retry/event call.
- **US-07 / key_link / signatureService → SignatureProviderPort** — `signatureService.ts:46`.
- **US-07 / key_link / signatureService → EventBus** — `signatureService.ts:47, 64, 83`.

### US-08 — signature module composition root
- **US-08 / artifact / compositionRoot.ts (signatures)** — present, 55 lines at `src/shared/signatures/compositionRoot.ts`.
- **US-08 / artifact / index.ts barrel** — present, 41 lines at `src/shared/signatures/index.ts`.
- **US-08 / truth / createSignatureModule selects provider via env switch** — `compositionRoot.ts:28-44` switch with exhaustive `never` default.
- **US-08 / truth / mock vs docuseal selection** — `compositionRoot.ts:30-38`.
- **US-08 / truth / index.ts side-effect bootstrap** — `index.ts:10-26` lazy singleton via `cached` + `init()`.
- **US-08 / key_link / signatureModule.service → SignatureProviderPort** — `compositionRoot.ts:46-53`.
- **US-08 / key_link / agreements module imports `getSignatureService`** — `agreements/index.ts:5,44`.

### US-09 — Agreement repo + types
- **US-09 / artifact / agreementRepoPort.ts** — present at `src/agreements/types/agreementRepoPort.ts:9-17`. 7 methods.
- **US-09 / artifact / agreementTypes.ts** — present, derives from `@prisma/client` at line 1.
- **US-09 / artifact / agreementRepositoryPrisma.ts** — present, 89 lines.
- **US-09 / truth / round-trip create/findById/update/findByProviderSubmissionId** — all 4 methods present at `agreementRepositoryPrisma.ts:14-35, 72-73`.
- **US-09 / truth / findStaleInProgress with PENDING + updatedAt < threshold** — `agreementRepositoryPrisma.ts:75-78`.
- **US-09 / truth / findManyByOrg requires organizationId** — type-enforced via `ListAgreementsFilters.organizationId: string` at `agreementTypes.ts:37`.
- **US-09 / key_link / agreementRepositoryPrisma → PrismaClient.agreement** — `agreementRepositoryPrisma.ts:11-12, 14-89`.
- **US-09 / key_link / AgreementRepoPort → Agreement (Prisma)** — re-exported at `agreementTypes.ts:1-4`.

### US-10 — agreement services
- **US-10 / artifact / requestAgreement.ts** — present, 158 lines at `src/agreements/services/requestAgreement.ts`. Note: `renderDispatchAgreement` removed per FIX-02.
- **US-10 / artifact / voidAgreement.ts** — present, 88 lines.
- **US-10 / artifact / finalizeAgreement.ts** — present, 108 lines.
- **US-10 / artifact / requestAgreement.test.ts** — present.
- **US-10 / artifact / voidAgreement.test.ts** — present.
- **US-10 / artifact / finalizeAgreement.test.ts** — present.
- **US-10 / truth / requestAgreement throws AgreementAlreadyPendingError when PENDING exists** — `requestAgreement.ts:74-82` (`AgreementAlreadyPendingError` extends CustomError, code `AGREEMENT_ALREADY_PENDING` per `errors/agreementErrors.ts`).
- **US-10 / truth / requestAgreement persists PENDING + emits agreement.generated** — `requestAgreement.ts:118-156`.
- **US-10 / truth / voidAgreement throws on non-PENDING** — `voidAgreement.ts:46-48` (uses `AgreementNotVoidableError`; see WARNING re. response code).
- **US-10 / truth / voidAgreement calls voidSubmission then update + emits voided** — `voidAgreement.ts:50-86`.
- **US-10 / truth / finalizeAgreement idempotent on SIGNED** — `finalizeAgreement.ts:56-62` short-circuits with `events: []`.
- **US-10 / truth / finalizeAgreement: fetch artifacts → SHA-256 → storage.put × 2 → update SIGNED → emit agreement.finalized** — `finalizeAgreement.ts:64-105`.
- **US-10 / key_link / requestAgreement → signatureService.createSubmission** — `requestAgreement.ts:105-116` with `correlationId` opts.
- **US-10 / key_link / requestAgreement → agreementRepo.countActivePending + .create** — `requestAgreement.ts:74-78, 118-131`.
- **US-10 / key_link / finalizeAgreement → storage.put** — `finalizeAgreement.ts:71-72`.

### US-11 — HTTP layer
- **US-11 / artifact / 4 controllers** — all present at `src/agreements/controllers/{request,get,list,void}AgreementController.ts`.
- **US-11 / artifact / 3 mappers** — all present at `controllers/mappers/`.
- **US-11 / artifact / agreementTransformer.ts** — present, 99 lines.
- **US-11 / artifact / 4 validator files** — present (id-only, void, request, list).
- **US-11 / artifact / agreementRoutes.ts** — present, 64 lines, mounts 4 routes with requireAuth + requireRole + validateRequest.
- **US-11 / artifact / agreementRoutes.integration.test.ts** — present.
- **US-11 / truth / POST /api/v1/agreements 201 with PENDING + embedUrl** — `requestAgreementController.ts:51` returns 201 + transformed data.
- **US-11 / truth / POST 409 on duplicate PENDING** — `AgreementAlreadyPendingError` (statusCode 409 per `errors/agreementErrors.ts`).
- **US-11 / truth / GET refreshes expired embed URL** — `getAgreementController.ts:41-57` calls `signatureService.refreshEmbedUrl` and persists.
- **US-11 / truth / GET signed returns artifacts with presigned URLs (15-min TTL)** — `agreementTransformer.ts:5, 64-67` `PRESIGN_TTL_SECONDS = 15*60`.
- **US-11 / truth / GET supports filters + pagination** — `listAgreementsValidator.ts:9-23` + `listAgreementsController.ts:23-43`.
- **US-11 / truth / requireRole([ADMIN, DISPATCHER])** — `agreementRoutes.ts:33,41,49,57`.
- **US-11 / key_link / agreementRoutes → controllers w/ middleware stack** — `agreementRoutes.ts:30-60`.
- **US-11 / key_link / agreementTransformer → storage.getPresignedGetUrl** — `agreementTransformer.ts:64-67`.

### US-12 — DocuSeal webhook
- **US-12 / artifact / verifyDocusealHmacMiddleware.ts** — present, 82 lines.
- **US-12 / artifact / docusealWebhookController.ts** — present, 148 lines.
- **US-12 / artifact / docusealWebhookRoutes.ts** — present, 45 lines.
- **US-12 / artifact / docusealWebhook.integration.test.ts** — present at `__tests__/integration/`.
- **US-12 / truth / valid HMAC + form.completed → 200** — `docusealWebhookController.ts:97-111` publishes `agreement.signed` and returns `{received:true}`.
- **US-12 / truth / missing/wrong signature → 401, body never parsed** — `verifyDocusealHmacMiddleware.ts:35-39, 62-69` returns 401 before `JSON.parse` at line 72.
- **US-12 / truth / crypto.timingSafeEqual** — `verifyDocusealHmacMiddleware.ts:64`.
- **US-12 / truth / replay protection — already-target-status returns `replayed: true` no event** — `docusealWebhookController.ts:83-93`.
- **US-12 / truth / event_type → state mapping** — `docusealWebhookController.ts:27-32` `TARGET_BY_EVENT` map matches contract literals.
- **US-12 / key_link / verifyDocusealHmacMiddleware ← env.DOCUSEAL_WEBHOOK_SECRET** — `compositionRoot.ts:131-134` injects secret.
- **US-12 / key_link / docusealWebhookController → eventBus** — `docusealWebhookController.ts:99, 113, 129`.

### US-13 — subscriber + watchdog
- **US-13 / artifact / agreementSignedSubscriber.ts** — present, 70 lines.
- **US-13 / artifact / signedAgreementWatchdog.ts** — present, 163 lines.
- **US-13 / artifact / agreementSignedSubscriber.test.ts** — present.
- **US-13 / artifact / signedAgreementWatchdog.test.ts** — present.
- **US-13 / truth / subscriber consumes agreement.signed → finalizeAgreement; idempotent silently** — `agreementSignedSubscriber.ts:27-32` calls finalize, returns void on empty events.
- **US-13 / truth / failures logged + rethrown for RabbitMQ retry** — `agreementSignedSubscriber.ts:60-66`.
- **US-13 / truth / watchdog scans every interval; reconciles stale PENDING** — `signedAgreementWatchdog.ts:47-137`. Cron `*/${intervalMin} * * * *` at line 143.
- **US-13 / truth / watchdog republishes agreement.signed (idempotent finalize)** — `signedAgreementWatchdog.ts:71-80`.
- **US-13 / truth / start/stop/runNow interface** — `signedAgreementWatchdog.ts:139-162`.
- **US-13 / key_link / subscriber → finalizeAgreement** — `agreementSignedSubscriber.ts:9-12, 29`.
- **US-13 / key_link / watchdog → signatureService.getSubmission + eventBus.publish** — `signedAgreementWatchdog.ts:68, 72/86/99/112`.

### US-14 — agreements composition + app.ts wiring
- **US-14 / artifact / agreements/compositionRoot.ts** — present, 171 lines.
- **US-14 / artifact / agreements/index.ts** — present, 62 lines.
- **US-14 / artifact / app.ts wired** — `src/app.ts:34` side-effect import; `:129` mounts `/api/v1/agreements`; `:168` mounts `/webhooks` before errorHandler.
- **US-14 / truth / single createAgreementsModule wires everything** — `compositionRoot.ts:61-170`.
- **US-14 / truth / app.ts mounts both routes** — confirmed via grep.
- **US-14 / truth / watchdog start/stop wired through index.ts shutdown** — `src/index.ts:8 import { stopAgreements }`, `:21` calls in shutdown.
- **US-14 / key_link / app.ts → agreementsRouter + docusealWebhookRouter** — `app.ts:34, 129, 168`.
- **US-14 / key_link / agreementsModule → signatureModule (`getSignatureService`)** — `agreements/index.ts:5, 44`.
- **US-14 / key_link / agreementsModule → carrierQueries cross-module read** — `compositionRoot.ts:13, 63`. Implementation in `queries/carrierQueries.ts` adapts Carrier schema (name → legalName, primaryContact relation → name+email).

### US-15 — DocuSeal local docker + staging stub
- **US-15 / artifact / docker-compose.local.yml** — DocuSeal block at lines 60-71 with `image: docuseal/docuseal:latest`, `ports: 3030:3000`, `volumes: docuseal_data`, `profiles: ["docuseal"]`.
- **US-15 / artifact / staging stub** — `hussle-app-dispatch-infra/dokploy/docuseal-staging.md` present (markdown declaration, declared but not auto-applied).
- **US-15 / truth / docker compose --profile docuseal up boots DocuSeal** — `profiles: ["docuseal"]` at line 71 confirms profile-gating.
- **US-15 / truth / docuseal-staging.fleetcommand.app deploy stub** — `docuseal-staging.md` includes Traefik labels for that hostname.

### INT-01 — webhook ↔ state machine
- **INT-01 / 7/7 checklist items** — webhook at `/webhooks` (not /api/v1), HMAC chain in correct order (raw → shuffle → verify → controller), DocuSealEventType matches contract verbatim, AgreementStatus matches schema, event mapping correct, subscriber wired with queue group `agreements.signed-finalizer`, watchdog republishes signed without inline row update. Full trace at task T-44 output line 1801 in tasks.md.

### VER-01 — original verification
- **VER-01 / supersede note** — original VERIFICATION.md replaced by this report; 14/14 ACs were green at T-45 prior to FIX cycle. FIX cycle preserved or strengthened all critical paths (templates moved from code to data; embedded signing UI scoped out as separate phase).

### FIX-01 — DocuSeal template_id shape
- **FIX-01 / artifact / dispatchAgreementFields.ts** — present, 22 lines, `Object.freeze` const + `DispatchAgreementFieldName` type at `src/agreements/templates/dispatchAgreementFields.ts:12-21`.
- **FIX-01 / artifact / docusealProvider.ts uses template_id** — `docusealProvider.ts:141-152` body has `template_id, send_email, submitters: [{ name, email, role: 'Carrier', values }]`. No `template_html` (verified via grep — only test assertion `expect(parsedBody.template_html).toBeUndefined()` at test line 123).
- **FIX-01 / artifact / docusealProvider.test.ts updated** — present, 14 tests, asserts new request shape (test line 122 `expect(parsedBody.template_id).toBe(7)`).
- **FIX-01 / artifact / signatures/compositionRoot.ts passes templateId** — `compositionRoot.ts:36` `templateId: deps.env.DOCUSEAL_DISPATCH_TEMPLATE_ID`.
- **FIX-01 / artifact / env.ts DOCUSEAL_DISPATCH_TEMPLATE_ID** — `env.ts:62` `parseInt(getEnv('DOCUSEAL_DISPATCH_TEMPLATE_ID', '0'), 10)`.
- **FIX-01 / artifact / .env.example documented** — `.env.example:95-96` documents var with runbook reference.
- **FIX-01 / truth / templateId fail-fast on 0** — `docusealProvider.ts:135-139` throws clear error before HTTP call.
- **FIX-01 / truth / values keys match DISPATCH_AGREEMENT_FIELDS** — `requestAgreement.ts:86-92` uses bracket syntax with constants.
- **FIX-01 / key_link / docusealProvider ← env.DOCUSEAL_DISPATCH_TEMPLATE_ID** — wired through `signatures/compositionRoot.ts:36` and `signatures/index.ts:19`.
- **FIX-01 / key_link / createSubmission body → DISPATCH_AGREEMENT_FIELDS** — service builds `variables: Record<DispatchAgreementFieldName, string>` at `requestAgreement.ts:86`.

### FIX-02 — drop renderDispatchAgreement from requestAgreement
- **FIX-02 / artifact / requestAgreement.ts rewritten** — present; no `renderDispatchAgreement` import (grep returned 0 hits).
- **FIX-02 / artifact / requestAgreement.test.ts updated** — `__tests__/requestAgreement.test.ts:12` imports DISPATCH_AGREEMENT_FIELDS.
- **FIX-02 / artifact / agreements compositionRoot.ts cleaned** — no `renderDispatchAgreement` reference.
- **FIX-02 / truth / requestAgreement does not import renderDispatchAgreement** — confirmed via grep.
- **FIX-02 / truth / no html in metadata to createSubmission** — `requestAgreement.ts:110-113` metadata is `{ carrierId, organizationId }` only.
- **FIX-02 / truth / variables keys typed via DispatchAgreementFieldName** — `requestAgreement.ts:86`.
- **FIX-02 / key_link / requestAgreement → DISPATCH_AGREEMENT_FIELDS** — `requestAgreement.ts:4-6`.
- **FIX-02 / key_link / requestAgreement → signatureService.createSubmission with no html** — `requestAgreement.ts:105-116`.

### FIX-03 — delete React Email template
- **FIX-03 / truth / agreementTemplates/ directory does not exist** — `test -d` returns DELETED.
- **FIX-03 / truth / no `agreementTemplates` or `renderDispatchAgreement` references** — `grep -rn` against `src/` returns 0 hits.

### FIX-04 — DocuSeal template runbook
- **FIX-04 / artifact / docs/runbooks/docuseal-template-setup.md** — present, ~163 lines. Covers why-this-exists (Open Source paywall), prerequisites, 7 setup steps, field reference table mapping all 5 DISPATCH_AGREEMENT_FIELDS constants → DocuSeal field names, per-environment matrix, 7-item troubleshooting (incl. 422 template_id-required case at line 134).
- **FIX-04 / truth / runbook field names verbatim match DISPATCH_AGREEMENT_FIELDS** — runbook lines 57-61 match `dispatchAgreementFields.ts:12-18` character-for-character.
- **FIX-04 / truth / per-env workflow documented** — line 82 + per-env matrix table.
- **FIX-04 / truth / 422 + missing-field troubleshooting present** — lines 134-153.

---

## VERIFIED-AS-SUPERSEDED (7)

- **US-05 / truth / "DocuSeal POSTs ... `template_html` body"** — superseded by **FIX-01**; new body shape `{ template_id, send_email, submitters[].values }` at `docusealProvider.ts:141-152`. The original truth referenced template_html which DocuSeal Open Source rejects with 422; the new shape is verified at FIX-01 above.
- **US-06 / artifact / `dispatchAgreement.tsx`** — superseded by **FIX-03**; file deleted intentionally on commit per task T-54.
- **US-06 / artifact / `renderDispatchAgreement.ts`** — superseded by **FIX-03**; file deleted.
- **US-06 / artifact / `__tests__/renderDispatchAgreement.test.ts`** — superseded by **FIX-03**; file + snapshot deleted.
- **US-06 / truth / "renderDispatchAgreement returns HTML string"** — superseded by **FIX-03**; templates now data-only in DocuSeal admin per environment, governed by DISPATCH_AGREEMENT_FIELDS constants.
- **US-06 / truth / "Rendered HTML contains DocuSeal text-tag anchors"** — superseded by **FIX-03**; field placement is configured in DocuSeal template UI, not in code.
- **US-06 / truth / "Snapshot test guards against unintended HTML changes"** — superseded by **FIX-03**; snapshot file deleted with renderer. New guard: TS-typed `DISPATCH_AGREEMENT_FIELDS` + runbook reference table at `docs/runbooks/docuseal-template-setup.md:57-61`.

---

## FAILED (BLOCKER) (0)

None. All artifacts present, all truths traceable, all key_links wired.

---

## UNCERTAIN (WARNING) (7)

- **US-04 / migration content** — migration directory exists at `prisma/migrations/20260514204734_add_agreement_model/` but `migration.sql` body not opened in this verification. Recommend: spot-check the SQL contains the 6 enum values, 3 indexes, 4 FKs.
- **US-11 / truth / "transparent embed URL refresh: first call returns stale, immediate second call returns refreshed"** — code at `getAgreementController.ts:41-57` refreshes when expired BEFORE returning, so the first GET already returns refreshed (not stale). Behavior is the safer outcome but contradicts the literal must_haves text. Recommend: confirm integration test asserts the actual returned URL contract; tasks.md prose may need a minor edit.
- **US-11 / truth / "POST /agreements integration tests run against real Postgres"** — task T-31 output notes "Real Postgres deferred to US-14 wiring; in-process Express + mocked services". The integration test exercises HTTP layer wiring without DB. Recommend: a smoke test against real DB (curl/Playwright) before staging deploy.
- **US-13 / truth / "watchdog runs every AGREEMENT_WATCHDOG_INTERVAL_MIN"** — `node-cron` schedule string `*/${intervalMin} * * * *` is registered, but actual scheduling depends on node-cron loading correctly in production. Recommend: smoke-check on staging that watchdog logs "Agreement watchdog scheduled" + "Agreement watchdog scanning" within the first few minutes.
- **US-14 / truth / "watchdog start/stop wired into SIGTERM/SIGINT"** — `index.ts:21 stopAgreements()` is called in shutdown. Order vs. `eventBus.close()` is set in the index.ts shutdown sequence but only manual SIGTERM testing confirms it runs cleanly. Recommend: local SIGTERM test, confirm watchdog stop log appears before bus close.
- **US-15 / truth / "Running docker compose ... boots a DocuSeal container on :3030"** — service definition is correct in YAML, but actual boot was not verified in this review. Recommend: `docker compose -f docker-compose-prod.yml -f docker-compose.local.yml --profile docuseal up docuseal` smoke test.
- **US-10 / truth / "voidAgreement throws InvalidTransitionError (409, code INVALID_STATUS_TRANSITION)"** — implementation throws `AgreementNotVoidableError` (local class in `errors/agreementErrors.ts`) instead of the canonical shared `InvalidTransitionError` (which would return 422 in this codebase per T-22 output). Behavior is correct (rejects non-PENDING void) but the response code may differ from the must_haves literal. Recommend: confirm the integration test asserts actual returned status and that 409-vs-422 contract matches FE expectations.

---

## Notes

- Spec deviations noted in prior VER-01 (15 items) remain accepted by user; this report does not re-litigate them. The FIX cycle (which arose from those deviations) is fully verified above.
- Carrier-portal-v2 embed widget is a separate phase (per user decision 2026-05-15). Backend's job ends at returning `embed_src` from POST /api/v1/agreements — that path is verified at `requestAgreementController.ts:51` + `agreementTransformer.ts:21,82-84`.
- Pre-existing `lint:deps` errors in `src/notifications/services/__tests__/tenantScoping.test.ts` not introduced by this feature.
- Pre-existing TS test compile failures in `src/loads/`, `src/sms-prompts/` are unrelated Prisma drift.
- Architecture flip (templates-as-code → templates-as-data) on 2026-05-15 deleted `src/shared/signatures/agreementTemplates/` (4 files) intentionally. New source of truth: DocuSeal admin UI templates per environment, with field names governed by `dispatchAgreementFields.ts` + runbook.
