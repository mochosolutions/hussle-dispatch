import { PageHeader, PageWrapper } from '@mocho/ui/components';

const ContactsIndexPage = () => (
  <PageWrapper errorContext="ContactsIndexPage">
    <PageHeader title="Contacts" />
  </PageWrapper>
);

export default ContactsIndexPage;
