// Re-export contact types from carrier types (single source of truth)
export type {
  Contact,
  ContactType,
  CreateContactInput,
  UpdateContactInput,
  PaginationMeta,
} from 'features/carrier/types';
