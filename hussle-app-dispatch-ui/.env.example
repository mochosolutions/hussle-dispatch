VITE_API_URL=http://localhost:3001
# WebSocket gateway origin (ROLE=ws-gateway, host port 3004). Must point at the
# gateway, not the API — required for dispatcher + driver-portal live updates.
VITE_WS_URL=http://localhost:3004
