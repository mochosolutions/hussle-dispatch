# Project Concerns

> Auto-generated by /bootstrap on 2026-03-24. Re-run /bootstrap to refresh.

## Critical
- **Hardcoded AWS credentials** in `src/utils/s3.ts` (lines 8-9, 93-94, 113-114) — access key and secret are committed to source. Rotate these credentials immediately and move to environment variables or Chrome extension storage.
- **No test command detected** — `package.json` test script is a placeholder (`echo "Error: no test specified" && exit 1`). No test runner installed.

## Warning
- **No CLAUDE.md found** at project root — agents lack project-specific context
- **No lint command or ESLint configured** — no code quality enforcement
- **Heavy use of `any` types** — `removeDuplicates.ts`, `s3.ts`, `redux/index.ts` all use untyped parameters extensively
- **No `.env` or `.env.example` files** — AWS credentials and config are hardcoded rather than externalized
- **Unused dependencies** — `cheerio` and `ioredis` are in package.json but not imported in source files
- **`var` usage** — `contentScript.ts` and `redux/index.ts` use `var` instead of `const`/`let`
- **Embedded test data** — `removeDuplicates.ts` contains ~500 lines of hardcoded DAT API sample data (`datData` export)

## Info
- **No Docker or CI/CD configuration detected**
- **No observability libraries detected** — only `console.log`/`console.error` for debugging
- **Project appears dormant** — last file modification was April 2023
- **Mixed JS/TS** — `formatters/index.js` and `test-script.js` are plain JavaScript alongside TypeScript files
