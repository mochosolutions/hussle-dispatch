# Coding Conventions

> Auto-generated by /bootstrap on 2026-03-24.

## dat-load-scraper

| Convention | Pattern | Example |
|-----------|---------|---------|
| Module system | ES modules (`import`/`export`) | `import {store} from "../redux"` |
| State management | Redux Toolkit + Redux Saga | `configureStore`, `takeEvery`, `yield put/call/select` |
| Property access | `lodash.get` for nested access | `get(datLoad, ['tripMiles'])` |
| DOM manipulation | Direct DOM API in sagas | `document.querySelector`, `el.setAttribute` |
| Variable declarations | Mixed `const`/`let`/`var` (inconsistent) | `var s = document.createElement('script')` |
| Type annotations | Loose — heavy use of `any` | `(loadArr: any, ratePerMile: number)` |
| Error handling | try/catch with `console.log`/`console.error` | `catch (e) { console.log("error", e) }` |
| Naming | camelCase functions/variables | `calculateMiniumAcceptableOffer`, `findDuplicatesId` |
| String formatting | Template literals | `` `${formattedCompanyName}_${equipmentTypeCode}` `` |
| Export style | Named exports | `export const`, `export class` |
| CSS | Injected via `document.createElement("style")` + inline CSS strings | `addCustomStyles(css)` |
| React components | Functional components, arrow functions | `const Popup = () => { ... }` |
| Form handling | Formik with inline config | `<Formik initialValues={{...}} onSubmit={...}>` |
| Currency formatting | `Intl.NumberFormat` factory pattern | `numberFormatterFactory({currency: 'USD'})` |
| Chrome APIs | Direct `chrome.runtime` / `chrome.tabs` calls | `chrome.runtime.onMessage.addListener` |
| Message passing | `window.postMessage` between content/injected scripts | `window.postMessage(saveDatLoadsToStore(datLoads))` |
