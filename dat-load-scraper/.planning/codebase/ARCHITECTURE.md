# Architecture Overview

> Auto-generated by /bootstrap on 2026-03-24. Update manually as the project evolves.

## System Type
JavaScript: single-package with 1 package

## Package Topology
| Package | Type | Stack | Depends On | Depended By |
|---------|------|-------|------------|-------------|
| dat-load-scraper | frontend (Chrome Extension) | TypeScript, React, Redux Toolkit, Redux Saga, Webpack | DAT Freight API, AWS S3/SQS | — |

## Technology Stack

### Runtime & Frameworks
- TypeScript 4.8 compiled via Webpack + ts-loader
- React 18 (popup UI)
- Redux Toolkit + Redux Saga (state management)
- Chrome Extension Manifest V3

### Data Layer
- AWS S3 (`dat-load` bucket) for load data storage
- AWS SQS FIFO queue (`hustle-dat-queue.fifo`) for pipeline
- ioredis (declared in package.json, unused in source)

### Infrastructure
- Webpack 5 (dev/prod configs)
- No Docker, no CI/CD detected

## Data Flow
- DAT Load Board (browser) → XHR intercept (script.ts) → `window.postMessage` → Content Script → Redux Store (saga processing)
- Redux Saga → DOM manipulation (hide duplicates, inject min rate elements)
- Popup UI → `chrome.tabs.sendMessage` → Content Script → Redux filter updates
- Load data → AWS S3/SQS pipeline (via `SqsProducer`)

## Key Patterns
- **XHR Monkey-patching**: Intercepts `XMLHttpRequest.prototype.open/send` to capture DAT API responses
- **Redux Saga side effects**: DOM mutations (duplicate removal, rate injection) handled as saga side effects
- **Composite dedup key**: Loads deduplicated by company + equipment + origin + destination + weight + length
- **Minimum rate calculation**: `tripMiles * ratePerMile * (1 + additionalFee)`

## Entry Points
| Package | Dev Command | Main Entry |
|---------|-------------|------------|
| dat-load-scraper | `npm run dev` (webpack --watch) | src/contentScript/contentScript.ts, src/popup/index.tsx |
